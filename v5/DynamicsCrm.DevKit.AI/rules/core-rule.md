# Core Project Rules

The core project rules are maintained in the repository root `AGENTS.md` so every supported AI client receives them automatically.

Read `AGENTS.md` first. Use the files under `DynamicsCrm.DevKit.AI/workflows/` for the detailed executable procedures.

# Tóm tắt
Tool: `manage_form`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageFormTool.cs`
Title hiện tại: Manage entity forms
Mô tả hiện trạng: Retrieve and modify form definitions for a Dataverse entity.
Token hiện tại: 494 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Retrieve and modify form definitions for a Dataverse entity.

ACTIONS:
- action='list': List active forms. Optional: form_type, include_formxml
- action='detail': Full FormXML + metadata. Required: form_id (or form_name for auto-resolve)
- action='update': Replace FormXML. Required: form_id + formxml
- action='rename': Change display name. Required: form_id + form_name
- action='undo': Restore from backup. Required: form_id + formxml (= backup file path)

WORKFLOW: build_form_xml → manage_form(action='update', formxml=<result>)
SAFETY: auto-backup before update/rename, XSD validates before write, backup failure blocks update.
ALWAYS use build_form_xml to construct FormXML — never write it manually.

TIPS:
- form_type=2 for main forms only
- form_name with 1 match auto-returns detail
- Read schema://formxml for XSD. Read docs://instructions_for_formxml for rules
- Set auto_publish=false when batching, then call publish_customizations once
```

Params hiện tại:
```text
action: string
The action to perform: 'list', 'detail', 'update', 'rename', or 'undo'.

entity_name: string
Entity logical name (e.g., 'account'). Use get_tables if unsure.

form_id: string = ""
GUID of a form. Required for detail/update/rename/undo. Empty for list.

form_name: string = ""
Filter by name (contains match). 1 match = auto-detail. Ignored if form_id set.

form_type: int = 0
Filter by type: 2=Main, 5=Mobile, 6=QuickView, 7=QuickCreate. 0 = all.

include_formxml: bool = false
Include FormXML in list mode (default: false). Detail mode always includes it.

formxml: string = ""
For 'update': FormXML string or file path from build_form_xml (.formxml). For 'undo': backup file path. Auto-detects file vs inline XML.

validate: bool = true
Validate against XSD before writing (default: true). Blocks if invalid.

backup: bool = true
Backup current FormXML before overwriting (default: true). Backup failure blocks update.

auto_publish: bool = true
Publish after changes (default: true). Set false when batching.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List/detail/update/rename/undo Dataverse forms. Updates replace FormXML from build_form_xml or backup, with optional XSD validation, backup and publish.

ACTIONS: list, detail, update, rename, undo.

WHEN TO USE:
- Find form IDs/XML.
- Apply build_form_xml output.
- Rename forms or use undo from backup safely.

MODE/CONVENTION:
- action=list with form_name contains: 1 match auto-detail; >1 returns candidates.
- detail/update/rename/undo should use form_id when ambiguous.

FUZZY/AMBIGUITY:
- form_name is contains match. Exactly 1 auto-details; multiple matches require form_id.

SAFETY:
- Use build_form_xml; backup/validation block unsafe updates by default.
```

Params đề xuất:
```text
action (string, required): list|detail|update|rename|undo.
entity_name (string, required): Table logical name.
form_id (string=""): Required for detail/update/rename/undo.
form_name (string=""): Contains filter; single match auto-details.
form_type (int=0): 2 main, 5 mobile, 6 quick view, 7 quick create, 0 all.
include_formxml (bool=false): Include XML in list; detail always includes.
formxml (string=""): Update XML/path or undo backup path.
validate (bool=true): XSD validate before write.
backup (bool=true): Backup current XML before mutation.
auto_publish (bool=true): Publish after mutation.
```

# Tổng kết
Token trước optimize: 494
Token sau optimize: 307
Tiết kiệm: 187 tokens (37.9%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `build_sitemap_xml`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/BuildSiteMapXmlTool.cs`
Title hiện tại: Build SiteMap XML with areas, groups, and subareas
Mô tả hiện trạng: Build modified SiteMap XML for a Model-Driven App (areas/groups/subareas).
Token hiện tại: 379 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Build modified SiteMap XML for a Model-Driven App (areas/groups/subareas). READ-ONLY — returns XML; use manage_sitemap(action='update') to apply.

12 OPERATIONS (pass as JSON array in 'operations'):
- add_area: label req. Optional: id, icon, show_groups, groups[]
- add_group: area+label req. Optional: id, is_profile, subareas[]
- add_subarea: area+group+(entity or url) req. Optional: id, label, icon, pass_params, default_dashboard, vector_icon, client
- remove_area: area req.
- remove_group: area+group req.
- remove_subarea: area+group+subarea req.
- update_area: area req. Optional: label, icon, show_groups
- update_group: area+group req. Optional: label, is_profile
- update_subarea: area+group+subarea req. Optional: label, entity, url, icon, pass_params, default_dashboard, vector_icon, client
- move_area: area+position req.
- move_group: area+group+position req.
- move_subarea: area+group+subarea+position req.

position values: 'first', 'last', 'after:<id>'. Auto-generates IDs (area_, group_, sa_). Fuzzy lookup by ID or label.
Read schema://sitemapxml for SiteMap XML structure and rules.
```

Params hiện tại:
```text
app: string
Model-Driven App name (fuzzy match) or GUID. Resolves to app_module_id and retrieves current SiteMap XML.

operations: string
JSON array of operation objects. Each requires 'action' + operation-specific fields (see tool description for required/optional fields per action).
Example: [{"action":"add_subarea","area":"Sales","group":"Customers","entity":"account"}]
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Read-only SiteMap XML builder for a Model-Driven App. JSON operations add/update/remove/move areas, groups and subareas with fuzzy ID/label matching; pass output to manage_sitemap.

WHEN TO USE:
- Generate SiteMap changes instead of hand-editing XML.
- Add/move/update/remove areas, groups and subareas.
- Prepare XML for manage_sitemap.

MODE/CONVENTION:
- app accepts GUID or contains-name lookup; exact name wins when present.
- Multiple app matches are an error; ask for exact name/GUID.

FUZZY/AMBIGUITY:
- app is contains-matched against app name. Multiple app matches require exact app name or GUID.

SAFETY:
- Read-only builder; does not apply changes until manage_sitemap.
```

Params đề xuất:
```text
app (string, required): App name fuzzy match or app GUID.
operations (string, required): JSON array; actions add/update/remove/move area/group/subarea with required identifiers and optional labels/icons/entity/url/position.
```

# Tổng kết
Token trước optimize: 379
Token sau optimize: 207
Tiết kiệm: 172 tokens (45.4%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

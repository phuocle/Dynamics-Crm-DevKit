# Tóm tắt
Tool: `get_tables`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetTablesTool.cs`
Title hiện tại: Inspect table metadata, columns, and relationships
Mô tả hiện trạng: Retrieve Dataverse entity/table metadata.
Token hiện tại: 250 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Retrieve Dataverse entity/table metadata.

TWO MODES:
- entity_name EMPTY: summary table of all entities (filter by keyword, custom_only, or names list). Includes IsAuditEnabled.
- entity_name PROVIDED: full detail — attributes (type, options, required), relationships (1:N, N:1, N:N), alternate keys

COMMON NAMES: account, contact, lead, opportunity, incident (Case), systemuser (User), team, annotation (Note)

WHEN TO USE:
- Discover entity/attribute names before building FetchXML
- Find join columns, picklist options, required fields, or primary key
- Check audit settings for a set of entities (use names= with solution entity list)
```

Params hiện tại:
```text
entity_name: string = ""
Entity logical name for full detail. Empty = list all entities.

filter: string = ""
LIST: keyword filter. DETAIL: prefix filter for attributes/relationships.

custom_only: bool = false
LIST only: show only custom entities.

include_intersect: bool = false
LIST only: include intersect (N:N) entities.

names: string = ""
LIST only: comma-separated logical names to filter (e.g. 'account,contact,lead'). Overrides filter/custom_only when provided.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Inspect Dataverse table metadata. Empty entity_name lists tables; entity_name returns attributes, options, relationships and keys. Use before FetchXML, forms, views or schema changes.

WHEN TO USE:
- Discover table, column, relationship and key names.
- Check picklist options, required fields and audit settings.
- Prepare FetchXML/forms/views/schema changes.

MODE/CONVENTION:
- entity_name empty = list tables; entity_name set = detail.
- names filters by exact logical-name list; filter is contains/prefix depending on mode.
```

Params đề xuất:
```text
entity_name (string=""): Table logical name for detail; empty lists tables.
filter (string=""): List keyword or detail prefix filter.
custom_only (bool=false): List only custom tables.
include_intersect (bool=false): Include N:N intersect tables in list.
names (string=""): Comma/semicolon logical-name list; overrides filter/custom_only.
```

# Tổng kết
Token trước optimize: 250
Token sau optimize: 183
Tiết kiệm: 67 tokens (26.8%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `manage_view`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageViewTool.cs`
Title hiện tại: Manage entity views
Mô tả hiện trạng: Manage Dataverse views (savedquery/userquery) — list, read, create, update, rename, restore.
Token hiện tại: 669 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Manage Dataverse views (savedquery/userquery) — list, read, create, update, rename, restore.

ACTIONS:
- action='list': List views with name, type, status. Optional: query_type, include_fetchxml, include_personal
- action='detail': Full FetchXML + LayoutXML + metadata for one view. Requires: view_id
- action='create': New Public view. Requires: view_name + layoutxml
- action='update': Modify LayoutXML/FetchXML. Requires: view_id + (layoutxml and/or cell_updates_json)
- action='rename': Change display name. Requires: view_id + view_name
- action='undo': Restore from backup. Requires: view_id + layoutxml (= backup file path)

WORKFLOW: list → modify XMLs → update. Auto: backup → XSD+sync validate → update → publish.
SYNC RULE: Every FetchXML <attribute> MUST match a LayoutXML <cell>. Tool blocks if out of sync.

TIPS:
- querytype: 0=Public, 4=QuickFind (search bar), 64=SubGrid
- view_name: 1 exact match = auto-returns detail
- Read docs://instructions_for_views for sync rules; schema://layoutxml + schema://fetchxml for XSD
- Set auto_publish=false when batching; call publish_customizations once after
```

Params hiện tại:
```text
action: string
The action to perform: 'list', 'detail', 'create', 'update', 'rename', or 'undo'.

entity_name: string
Entity logical name (e.g., 'account'). Use get_tables if unsure.

view_id: string = ""
GUID of a view. Required for detail/update/rename/undo. Empty for list/create.

view_name: string = ""
Filter by name (contains match). 1 match = auto-detail. Ignored if view_id set.

query_type: int = -1
Filter by type: 0=Public, 1=Lookup, 4=QuickFind, 64=SubGrid. -1 = all.

include_fetchxml: bool = false
Include FetchXML/LayoutXML in list mode (default: false). Detail mode always includes.

include_personal: bool = false
Include personal views (userquery) owned by current user. Default: false.

layoutxml: string = ""
For 'update'/'create': LayoutXML. For 'undo': layout backup path. Ignored for list/detail/rename.

fetchxml: string = ""
FetchXML. Empty = keep existing (update) or auto-generate (create). For 'undo': fetch backup path.

validate: bool = true
Validate XMLs and check FetchXML<>LayoutXML sync (default: true). Blocks if invalid.

backup: bool = true
Backup current XMLs before overwriting (default: true). Backup failure blocks update.

auto_publish: bool = true
Publish after changes (default: true). Set false when batching.

cell_updates_json: string = ""
JSON array of cell attribute updates. Each item: {cell_name, set_attributes, remove_attributes}. Patch cell attributes (e.g., imageproviderwebresource, imageproviderfunctionname, ishidden) without rebuilding full LayoutXML. Can be used alone or with layoutxml.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List/detail/create/update/rename/undo Dataverse views. Handles savedquery/userquery XML, validates FetchXML/LayoutXML sync, supports backups and cell attribute patching.

WHEN TO USE:
- Find view IDs/XML.
- Create/update/rename/restore views.
- Patch LayoutXML cells while keeping FetchXML/LayoutXML synced.

MODE/CONVENTION:
- action=list with view_name contains: 1 match auto-detail; >1 returns candidates.
- detail/update/rename/undo should use view_id when ambiguous.

FUZZY/AMBIGUITY:
- view_name is contains match. Exactly 1 auto-details; multiple matches require view_id.

SAFETY:
- Validation blocks FetchXML/LayoutXML mismatch by default.
```

Params đề xuất:
```text
action (string, required): list|detail|create|update|rename|undo.
entity_name (string, required): Table logical name.
view_id (string=""): Required for detail/update/rename/undo.
view_name (string=""): Filter/name; single match auto-details.
query_type (int=-1): 0 public, 1 lookup, 4 quick find, 64 subgrid, -1 all.
include_fetchxml (bool=false): Include XML in list; detail always includes.
include_personal (bool=false): Include current user's personal views.
layoutxml (string=""): Create/update LayoutXML or undo layout backup.
fetchxml (string=""): Create/update FetchXML or undo fetch backup.
validate (bool=true): Validate XML and Fetch/Layout sync.
backup (bool=true): Backup before mutation.
auto_publish (bool=true): Publish after mutation.
cell_updates_json (string=""): Patch LayoutXML cells without rebuilding full XML.
```

# Tổng kết
Token trước optimize: 669
Token sau optimize: 343
Tiết kiệm: 326 tokens (48.7%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

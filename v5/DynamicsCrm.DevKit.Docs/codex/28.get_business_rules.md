# Tóm tắt
Tool: `get_business_rules`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetBusinessRulesTool.cs`
Title hiện tại: List business rules for an entity
Mô tả hiện trạng: List business rules (client-side logic) for a Dataverse entity.
Token hiện tại: 201 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
List business rules (client-side logic) for a Dataverse entity.

TWO MODES:
- rule_id EMPTY: list rules with name, scope, status
- rule_id PROVIDED: full detail with conditions and actions parsed from XAML

WHEN TO USE:
- Debug form behavior (fields hiding/showing unexpectedly)
- Audit client-side logic before adding JavaScript

TIPS:
- Business rules run client-side BEFORE JavaScript form events
- Scope 'Entity' means the rule runs on ALL forms
```

Params hiện tại:
```text
entity_name: string
Entity logical name (lowercase). Use get_tables to discover names.

rule_id: string = ""
GUID of a specific business rule for full detail. Leave empty to list all.

status: string = ""
Filter by status: 'active' (activated rules), 'draft' (deactivated/not yet activated). Leave empty to return all statuses.

max_records: int = 50
Maximum number of rules to return. Default: 50. Max: 200.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List an entity's business rules or inspect one rule with parsed XAML conditions/actions. Use when debugging form/client behavior before adding JavaScript.

WHEN TO USE:
- Debug client-side form behavior.
- Inspect business rule conditions/actions.
- Audit rules before adding JavaScript.

MODE/CONVENTION:
- rule_id empty = list rules; rule_id set = detail.
```

Params đề xuất:
```text
entity_name (string, required): Table logical name.
rule_id (string=""): Business rule GUID for detail; empty lists.
status (string=""): active|draft|empty all.
max_records (int=50): Result limit, max 200.
```

# Tổng kết
Token trước optimize: 201
Token sau optimize: 128
Tiết kiệm: 73 tokens (36.3%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

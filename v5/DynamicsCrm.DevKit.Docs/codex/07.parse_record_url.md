# Tóm tắt
Tool: `parse_record_url`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ParseRecordUrlTool.cs`
Title hiện tại: Parse a Dynamics 365 URL to entity and record ID
Mô tả hiện trạng: Parse a Dynamics 365/Power Platform URL or string to extract entity logical name and record ID.
Token hiện tại: 143 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Parse a Dynamics 365/Power Platform URL or string to extract entity logical name and record ID.

Supports: main.aspx (etn/etc), Web API, Power Apps/Automate maker portal, rundialog, workflow/report/solution editor URLs, and raw GUIDs.

Returns: EntityName, RecordId, Source, EnvironmentId (maker URLs only).

TIPS:
- If EntityName is '(unknown)', ask user for entity name or a full record URL
- Auto-resolves entity type codes (etc) and entitySetNames via Dataverse
```

Params hiện tại:
```text
input: string
URL, GUID, or text to parse (Dynamics 365 record URL, Web API URL, maker portal URL, or raw GUID).
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Parse Dynamics/Power Platform URLs, text or raw GUIDs into entity logical name, record ID, source and maker environment ID when available. Resolves entity type codes/entity sets when possible.

WHEN TO USE:
- Extract entity and GUID from Dynamics/Power Platform URLs.
- Resolve etc/entityset URLs.
- Handle raw GUID/text copied from UI.

MODE/CONVENTION:
- URL/text/GUID in, parsed entity/record/source out; unknown entity requires user clarification.
```

Params đề xuất:
```text
input (string, required): Record URL, Web API URL, maker URL, text or GUID.
```

# Tổng kết
Token trước optimize: 143
Token sau optimize: 118
Tiết kiệm: 25 tokens (17.5%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `manage_record`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageRecordTool.cs`
Title hiện tại: Manage a single record (CRUD)
Mô tả hiện trạng: Perform CRUD on a single Dataverse record.
Token hiện tại: 237 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Perform CRUD on a single Dataverse record.

ACTIONS:
- action='create': Requires entity_name + fields_json. Returns new GUID.
- action='read': Requires entity_name + record_id. Optional: columns.
- action='update': Requires entity_name + record_id + fields_json.
- action='delete': Requires entity_name + record_id. WARNING: irreversible.

TIPS:
- Use get_tables for field names/types; use execute_fetchxml to find lookup GUIDs.
- Partial update: include only fields to change.
- Delete may fail on dependencies; may cascade-delete children per relationship config.
```

Params hiện tại:
```text
action: string
The CRUD action to perform: 'create', 'read', 'update', or 'delete'.

entity_name: string
Entity logical name (e.g., 'account'). Required for all actions.

record_id: string = ""
Record GUID. Required for read, update, and delete. Must be empty for create.

fields_json: string = ""
JSON object of field values for create/update. Required for create and update; ignored for read and delete.

columns: string = ""
Comma-separated column logical names for 'read' only. Empty = all columns.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Single-record Dataverse tool with exactly four actions: create, read, update, delete. Create/update use fields_json; read can limit columns; delete is irreversible and follows relationship cascade rules.

ACTIONS: create, read, update, delete.

WHEN TO USE:
- Create/read/update/delete one known record.
- For update, send only the fields to change.
- Verify a record before destructive changes.

SAFETY:
- delete is irreversible and may cascade by relationship config.
```

Params đề xuất:
```text
action (string, required): create|read|update|delete.
entity_name (string, required): Table logical name.
record_id (string=""): GUID for read/update/delete; empty for create.
fields_json (string=""): JSON field values for create/update.
columns (string=""): Read-only comma-separated columns; empty returns all.
```

# Tổng kết
Token trước optimize: 237
Token sau optimize: 167
Tiết kiệm: 70 tokens (29.5%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

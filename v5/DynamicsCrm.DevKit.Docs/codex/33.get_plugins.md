# Tóm tắt
Tool: `get_plugins`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetPluginsTool.cs`
Title hiện tại: List plugin registrations and steps
Mô tả hiện trạng: List and inspect Dataverse plugin assemblies, types, and processing steps.
Token hiện tại: 289 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
List and inspect Dataverse plugin assemblies, types, and processing steps.

THREE MODES:
- No filters: list all assemblies with type counts
- assembly_name (single match): assembly detail with types + steps + images
- entity_name: all steps on that entity across all assemblies

TIPS:
- Stages: PreValidation, PreOperation, PostOperation, MainOperation (Custom API/DataProvider)
- include_config defaults to false — set true only when secure config inspection is needed
```

Params hiện tại:
```text
assembly_name: string = ""
Filter by assembly name (contains). Empty = list all.

entity_name: string = ""
Filter steps by entity (e.g., 'account'). Shows all steps on this entity.

message_name: string = ""
Filter by SDK message (e.g., 'Create', 'Update', 'Delete').

type_name: string = ""
Filter by plugin type name (contains).

include_images: bool = true
Include pre/post images. Default: true.

include_config: bool = false
Include config values. Default: false (security).

stage: string = ""
'prevalidation', 'preoperation', 'postoperation', 'mainoperation'. Empty = all.

mode: string = ""
'sync' or 'async'. Empty = both.

active_only: bool = true
Only activated steps. Default: true.

max_records: int = 100
Max steps (1-500). Default: 100.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List plugin assemblies or inspect plugin types, steps and images. Filter across assemblies by entity/message/type/stage/mode; secure config is hidden unless requested.

WHEN TO USE:
- List plugin assemblies.
- Inspect plugin types, steps and images.
- Find steps by entity/message/stage/mode.

MODE/CONVENTION:
- No filters = list assemblies.
- assembly_name contains: 1 match details, >1 lists candidates.
- entity_name returns steps across assemblies for that table.

FUZZY/AMBIGUITY:
- assembly_name is contains match. Multiple assemblies return a candidate list instead of detail.
```

Params đề xuất:
```text
assembly_name (string=""): Assembly contains filter; single match details.
entity_name (string=""): Show steps for a table.
message_name (string=""): SDK message filter.
type_name (string=""): Plugin type contains filter.
include_images (bool=true): Include step images.
include_config (bool=false): Include config values.
stage (string=""): prevalidation|preoperation|postoperation|mainoperation.
mode (string=""): sync|async.
active_only (bool=true): Only enabled steps.
max_records (int=100): Step/result limit, max 500.
```

# Tổng kết
Token trước optimize: 289
Token sau optimize: 245
Tiết kiệm: 44 tokens (15.2%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

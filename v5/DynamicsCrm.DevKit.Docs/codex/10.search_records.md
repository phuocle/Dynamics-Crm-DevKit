# Tóm tắt
Tool: `search_records`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/SearchRecordsTool.cs`
Title hiện tại: Search records by keyword
Mô tả hiện trạng: Dataverse Relevance Search (full-text) across one or more entities.
Token hiện tại: 341 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Dataverse Relevance Search (full-text) across one or more entities. Returns records ranked by relevance with highlights.

ACTIONS:
- action='search' (default): Full-text search. Required: search_term
- action='status': Relevance Search config — enabled status, indexed entities/fields, sync status, storage size, document count

WHEN TO USE:
- Find records by keyword across multiple entities
- Quick text search without knowing the exact field to filter on
- Check if Relevance Search is enabled and which entities are indexed (action='status')

IMPORTANT: Relevance Search must be enabled. Max 100 results. For larger datasets or precise filtering, use execute_fetchxml.
```

Params hiện tại:
```text
action: string = "search"
'search' (default): full-text search, requires search_term. 'status': show configuration, indexed entities/fields, sync status, storage statistics.

search_term: string = ""
Text to search (1-100 chars). Required for action='search'. Syntax: + (AND), | (OR), - (NOT), * (wildcard), "quoted" (phrase), () (grouping).

entities: string = ""
Comma-separated entity logical names to scope the search (e.g., 'account,contact'). Empty = all searchable entities.

top: int = 50
Maximum number of results to return. Default: 50. Max: 100. Use a smaller value (e.g. 10) for quick lookups.

filter: string = ""
OData-style filter (eq, ne, gt, ge, lt, le, and, or, not). E.g., 'statecode eq 0'. Empty = no filter.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Run Dataverse Relevance Search or inspect search status/config. Best for keyword discovery across indexed tables; use execute_fetchxml for precise or large queries.

WHEN TO USE:
- Keyword search across indexed tables.
- Find records when field names are unknown.
- Check Relevance Search configuration.
```

Params đề xuất:
```text
action (string="search"): search|status.
search_term (string=""): Required for search; supports + | - * quotes and grouping.
entities (string=""): Comma-separated table scope; empty all searchable.
top (int=50): Result limit, max 100.
filter (string=""): Optional OData-style filter.
```

# Tổng kết
Token trước optimize: 341
Token sau optimize: 127
Tiết kiệm: 214 tokens (62.8%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

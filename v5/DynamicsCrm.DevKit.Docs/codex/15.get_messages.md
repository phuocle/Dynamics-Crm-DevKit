# Tóm tắt
Tool: `get_messages`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetMessagesTool.cs`
Title hiện tại: List SDK messages and custom actions
Mô tả hiện trạng: Discover SDK messages and Custom Actions for a Dataverse entity.
Token hiện tại: 252 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Discover SDK messages and Custom Actions for a Dataverse entity.

TWO MODES (controlled by message_name):
- List (message_name empty): all SDK messages + Custom Action names for entity or globally
- Detail (message_name set): parameters, supported entities, plugin steps for a specific message

SCOPING: entity_name for entity-bound; 'none'/empty for global/unbound messages (WhoAmI, etc.).

WHEN TO USE:
- Find SDK messages available for plugin registration
- Discover Custom Actions registered on an entity
- Get input/output parameters of a legacy Custom Action (workflow category=3)

TIPS:
- For modern Custom APIs use get_custom_apis instead
- This covers legacy Custom Actions (workflow-based) that get_custom_apis does NOT
```

Params hiện tại:
```text
entity_name: string = "none"
Entity logical name (lowercase). Use 'none'/empty for global/unbound messages. Use get_tables to discover names. Ignored in detail mode when message_name is set.

message_name: string = ""
Message or Custom Action name for detail mode. Empty = list mode.

include_custom_actions: bool = true
true: include Custom Actions in list results. false: SDK messages only. Ignored when message_name is set.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Discover SDK messages and legacy workflow Custom Actions. Empty message_name lists entity/global messages; set message_name for parameters, supported entities and plugin steps. Use get_custom_apis for modern Custom API.

WHEN TO USE:
- Find SDK messages for plugin registration.
- Use detail mode to view legacy workflow Custom Action parameters.
- Separate legacy Custom Actions from modern Custom APIs.

MODE/CONVENTION:
- message_name empty = list; message_name set = detail.
- entity_name scopes list mode; detail uses message_name.
```

Params đề xuất:
```text
entity_name (string="none"): Bound table logical name; none/empty means global.
message_name (string=""): Message/action name for detail; empty lists.
include_custom_actions (bool=true): Include legacy Custom Actions in list mode.
```

# Tổng kết
Token trước optimize: 252
Token sau optimize: 154
Tiết kiệm: 98 tokens (38.9%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

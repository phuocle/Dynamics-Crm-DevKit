# Tóm tắt
Tool: `whoami`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/WhoAmITool.cs`
Title hiện tại: Get current user and environment info
Mô tả hiện trạng: Get current user identity and environment info.
Token hiện tại: 100 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Get current user identity and environment info.

Returns: user (ID, name, email, roles), org (ID, URL, version), tenant/environment IDs, language, currency, fiscal/audit settings.

WHEN TO USE:
- Confirm connected user and environment
- Check security roles for permission troubleshooting
- Get user ID for FetchXML owner filters
```

Params hiện tại:
```text
include_token: bool = false
true: include OAuth access token (~400 extra tokens). Default: false. Use only for direct Web API calls.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Get connected Dataverse user, org, environment IDs/URL/version, roles, locale/currency and audit/fiscal settings. Use to confirm context or troubleshoot permissions; token output is optional.

WHEN TO USE:
- Confirm connected user/environment.
- Troubleshoot security roles.
- Get user/org IDs for filters.
```

Params đề xuất:
```text
include_token (bool=false): Include OAuth access token only for direct Web API calls.
```

# Tổng kết
Token trước optimize: 100
Token sau optimize: 81
Tiết kiệm: 19 tokens (19.0%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

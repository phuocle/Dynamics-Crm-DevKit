# Tóm tắt
Tool: `manage_role`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageRoleTool.cs`
Title hiện tại: Manage security roles
Mô tả hiện trạng: Manage security roles: list, inspect, CRUD, copy, assign/unassign users.
Token hiện tại: 448 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Manage security roles: list, inspect, CRUD, copy, assign/unassign users.

ACTIONS:
- action='list': List root roles. Optional: role_name, business_unit_id, max_records
- action='detail': Role privileges grouped by entity. Required: role_id. Optional: entity_name
- action='user': User's roles + effective privileges. Required: user_id. Optional: entity_name
- action='assign': Assign role to user. Required: role_id + user_id
- action='unassign': Remove role from user. Required: role_id + user_id
- action='create': Create custom role. Required: role_name. Optional: business_unit_id
- action='update': Rename role. Required: role_id + role_name
- action='delete': Delete custom role (irreversible). Required: role_id
- action='copy': Clone role with all privileges. Required: role_id + role_name

WHEN TO USE:
- Debug 'access denied': action='user' + entity_name
- Audit role grants: action='detail'
- Provision access: action='assign'/'unassign', or action='create'/'copy'

TIPS:
- Depth levels: User < BU < Parent:ChildBU < Org
- Only root roles listed (not inherited BU copies)
- Managed roles cannot be deleted; use action='copy' to clone then customize
```

Params hiện tại:
```text
action: string
Action: list, detail, user, assign, unassign, create, update, delete, copy.

user_id: string = ""
Email or GUID. Required for: user, assign, unassign.

role_id: string = ""
Role GUID. Required for: detail, assign, unassign, update, delete, copy.

role_name: string = ""
Filter (contains) in list. New name for create/update/copy.

business_unit_id: string = ""
BU GUID. Filter in list. Target BU for create (empty = root BU).

entity_name: string = ""
Filter privileges by entity (e.g. 'account'). For detail and user only.

max_records: int = 50
Max roles in list mode. Default: 50, max: 250.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Manage Dataverse security roles with actions list, detail, user, assign, unassign, create, update, delete, copy. Use detail/user for privilege output.

WHEN TO USE:
- Debug access denied issues.
- Audit role/entity privileges.
- Assign, copy or manage security roles.

SAFETY:
- delete affects custom roles only; managed roles cannot be deleted.
```

Params đề xuất:
```text
action (string, required): list|detail|user|assign|unassign|create|update|delete|copy.
user_id (string=""): Email or GUID for user/assign/unassign.
role_id (string=""): Role GUID for detail/assign/unassign/update/delete/copy.
role_name (string=""): List filter or new role name.
business_unit_id (string=""): List/create BU; empty root BU on create.
entity_name (string=""): Privilege filter for detail/user.
max_records (int=50): List limit, max 250.
```

# Tổng kết
Token trước optimize: 448
Token sau optimize: 194
Tiết kiệm: 254 tokens (56.7%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `manage_environment_variable`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageEnvironmentVariableTool.cs`
Title hiện tại: Manage environment variables
Mô tả hiện trạng: List, get, create, update, delete, or clear Dataverse environment variables.
Token hiện tại: 400 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
List, get, create, update, delete, or clear Dataverse environment variables.

ACTIONS:
- action='list': List all variables (name, type, default/current value). Optional: solution_name, max_records
- action='detail': Get details for one variable. Requires variable_name
- action='create': Create variable definition. Requires variable_name + display_name + type. Optional: default_value, value, description, solution_name
- action='update': Update existing variable. Requires variable_name. Optional: display_name, default_value, value, description
- action='delete': Permanently delete definition + current value. Requires variable_name. WARNING: irreversible
- action='clear': Delete current value only (reverts to default). Requires variable_name

TIPS:
- Current value overrides default value
- Type cannot be changed after creation
- Does not require publishing in most cases
```

Params hiện tại:
```text
action: string
The action to perform: 'list', 'detail', 'create', 'update', 'delete', or 'clear'.

variable_name: string = ""
Schema name with publisher prefix (e.g., 'new_ApiEndpoint'). Required for detail, create, update, delete, and clear. Leave empty for list.

solution_name: string = ""
Solution unique name. Filters results (list only) or adds definition to solution (create only). Leave empty for all.

max_records: int = 50
Maximum number of variables to return in list mode. Default is 50.

display_name: string = ""
Display name. Required on create.

type: string = ""
Type: 'string', 'number', 'boolean', 'json', 'datasource', 'secret'. Required on create, ignored on update.

default_value: string = ""
Default value for the definition.

value: string = ""
Current value override. Creates/updates the value record.

description: string = ""
Variable description.

auto_publish: bool = false
Publish after changes. Default: false.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List, detail, create, update, delete or clear Dataverse environment variables. Current value overrides default; type is create-only; delete removes definition and value.

WHEN TO USE:
- List variables or use detail for one variable.
- Create/update default/current values.
- Clear override values or delete definitions.

SAFETY:
- delete removes definition/current value; clear removes current value only.
```

Params đề xuất:
```text
action (string, required): list|detail|create|update|delete|clear.
variable_name (string=""): Schema name; required except list.
solution_name (string=""): List filter or add-to-solution on create.
max_records (int=50): List limit.
display_name (string=""): Required on create.
type (string=""): string|number|boolean|json|datasource|secret; create-only.
default_value (string=""): Definition default.
value (string=""): Current override value.
description (string=""): Definition description.
auto_publish (bool=false): Publish after changes if requested.
```

# Tổng kết
Token trước optimize: 400
Token sau optimize: 206
Tiết kiệm: 194 tokens (48.5%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `get_system_jobs`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetSystemJobsTool.cs`
Title hiện tại: List and inspect system jobs
Mô tả hiện trạng: List/inspect system jobs (asyncoperation): async failures, workflow errors, bulk ops, imports, solutions.
Token hiện tại: 332 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
List/inspect system jobs (asyncoperation): async failures, workflow errors, bulk ops, imports, solutions.

MODES:
- record_id empty: list jobs with filters (default: failed, last 24h)
- record_id provided: full detail with error message + stack trace

WHEN TO USE:
- Debug async failures: list failed jobs, then detail for full stack trace
- Monitor bulk ops: filter by operation_type ('bulk_delete', 'import', 'solution')
- Trace a request: use correlation_id to find all jobs for one operation

TIPS:
- Async plugin failures: combine with get_plugin_trace_logs for trace output
- Use entity_name to filter jobs for a specific entity
```

Params hiện tại:
```text
record_id: string = ""
GUID for detail mode. Empty = list. Use parse_record_url to extract from URL.

entity_name: string = ""
Filter by entity (e.g., 'account'). Empty = all.

status: string = "failed"
'failed' (default), 'succeeded', 'waiting', 'in_progress', 'canceled', 'all'.

operation_type: string = ""
'plugin', 'workflow', 'bulk_delete', 'import', 'goal_rollup', 'solution', 'all'. Empty = all.

name_filter: string = ""
Filter by name (contains).

correlation_id: string = ""
Exact GUID. Traces a single request across jobs.

minutes_ago: int = 0
Time range in minutes. Default: 1440 (24h). Max: 43200.

max_records: int = 50
Max results. Default: 50. Max: 500.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List or inspect asyncoperation system jobs: async plugins, workflows, imports, bulk jobs and solution operations. Default list is failed jobs in the last 24h.

WHEN TO USE:
- Debug async plugin/workflow/import/solution failures.
- Inspect full async error stack.
- Monitor recent system jobs.

MODE/CONVENTION:
- record_id empty = list jobs; record_id set = full detail.
```

Params đề xuất:
```text
record_id (string=""): Job GUID for detail; empty lists.
entity_name (string=""): Regarding table filter.
status (string="failed"): failed|succeeded|waiting|in_progress|canceled|all.
operation_type (string=""): plugin|workflow|bulk_delete|import|goal_rollup|solution|all.
name_filter (string=""): Name contains filter.
correlation_id (string=""): Exact request correlation GUID.
minutes_ago (int=0): Range; default 1440, max 43200.
max_records (int=50): List limit, max 500.
```

# Tổng kết
Token trước optimize: 332
Token sau optimize: 208
Tiết kiệm: 124 tokens (37.3%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

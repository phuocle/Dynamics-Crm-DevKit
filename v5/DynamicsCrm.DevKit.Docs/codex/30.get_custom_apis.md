# Tóm tắt
Tool: `get_custom_apis`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetCustomApisTool.cs`
Title hiện tại: List custom API definitions
Mô tả hiện trạng: Retrieve Custom API definitions from Dataverse — modern replacement for Custom Actions (binding, visibility, plugin attachment, typed params).
Token hiện tại: 205 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Retrieve Custom API definitions from Dataverse — modern replacement for Custom Actions (binding, visibility, plugin attachment, typed params).

MODES:
- api_name empty: list matching APIs (filter by entity_name, status, include_microsoft)
- api_name set: full detail — request parameters, response properties, plugin binding

TIPS:
- Managed APIs excluded by default; set include_microsoft=true to include them
- isFunction=true → GET (no side effects); isFunction=false → POST Action
```

Params hiện tại:
```text
api_name: string = ""
Unique name for full detail. Empty = list all.

entity_name: string = ""
Filter by bound entity logical name (e.g., 'account'). Empty = all.

include_microsoft: bool = false
Include managed APIs (Microsoft and third-party). Default: false.

status: string = "active"
'active' (default), 'inactive', or 'all'.

max_records: int = 100
Max results in list mode (1-500). Default: 100.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List or inspect modern Dataverse Custom APIs. Detail includes binding, visibility, plugin, request parameters and response properties; managed APIs are excluded by default.

WHEN TO USE:
- List modern Custom APIs.
- Inspect binding, plugin and typed parameters.
- Decide GET function vs POST action usage.

MODE/CONVENTION:
- api_name empty = list; api_name set = detail by unique name.
```

Params đề xuất:
```text
api_name (string=""): Unique name for detail; empty lists.
entity_name (string=""): Bound table filter.
include_microsoft (bool=false): Include managed/Microsoft APIs.
status (string="active"): active|inactive|all.
max_records (int=100): List limit, max 500.
```

# Tổng kết
Token trước optimize: 205
Token sau optimize: 145
Tiết kiệm: 60 tokens (29.3%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

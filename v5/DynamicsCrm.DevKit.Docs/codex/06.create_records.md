# Tóm tắt
Tool: `create_records`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/CreateRecordsTool.cs`
Title hiện tại: Create multiple records in parallel
Mô tả hiện trạng: Create multiple Dataverse records in parallel — optimized for data migration.
Token hiện tại: 248 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Create multiple Dataverse records in parallel — optimized for data migration. Partial failures reported per-item; successful records still created.

DEFAULT (shown in output when not provided):
  max_parallelism: from server x-ms-dop-hint (typically 4–8 for cloud). Hard limit: 52.

TIPS:
- records_json: inline JSON array, .json file path (from generate_demo_data), or .csv (headers=Display Name; lookups by Name: 1 match=GUID, 0 or 2+=skipped)
- Polymorphic lookups: use "fieldname@targetentity" syntax
- max_parallelism=1–2 for on-prem/throttled envs; max 5000 records per call
```

Params hiện tại:
```text
entity_name: string
Entity logical name (e.g., 'account'). Required.

records_json: string
JSON array of field objects (inline), OR file path (.json from generate_demo_data), OR CSV file path (.csv with Display Name headers). Max 5000 records.

max_parallelism: int = 0
Max concurrent requests. 0 (default) = use server hint (x-ms-dop-hint). Clamped to 1–52.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Bulk-create Dataverse records from inline JSON, JSON file or CSV. Runs in parallel using server DOP hint unless max_parallelism is set; reports per-row failures without rolling back successful rows.

WHEN TO USE:
- Import generated/migration data.
- Create many records with per-row error reporting.
- Load JSON/CSV after lookup/picklist normalization.

FUZZY/AMBIGUITY:
- CSV lookup-by-name: exactly 1 match becomes GUID; 0 or 2+ is skipped with warning.
```

Params đề xuất:
```text
entity_name (string, required): Target table logical name.
records_json (string, required): JSON array, .json path, or .csv path; max 5000.
max_parallelism (int=0): 0 uses server hint; otherwise 1-52 concurrent creates.
```

# Tổng kết
Token trước optimize: 248
Token sau optimize: 165
Tiết kiệm: 83 tokens (33.5%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

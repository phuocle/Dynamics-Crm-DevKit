# Tóm tắt
Tool: `get_business_process_flows`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetBusinessProcessFlowsTool.cs`
Title hiện tại: List business process flows and stages
Mô tả hiện trạng: List and inspect Business Process Flows (BPFs) and their stages.
Token hiện tại: 250 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
List and inspect Business Process Flows (BPFs) and their stages. Each BPF auto-creates its own Dataverse entity.

TWO MODES:
- bpf_id empty (and bpf_name matches 0 or 2+): list all matching BPFs
- bpf_id provided (or bpf_name matches exactly 1): detail with all stages

TIPS:
- BPFs can span multiple entities (e.g., Lead → Opportunity); each stage has its own primaryEntity
- uniqueName is also the logical name of the BPF's auto-created entity
```

Params hiện tại:
```text
bpf_id: string = ""
BPF GUID for full detail. Empty = list mode.

bpf_name: string = ""
Filter by name (contains). If exactly 1 match, returns detail.

entity_name: string = ""
Filter by primary entity (e.g., 'lead', 'opportunity').

status: string = "active"
'active' (default), 'draft', or 'all'.

include_stages: bool = false
Include stages in list mode (default: false). Always included in detail.

max_records: int = 50
Max BPFs (1-250). Default: 50.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: List or inspect Business Process Flows and stages. bpf_name with one match or bpf_id returns detail; uniqueName is also the auto-created BPF table logical name.

WHEN TO USE:
- List BPFs for an entity.
- Inspect stages/entities for one BPF.
- Find the BPF uniqueName/logical table name.

MODE/CONVENTION:
- bpf_id set = detail.
- bpf_name contains: exactly 1 auto-details; 0 or 2+ returns list.
```

Params đề xuất:
```text
bpf_id (string=""): BPF GUID for detail; empty lists.
bpf_name (string=""): Name contains filter; one match auto-details.
entity_name (string=""): Primary entity filter.
status (string="active"): active|draft|all.
include_stages (bool=false): Include stages in list; detail always includes.
max_records (int=50): List limit, max 250.
```

# Tổng kết
Token trước optimize: 250
Token sau optimize: 191
Tiết kiệm: 59 tokens (23.6%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt
Tool: `execute_fetchxml`
Source: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ExecuteFetchXmlTool.cs`
Title hiện tại: Run a FetchXML query
Mô tả hiện trạng: Execute a FetchXML query against Dataverse.
Token hiện tại: 165 tokens (`o200k_base`, chỉ tính tool description + params block bên dưới).

# Before optimize
Tool description hiện tại:
```text
Execute a FetchXML query against Dataverse. Returns markdown table. Max 5000 records. Supports auto-paging.

RULES:
- Use lowercase logical names; call get_tables if unsure
- DO NOT use top/count/page in <fetch> — use max_records instead
- Read schema://fetchxml for structure, operators, joins, and aggregation syntax
```

Params hiện tại:
```text
fetchxml: string
FetchXML query starting with <fetch>. Must use lowercase logical names.

max_records: int = 5000
Max records to return (1–5000, default 5000). Use a smaller value (e.g. 10–100) for samples.

get_all: bool = false
true: auto-page until max_records or no more rows. false: first page only (default). Use true for complete datasets.
```

# After optimize
Tool description đề xuất:
```text
SUMMARY: Run FetchXML and return a markdown table. Use lowercase logical names, max_records/get_all for paging, and avoid top/count/page in the XML.

WHEN TO USE:
- Run precise Dataverse queries.
- Sample or export records with joins/aggregates.
- Validate logical names from get_tables.
```

Params đề xuất:
```text
fetchxml (string, required): Query beginning with <fetch>.
max_records (int=5000): Return limit, 1-5000.
get_all (bool=false): Auto-page until max_records or no more rows.
```

# Tổng kết
Token trước optimize: 165
Token sau optimize: 108
Tiết kiệm: 57 tokens (34.5%).
Ghi chú: token estimate chỉ tính nội dung tool description + params, không tính Markdown wrapper của file này.

# Tóm tắt

**Tool:** `parse_record_url`
**Mô tả:** Parse URL/string của Dynamics 365/Power Platform để lấy entity logical name + record ID.
**Token hiện tại (description + parameters):** ~140 tokens
- Description: ~115 tokens (~450 chars)
- 1 parameter: ~25 tokens (~110 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Parse a Dynamics 365/Power Platform URL or string to extract entity logical name and record ID.

Supports: main.aspx (etn/etc), Web API, Power Apps/Automate maker portal, rundialog, workflow/report/solution editor URLs, and raw GUIDs.

Returns: EntityName, RecordId, Source, EnvironmentId (maker URLs only).

TIPS:
- If EntityName is '(unknown)', ask user for entity name or a full record URL
- Auto-resolves entity type codes (etc) and entitySetNames via Dataverse
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[input] (string, required)
URL, GUID, or text to parse (Dynamics 365 record URL, Web API URL, maker portal URL, or raw GUID).
```

---

# After optimize

## Phân tích

Source: [ParseRecordUrlTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/ParseRecordUrlTool.cs)

- "Supports:" liệt kê dài — gom thành 1 dòng compact.
- "Returns:" gom vào câu chính.
- TIPS ngắn — giữ.

## Block 1 — Tool description (optimized)

```text
Extract entity logical name + record ID from a D365/Power Platform URL/GUID. Supports main.aspx (etn/etc), Web API, maker portal, rundialog, workflow/report/solution editor URLs, raw GUIDs. Returns EntityName, RecordId, Source, EnvironmentId (maker only). Auto-resolves etc → entitySetName via Dataverse. If EntityName is '(unknown)', ask user.
```

## Block 2 — Parameter descriptions (optimized)

```text
[input] (string, required)
URL, GUID, or text (record URL, Web API URL, maker portal URL, raw GUID).
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~115 tokens | ~80 tokens | -30% |
| Parameter (1) | ~25 tokens | ~20 tokens | -20% |
| **Tổng** | **~140 tokens** | **~100 tokens** | **~29%** |

**Kết luận:** Tiết kiệm khoảng **29%** (~40 tokens). Tool này vốn đã ngắn nên dư địa optimize ít.

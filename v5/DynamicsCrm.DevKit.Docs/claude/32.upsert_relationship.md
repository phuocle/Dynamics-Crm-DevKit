# Tóm tắt

**Tool:** `upsert_relationship`
**Source:** `DynamicsCrm.DevKit.Cli/Mcp/Tools/UpsertRelationshipTool.cs`
**Title hiện tại:** Create, update, or delete Dataverse relationships
**Mô tả:** CRUD relationship Dataverse (1:N, N:N) + manage polymorphic lookup targets.
**Token hiện tại (description + parameters):** ~768 tokens (`o200k_base`)
- Description: ~290 tokens (~1170 chars)
- 22 parameters: ~478 tokens (~1170 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Create, update, or delete Dataverse relationships (1:N, N:N) and manage polymorphic lookup targets.

ACTIONS:
- action='create_1n': Create 1:N + lookup column. Required: referenced_entity + referencing_entity + solution_name
- action='create_nn': Create N:N + intersect entity. Required: entity1 + entity2 + solution_name
- action='update': Update cascade/menu/hierarchy on existing relationship. Required: relationship_name
- action='delete': Delete relationship by schema name. Required: relationship_name
- action='add_target': Add target to polymorphic lookup. Required: entity_name + attribute_name + referenced_entity + solution_name
- action='remove_target': Remove target from polymorphic lookup. Required: entity_name + attribute_name + referenced_entity

TIPS:
- Use get_tables to find relationship_name; use build_form_xml to add lookup to a form
- WARNING: remove_target permanently deletes data in that lookup target
- is_hierarchical=true: marks this self-referential 1:N as the OOB hierarchy relationship (only one per entity; only valid when referenced_entity == referencing_entity)
- Cascade presets: Parental (all Cascade), Referential (NoCascade + RemoveLink on delete), ReferentialRestrictDelete (NoCascade + Restrict on delete)
- Cascade types: Cascade, NoCascade, RemoveLink, Restrict, Active, UserOwned
- Read docs://schema_tools_guide for cascade preset values and cascade type options
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[action] (string, default: "")
The action to perform: 'create_1n', 'create_nn', 'update', 'delete', 'add_target', 'remove_target'.

[relationship_name] (string, default: "")
Relationship schema name. Required for update/delete. Auto-generated for create.

[referenced_entity] (string, default: "")
Parent entity logical name (1:N create, add/remove target).

[referencing_entity] (string, default: "")
Child entity logical name (1:N create).

[entity1] (string, default: "")
First entity for N:N relationship.

[entity2] (string, default: "")
Second entity for N:N relationship.

[intersect_entity_name] (string, default: "")
Intersect entity name for N:N. Auto-generated if empty.

[entity_name] (string, default: "")
Entity with the polymorphic lookup (add/remove target).

[attribute_name] (string, default: "")
Polymorphic lookup logical name (add/remove target).

[cascade_preset] (string, default: "")
Cascade preset: 'Parental', 'Referential' (default), 'ReferentialRestrictDelete'. Read docs://schema_tools_guide for values.

[cascade_assign] (string, default: "")
Override cascade Assign behavior.

[cascade_delete] (string, default: "")
Override cascade Delete behavior.

[cascade_merge] (string, default: "")
Override cascade Merge behavior.

[cascade_reparent] (string, default: "")
Override cascade Reparent behavior.

[cascade_share] (string, default: "")
Override cascade Share behavior.

[cascade_unshare] (string, default: "")
Override cascade Unshare behavior.

[menu_behavior] (string, default: "")
Associated menu behavior: 'UseCollectionName' (default), 'UseLabel', 'DoNotDisplay'.

[menu_group] (string, default: "")
Associated menu group: 'Details' (default), 'Sales', 'Service', 'Marketing'.

[menu_order] (int, default: 10000)
Associated menu order. Default: 10000.

[lookup_display_name] (string, default: "")
Display name for the lookup column (1:N create).

[solution_name] (string, default: "")
Solution unique name. Required for create_1n, create_nn, and add_target to resolve the publisher prefix.

[is_hierarchical] (bool, default: false)
Mark this self-referential 1:N as the OOB hierarchy relationship (create_1n and update only). Only one hierarchy relationship per entity is allowed. Only valid when referenced_entity == referencing_entity.

[auto_publish] (bool, default: true)
Publish after changes. Default: true.
```

---

# After optimize

## Phân tích

Source: [UpsertRelationshipTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/UpsertRelationshipTool.cs)

- 6 cascade params có chung pattern "Override cascade X behavior" — gom 1 dòng định nghĩa.
- TIPS giữ cascade presets/types vì nghiệp vụ critical.
- ACTIONS giữ vì required khác nhau.
- is_hierarchical giải thích dài — nén.

## Block 1 — Tool description (optimized)

```text
Dataverse relationships (1:N, N:N) + polymorphic lookup targets. Required:
- create_1n: referenced_entity + referencing_entity + solution_name (auto-creates lookup column)
- create_nn: entity1 + entity2 + solution_name (auto-creates intersect entity)
- update: relationship_name (cascade/menu/hierarchy)
- delete: relationship_name
- add_target: entity_name + attribute_name + referenced_entity + solution_name
- remove_target: entity_name + attribute_name + referenced_entity (deletes data permanently)

is_hierarchical: only for self-referential 1:N (referenced=referencing), max 1/entity.
Cascade presets: Parental (all Cascade) | Referential (NoCascade + RemoveLink on delete) | ReferentialRestrictDelete (NoCascade + Restrict on delete).
Cascade types: Cascade | NoCascade | RemoveLink | Restrict | Active | UserOwned.
See docs://schema_tools_guide.

WHEN TO USE:
- Create 1:N (with auto lookup column) or N:N (with intersect entity)
- Update cascade behavior, associated menu, or hierarchy flag
- Add / remove a target on a polymorphic lookup (e.g. customer → account, contact, custom)
- Inspect relationship_name first via get_tables before update/delete

FUZZY/AMBIGUITY:
- solution_name resolves exact uniquename → exact display name → display-name contains. Multiple matches require exact unique name.
```

## Block 2 — Parameter descriptions (optimized)

```text
[action] (string, default: "")
create_1n / create_nn / update / delete / add_target / remove_target.

[relationship_name] (string, default: "")
Schema name. Required: update/delete. Auto-generated on create.

[referenced_entity] (string, default: "")
Parent (1:N create, add/remove target).

[referencing_entity] (string, default: "")
Child (1:N create).

[entity1], [entity2] (string, default: "")
N:N entities.

[intersect_entity_name] (string, default: "")
N:N intersect. Auto if empty.

[entity_name] (string, default: "")
Entity with polymorphic lookup (add/remove target).

[attribute_name] (string, default: "")
Polymorphic lookup logical name.

[cascade_preset] (string, default: "")
Parental / Referential / ReferentialRestrictDelete.

[cascade_assign|delete|merge|reparent|share|unshare] (string, default: "")
Override cascade behavior. Values from cascade types.

[menu_behavior] (string, default: "")
UseCollectionName / UseLabel / DoNotDisplay.

[menu_group] (string, default: "")
Details / Sales / Service / Marketing.

[menu_order] (int, default: 10000)

[lookup_display_name] (string, default: "")
1:N create only.

[solution_name] (string, default: "")
Required: create_1n, create_nn, add_target.

[is_hierarchical] (bool, default: false)
Self-referential 1:N (create_1n/update). 1 per entity, referenced=referencing.

[auto_publish] (bool, default: true)
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~290 tokens | ~245 tokens | -16% |
| Parameters (22) | ~478 tokens | ~202 tokens | -58% |
| **Tổng** | **~768 tokens** | **~447 tokens** | **~42%** |

**Kết luận:** Tiết kiệm khoảng **42%** (~321 tokens, `o200k_base`). Restore WHEN TO USE riêng. Gom 6 cascade_* params thành 1 entry và entity1/entity2 thành 1 dòng.

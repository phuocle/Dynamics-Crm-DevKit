# Tóm tắt

**Tool:** `manage_webresource`
**Mô tả:** List/inspect/CRUD web resources Dataverse.
**Token hiện tại (description + parameters):** ~445 tokens
- Description: ~210 tokens (~835 chars)
- 11 parameters: ~235 tokens (~942 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List, inspect, create, update, or delete web resources in Dataverse.

FIVE ACTIONS:
- action='list': List web resources filtered by name, type, solution. Optional: name, type_filter, solution_name, max_records
- action='detail': Full metadata for one web resource. Requires web_resource_id
- action='create': Create a new web resource. Requires name + content (base64) + type. Optional: display_name, description, solution_name
- action='update': Update content and/or metadata. Requires web_resource_id. Optional: content, display_name, description
- action='delete': Permanently delete a web resource. Requires web_resource_id. WARNING: cannot be undone

TIPS:
- Call this tool first to find library_name needed for build_form_xml add_event/add_library
- Names follow convention: {prefix}_/path/filename.ext (e.g., v4_/entities/Account.form.js)
- Related: build_form_xml (add events/libraries), manage_form (inspect form structure)
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[action] (string, required)
The action to perform: 'list', 'detail', 'create', 'update', or 'delete'.

[web_resource_id] (string, default: "")
GUID of the web resource. Required for detail, update, and delete. Empty for list and create.

[name] (string, default: "")
Web resource unique name (e.g., 'v4_/entities/Account.form.js'). Required for create. For list: contains filter on name.

[display_name] (string, default: "")
Display name. Optional for create/update.

[description] (string, default: "")
Description text. Optional for create/update.

[content] (string, default: "")
Base64 encoded file content. Required for create. Optional for update.

[type] (string, default: "")
Web resource type for create: 'js', 'html', 'css', 'xml', 'png', 'jpg', 'gif', 'svg', 'ico', 'resx', 'xsl', 'xap'. Required for create, ignored for other actions. For list: use type_filter instead.

[solution_name] (string, default: "")
Filter by solution unique name (list mode). Solution to add to (create mode).

[type_filter] (string, default: "")
Filter by type in list mode: 'js', 'html', 'css', 'xml', 'png', 'jpg', 'gif', 'svg', 'ico', 'resx', 'xsl', 'xap'. Empty = all.

[max_records] (int, default: 50)
Max results for list mode (1-500). Default: 50.

[auto_publish] (bool, default: true)
Publish after changes. Default: true.
```

---

# After optimize

## Phân tích

Source: [ManageWebResourceTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageWebResourceTool.cs)

- 12 types liệt kê 2 lần (param `type` và `type_filter`) — gom 1 lần ra description.
- ACTIONS giữ vì required khác.
- TIPS giữ thông tin tool combo.

## Block 1 — Tool description (optimized)

```text
Web resources Dataverse — list/detail/create/update/delete. Required:
- list: optional name, type_filter, solution_name, max_records
- detail: web_resource_id
- create: name + content (base64) + type. Optional: display_name, description, solution_name
- update: web_resource_id. Optional: content, display_name, description
- delete: web_resource_id (irreversible)

Types: js, html, css, xml, png, jpg, gif, svg, ico, resx, xsl, xap.
Naming convention: {prefix}_/path/filename.ext (e.g. 'v4_/entities/Account.form.js'). Use this tool first to find library_name for build_form_xml add_event/add_library.
```

## Block 2 — Parameter descriptions (optimized)

```text
[action] (string, required)
list / detail / create / update / delete.

[web_resource_id] (string, default: "")
GUID. Required: detail/update/delete.

[name] (string, default: "")
Unique name (e.g. 'v4_/entities/Account.form.js'). Required: create. list: contains filter.

[display_name] (string, default: "")

[description] (string, default: "")

[content] (string, default: "")
Base64. Required: create.

[type] (string, default: "")
Required: create. See description for values. Ignored on other actions.

[solution_name] (string, default: "")
list: filter. create: add to solution.

[type_filter] (string, default: "")
list only. See type values.

[max_records] (int, default: 50)
1–500.

[auto_publish] (bool, default: true)
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~210 tokens | ~155 tokens | -26% |
| Parameters (11) | ~235 tokens | ~120 tokens | -49% |
| **Tổng** | **~445 tokens** | **~275 tokens** | **~38%** |

**Kết luận:** Tiết kiệm khoảng **38%** (~170 tokens). Gom 12 file types về description, params chỉ trỏ đến.

# Tóm tắt

**Tool:** `publish_customizations`
**Mô tả:** Publish thay đổi metadata Dataverse (entities, attributes, forms, views, option sets, relationships).
**Token hiện tại (description + parameters):** ~177 tokens
- Description: ~100 tokens (~390 chars)
- 3 parameters: ~77 tokens (~258 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Publish Dataverse customizations to make metadata changes visible. Required after changes to entities, attributes, forms, views, option sets, or relationships.

WHEN TO USE:
- After upsert_*/execute_webapi metadata changes (if auto_publish was false)
- When user reports changes not showing up

TIPS:
- Publish specific entities for faster execution than PublishAll
- PublishAll can take 30+ seconds on large environments
- Idempotent — safe to republish already-published changes
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[entities] (string, default: "")
Comma-separated entity logical names (e.g., 'account,contact'). Empty = publish ALL customizations.

[include_global_optionset] (bool, default: false)
Also publish global option sets. Only applies when entities is specified. Default: false.

[include_sitemap] (bool, default: false)
Also publish the sitemap. Only applies when entities is specified. Default: false.
```

---

# After optimize

## Phân tích

Source: [PublishCustomizationsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/PublishCustomizationsTool.cs)

- WHEN TO USE 2 bullets gom 1 dòng.
- TIPS 3 bullets — bullet "PublishAll 30+ seconds" có thể bỏ; "Idempotent" gom 1 từ.
- "Default: false" thừa trong các bool param (default đã ghi).

## Block 1 — Tool description (optimized)

```text
Publish Dataverse metadata changes (entities, attributes, forms, views, option sets, relationships). Specific-entity publish is faster than PublishAll. Idempotent.

WHEN TO USE:
- After upsert_* / execute_webapi metadata changes when auto_publish=false
- When user reports changes not showing up
- Batch many changes then publish once at the end
```

## Block 2 — Parameter descriptions (optimized)

```text
[entities] (string, default: "")
Comma-separated logical names (e.g. 'account,contact'). Empty = PublishAll.

[include_global_optionset] (bool, default: false)
Also publish global option sets. Requires entities set.

[include_sitemap] (bool, default: false)
Also publish sitemap. Requires entities set.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~100 tokens | ~80 tokens | -20% |
| Parameters (3) | ~77 tokens | ~40 tokens | -48% |
| **Tổng** | **~177 tokens** | **~120 tokens** | **~32%** |

**Kết luận:** Tiết kiệm khoảng **32%** (~57 tokens). Restore WHEN TO USE block riêng.

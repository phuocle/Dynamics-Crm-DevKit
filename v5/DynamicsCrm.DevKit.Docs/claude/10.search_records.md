# Tóm tắt

**Tool:** `search_records`
**Mô tả:** Dataverse Relevance Search (full-text) qua nhiều entity. 2 actions: `search` / `status`.
**Token hiện tại (description + parameters):** ~280 tokens
- Description: ~155 tokens (~620 chars)
- 5 parameters: ~125 tokens (~495 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Dataverse Relevance Search (full-text) across one or more entities. Returns records ranked by relevance with highlights.

ACTIONS:
- action='search' (default): Full-text search. Required: search_term
- action='status': Relevance Search config — enabled status, indexed entities/fields, sync status, storage size, document count

WHEN TO USE:
- Find records by keyword across multiple entities
- Quick text search without knowing the exact field to filter on
- Check if Relevance Search is enabled and which entities are indexed (action='status')

IMPORTANT: Relevance Search must be enabled. Max 100 results. For larger datasets or precise filtering, use execute_fetchxml.
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[action] (string, default: "search")
'search' (default): full-text search, requires search_term. 'status': show configuration, indexed entities/fields, sync status, storage statistics.

[search_term] (string, default: "")
Text to search (1-100 chars). Required for action='search'. Syntax: + (AND), | (OR), - (NOT), * (wildcard), "quoted" (phrase), () (grouping).

[entities] (string, default: "")
Comma-separated entity logical names to scope the search (e.g., 'account,contact'). Empty = all searchable entities.

[top] (int, default: 50)
Maximum number of results to return. Default: 50. Max: 100. Use a smaller value (e.g. 10) for quick lookups.

[filter] (string, default: "")
OData-style filter (eq, ne, gt, ge, lt, le, and, or, not). E.g., 'statecode eq 0'. Empty = no filter.
```

---

# After optimize

## Phân tích

Source: [SearchRecordsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/SearchRecordsTool.cs)

- "WHEN TO USE" 3 bullets gom 1 dòng.
- "ACTIONS" lặp tên action với param description.
- Param `action` description nói lặp với block ACTIONS — gom 1 nơi.

## Block 1 — Tool description (optimized)

```text
Dataverse Relevance Search (full-text, ranked, with highlights) across entities. action='search' (full-text, needs search_term) or 'status' (config: enabled state, indexed entities/fields, sync status, storage). Use for keyword search when you don't know the field. Requires Relevance Search enabled; max 100 results — use execute_fetchxml for larger / precise filtering.
```

## Block 2 — Parameter descriptions (optimized)

```text
[action] (string, default: "search")
'search' or 'status'.

[search_term] (string, default: "")
Required for search. 1–100 chars. Syntax: + (AND), | (OR), - (NOT), * (wildcard), "phrase", () (group).

[entities] (string, default: "")
Comma-separated logical names (e.g. 'account,contact'). Empty = all searchable.

[top] (int, default: 50)
1–100.

[filter] (string, default: "")
OData filter (eq, ne, gt, ge, lt, le, and, or, not). E.g. 'statecode eq 0'.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~155 tokens | ~85 tokens | -45% |
| Parameters (5) | ~125 tokens | ~75 tokens | -40% |
| **Tổng** | **~280 tokens** | **~160 tokens** | **~43%** |

**Kết luận:** Tiết kiệm khoảng **43%** (~120 tokens).

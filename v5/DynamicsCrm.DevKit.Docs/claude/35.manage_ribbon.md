# Tóm tắt

**Tool:** `manage_ribbon`
**Mô tả:** Quản lý RibbonDiffXml (classic/legacy ribbon) cho entity Dataverse — DEFAULT cho mọi yêu cầu button thông thường.
**Token hiện tại (description + parameters):** ~370 tokens
- Description: ~250 tokens (~995 chars)
- 5 parameters: ~120 tokens (~480 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Retrieve and modify RibbonDiffXml for Dataverse entities via solution import.

TOOL SELECTION — READ BEFORE CHOOSING:
CLASSIC/LEGACY ribbon (RibbonDiffXml). DEFAULT FALLBACK for all button requests.
Use when: 'ribbon', 'legacy', 'classic', 'button', 'nút', 'custom button', 'action button', 'UI button', 'JavaScript button', 'sub_grid button', 'homepage grid button', or any generic button request.
Use manage_command ONLY for: 'modern', 'Power Fx', 'appaction', 'new UI', 'Model-Driven App command bar', 'command designer'.
When in doubt → always use manage_ribbon, never manage_command.

ACTIONS: list, buttons, detail, update, undo
- list: entities with ribbon customizations in solution 'devkit-ribbon'
- buttons: all ribbon buttons (OOB+custom) across form/main_grid/sub_grid. Required: entity_name
- detail: show current RibbonDiffXml. Required: entity_name
- update: apply ribbonxml from build_ribbon_xml. Required: entity_name + ribbonxml. Backup→Import→PublishAll
- undo: restore from backup file. Required: entity_name + ribbonxml (backup path)

WORKFLOW: build_ribbon_xml → manage_ribbon(action='update') [auto-publishes all by default]
Auto-backup before update; backup failure blocks update.
NOTE: Ribbon requires PublishAll (not entity-scoped publish). auto_publish=true (default) runs PublishAll synchronously. Set false when batching, then call publish_customizations once.
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[action] (string, required)
'list', 'buttons', 'detail', 'update', or 'undo'.

[entity_name] (string, default: "")
Entity logical name (e.g., 'account'). Required for detail/update/undo.

[ribbonxml] (string, default: "")
For 'update': RibbonDiffXml file path from build_ribbon_xml. For 'undo': backup file path.

[auto_publish] (bool, default: true)
Publish after changes (default: true). Set false when batching.

[backup] (bool, default: true)
Backup current ribbon before overwriting (default: true). Backup failure blocks update.
```

---

# After optimize

## Phân tích

Source: [ManageRibbonTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageRibbonTool.cs)

- TOOL SELECTION block dài nhưng critical — nén nhưng giữ trigger words.
- WORKFLOW + NOTE + Auto-backup gom 1 đoạn ngắn.
- Param descriptions ngắn rồi.

## Block 1 — Tool description (optimized)

```text
Classic/legacy RibbonDiffXml for Dataverse entities (via solution import).

TOOL SELECTION: DEFAULT FALLBACK for all button requests. Use when: 'ribbon', 'legacy', 'classic', 'button', 'nút', 'custom button', 'action button', 'UI button', 'JavaScript button', 'sub_grid button', 'homepage grid button', generic button. Use manage_command ONLY for 'modern', 'Power Fx', 'appaction', 'new UI', 'command designer'. When in doubt → manage_ribbon.

Actions:
- list: entities with ribbon customizations in solution 'devkit-ribbon'
- buttons: all OOB+custom buttons across form/main_grid/sub_grid (entity_name)
- detail: current RibbonDiffXml (entity_name)
- update: apply ribbonxml from build_ribbon_xml. Flow: backup → import → PublishAll
- undo: restore (entity_name + backup path)

WORKFLOW: build_ribbon_xml → manage_ribbon(update). Auto-backup; failure blocks update. Ribbon needs PublishAll (entity-scoped publish doesn't work). auto_publish=true runs PublishAll sync; set false to batch then call publish_customizations.
```

## Block 2 — Parameter descriptions (optimized)

```text
[action] (string, required)
list / buttons / detail / update / undo.

[entity_name] (string, default: "")
Logical name. Required: detail/update/undo/buttons.

[ribbonxml] (string, default: "")
update: file path from build_ribbon_xml. undo: backup path.

[auto_publish] (bool, default: true)
Runs PublishAll. false to batch.

[backup] (bool, default: true)
Backup before overwrite.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~250 tokens | ~190 tokens | -24% |
| Parameters (5) | ~120 tokens | ~55 tokens | -54% |
| **Tổng** | **~370 tokens** | **~245 tokens** | **~34%** |

**Kết luận:** Tiết kiệm khoảng **34%** (~125 tokens). Giữ trọn TOOL SELECTION trigger keywords (critical cho routing đúng).

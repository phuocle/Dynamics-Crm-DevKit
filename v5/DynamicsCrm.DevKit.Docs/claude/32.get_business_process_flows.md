# Tóm tắt

**Tool:** `get_business_process_flows`
**Mô tả:** List/inspect Business Process Flows (BPFs) và stages.
**Token hiện tại (description + parameters):** ~190 tokens
- Description: ~110 tokens (~440 chars)
- 6 parameters: ~80 tokens (~325 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List and inspect Business Process Flows (BPFs) and their stages. Each BPF auto-creates its own Dataverse entity.

TWO MODES:
- bpf_id empty (and bpf_name matches 0 or 2+): list all matching BPFs
- bpf_id provided (or bpf_name matches exactly 1): detail with all stages

TIPS:
- BPFs can span multiple entities (e.g., Lead → Opportunity); each stage has its own primaryEntity
- uniqueName is also the logical name of the BPF's auto-created entity
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[bpf_id] (string, default: "")
BPF GUID for full detail. Empty = list mode.

[bpf_name] (string, default: "")
Filter by name (contains). If exactly 1 match, returns detail.

[entity_name] (string, default: "")
Filter by primary entity (e.g., 'lead', 'opportunity').

[status] (string, default: "active")
'active' (default), 'draft', or 'all'.

[include_stages] (bool, default: false)
Include stages in list mode (default: false). Always included in detail.

[max_records] (int, default: 50)
Max BPFs (1-250). Default: 50.
```

---

# After optimize

## Phân tích

Source: [GetBusinessProcessFlowsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/GetBusinessProcessFlowsTool.cs)

- TWO MODES + TIPS gom 1 đoạn.
- TIPS giữ multi-entity & uniqueName behavior.

## Block 1 — Tool description (optimized)

```text
Business Process Flows (BPFs) + stages. Each BPF auto-creates its own Dataverse entity (uniqueName = logical name). bpf_id empty (or bpf_name 0/multi match) = list. Set (or bpf_name 1 match) = detail with stages. BPFs can span multiple entities (Lead → Opportunity); each stage has its own primaryEntity.
```

## Block 2 — Parameter descriptions (optimized)

```text
[bpf_id] (string, default: "")
GUID → detail. Empty = list.

[bpf_name] (string, default: "")
Name contains. 1 match → auto-detail.

[entity_name] (string, default: "")
Primary entity filter (e.g. 'lead').

[status] (string, default: "active")
'active' / 'draft' / 'all'.

[include_stages] (bool, default: false)
List only. Detail always includes.

[max_records] (int, default: 50)
1–250.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~110 tokens | ~80 tokens | -27% |
| Parameters (6) | ~80 tokens | ~50 tokens | -38% |
| **Tổng** | **~190 tokens** | **~130 tokens** | **~32%** |

**Kết luận:** Tiết kiệm khoảng **32%** (~60 tokens).

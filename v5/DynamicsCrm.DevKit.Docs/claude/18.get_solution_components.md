# Tóm tắt

**Tool:** `get_solution_components`
**Mô tả:** List tất cả component bên trong 1 solution Dataverse (tìm theo uniqueName/displayName fuzzy).
**Token hiện tại (description + parameters):** ~301 tokens (o200k_base)
- Description: ~175 tokens (~698 chars)
- 3 parameters: ~126 tokens (~415 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List all components inside a Dataverse solution by uniqueName or displayName (fuzzy/contains matching).

Returns: solution info + component summary (count per type) + full detail table (componentType, objectId, name).

BEHAVIORS:
- 1 match → components; multiple → disambiguation list; 0 → error
- Full Entity (rootComponentBehavior=0): listed as-is; use get_tables for sub-components
- include_active_layers=true: adds ActiveLayer (Yes/No) via msdyn_componentlayer
- active_layers_only=true: implies include_active_layers=true; shows only components with active layers

WHEN TO USE:
- Audit solution contents before packaging/deploying
- Find objectIds of components (plugins, web resources, workflows)
- Identify unmanaged customizations before deploying managed solutions
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[solution_name] (string, required)
Solution uniqueName (e.g. 'DevKit_Core') or displayName (e.g. 'DevKit Core'). Fuzzy (contains) matching on both fields. If multiple match, the tool returns the list — re-call with exact uniqueName.

[include_active_layers] (bool, default: false)
When true, queries msdyn_componentlayer and adds an ActiveLayer (Yes/No) column. Active layer = unmanaged customization exists. Batched in groups of 200. Default: false.

[active_layers_only] (bool, default: false)
When true, implies include_active_layers=true and filters to only components with an active layer. Default: false.
```

---

# After optimize

## Phân tích

Source: [GetSolutionComponentsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/GetSolutionComponentsTool.cs)

- "Returns:" có thể gom vào description chính.
- "BEHAVIORS" 4 bullets — hai bullets active_layers nói lặp với param descriptions, có thể bỏ.
- "WHEN TO USE" 3 bullets gom 1 dòng.
- Param `include_active_layers` có "Batched in groups of 200" — chi tiết kỹ thuật, có thể bỏ.

## Block 1 — Tool description (optimized)

```text
List components in a Dataverse solution. Returns: solution info + count-per-type summary + detail (componentType, objectId, name). Full Entity components (rootComponentBehavior=0) listed as-is — use get_tables for sub-components.

WHEN TO USE:
- Audit solution contents before packaging/deploying
- Find objectIds for plugins, web resources, workflows
- Identify unmanaged customizations before importing managed solutions

FUZZY/AMBIGUITY:
- solution_name matches uniqueName + displayName (contains); 0/multi → tool returns disambiguation list and stops; AI must ask user (re-call with exact uniqueName). 1 → auto.
```

## Block 2 — Parameter descriptions (optimized)

```text
[solution_name] (string, required)
Solution unique/display name; multiple matches return choices.

[include_active_layers] (bool, default: false)
Add ActiveLayer (Yes/No) via msdyn_componentlayer. Active = unmanaged customization exists.

[active_layers_only] (bool, default: false)
Show only active-layer components; implies include_active_layers.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~175 tokens | ~135 tokens | -23% |
| Parameters (3) | ~126 tokens | ~46 tokens | -63% |
| **Tổng** | **~301 tokens** | **~181 tokens** | **~40%** |

**Kết luận:** Tiết kiệm khoảng **40%** (~120 tokens). Restore WHEN TO USE + tách FUZZY/AMBIGUITY block rõ ràng.

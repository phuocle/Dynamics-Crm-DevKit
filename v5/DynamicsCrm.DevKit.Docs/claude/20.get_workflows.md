# Tóm tắt

**Tool:** `get_workflows`
**Mô tả:** List/inspect classic workflows (background + real-time) cho entity Dataverse.
**Token hiện tại (description + parameters):** ~310 tokens
- Description: ~190 tokens (~755 chars)
- 7 parameters: ~120 tokens (~485 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List and inspect classic workflows (background and real-time) for a Dataverse entity.

TWO MODES:
- workflow_id EMPTY: list workflows matching filters
- workflow_id PROVIDED: full detail for a single workflow

SCOPE: Classic workflows only. Use get_business_rules, get_custom_apis, get_business_process_flows, get_flows for others.

KEY OUTPUT FIELDS:
- triggeronupdateattributelist: update-trigger fields ('statecode'=status change, 'ownerid'=assignment)
- mode: Background (async, always Post) or Realtime (sync, Pre/Post stages)
- scope: User/BU/Parent:ChildBU/Org. runas: Owner/Caller

WHEN TO USE:
- Check if a field triggers any workflow: trigger_field + entity_name
- Find synchronous workflows: mode='realtime'

TIPS:
- Realtime Pre-operation can cancel/rollback the operation
- If name_filter matches exactly 1 workflow, auto-switches to detail mode
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[workflow_id] (string, default: "")
Workflow GUID for detail mode. Empty = list mode.

[entity_name] (string, default: "")
Entity logical name (e.g., 'account'). Empty = all entities.

[mode] (string, default: "")
'background' (async) or 'realtime' (sync). Empty = both.

[active_only] (bool, default: true)
Only activated workflows. Default: true.

[trigger_field] (string, default: "")
Filter by update trigger field (contains match, e.g., 'revenue', 'statecode').

[name_filter] (string, default: "")
Filter by name (contains match). If exactly 1 match, returns detail.

[max_records] (int, default: 50)
Max records. Default: 50, max: 250.
```

---

# After optimize

## Phân tích

Source: [GetWorkflowsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/GetWorkflowsTool.cs)

- KEY OUTPUT FIELDS quan trọng (AI cần biết để hỏi đúng) → giữ.
- WHEN TO USE 2 bullets gom 1 dòng.
- SCOPE 1 dòng giữ vì routing tới 4 tool khác.

## Block 1 — Tool description (optimized)

```text
Classic workflows (background async + realtime sync) for a Dataverse entity. workflow_id empty = list. Set = detail. Classic only — use get_business_rules / get_custom_apis / get_business_process_flows / get_flows for others.

Key fields:
- triggeronupdateattributelist: trigger fields ('statecode'=status, 'ownerid'=assignment)
- mode: Background (async, Post) | Realtime (sync, Pre/Post; Pre can cancel/rollback)
- scope: User/BU/Parent:ChildBU/Org. runas: Owner/Caller

Fuzzy on name_filter: 0/multi → tool returns disambiguation list and stops; AI must ask user. 1 → auto-detail.

WHEN TO USE:
- Check if a field triggers any workflow (trigger_field + entity_name)
- Find synchronous workflows (mode='realtime'; Pre-op can cancel/rollback)
- Inspect workflow steps before refactoring/disabling
```

> Convention: see `00.conventions.md` — tool kết thúc `s`, áp pattern list/detail by id.

## Block 2 — Parameter descriptions (optimized)

```text
[workflow_id] (string, default: "")
GUID → detail. Empty = list.

[entity_name] (string, default: "")
Logical name (e.g. 'account'). Empty = all.

[mode] (string, default: "")
'background' / 'realtime'. Empty = both.

[active_only] (bool, default: true)

[trigger_field] (string, default: "")
Update trigger field contains (e.g. 'revenue', 'statecode').

[name_filter] (string, default: "")
Name contains. 1 match → auto-detail.

[max_records] (int, default: 50)
Max 250.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~190 tokens | ~165 tokens | -13% |
| Parameters (7) | ~120 tokens | ~70 tokens | -42% |
| **Tổng** | **~310 tokens** | **~235 tokens** | **~24%** |

**Kết luận:** Tiết kiệm khoảng **24%** (~75 tokens). Restore WHEN TO USE riêng + fuzzy disambiguation rule cho `name_filter`. Description giữ KEY OUTPUT FIELDS.

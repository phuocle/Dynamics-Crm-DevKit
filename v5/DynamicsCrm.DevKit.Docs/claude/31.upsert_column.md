# Tóm tắt

**Tool:** `upsert_column`
**Source:** `DynamicsCrm.DevKit.Cli/Mcp/Tools/UpsertColumnTool.cs`
**Title hiện tại:** Create or update a table column
**Mô tả:** Tạo/update column (attribute) Dataverse, auto-detect create vs update theo 16 attribute types.
**Token hiện tại (description + parameters):** ~966 tokens (`o200k_base`)
- Description: ~300 tokens (~1190 chars)
- 25 parameters: ~666 tokens (~1800 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Create or update a Dataverse column (attribute). Auto-detects create vs update. Types: string, memo, integer, bigint, decimal, money, float, boolean, datetime, lookup, customer, picklist, multipicklist, image, file.

CREATE (attribute does not exist): attribute_type + display_name required.
- lookup: auto-creates 1:N relationship. Required: lookup_target
- customer: polymorphic lookup (account+contact), no lookup_target needed
- picklist/multipicklist: provide options JSON or global_optionset_name

UPDATE (attribute already exists): attribute_type ignored (immutable). Omit params to keep values.
- picklist/multipicklist: use add_options, update_options, delete_options

CREATE PREFIX CONFIRMATION FLOW:
1. First call (confirmed_prefix empty): tool resolves prefix from solution_name or attribute_name and returns [PrefixConfirmationRequired] — NOT an error.
2. Ask user to confirm the prefix shown.
3. Re-call with confirmed_prefix set to the agreed prefix to proceed.

TIPS:
- attribute_name must include publisher prefix (e.g., 'new_priority'); or provide solution_name to auto-resolve prefix.
- After create, use build_form_xml to add the column to a form
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[entity_name] (string, required)
Entity logical name (e.g., 'account').

[attribute_name] (string, required)
Logical name with publisher prefix (e.g., 'new_priority'). Or just the name (e.g., 'priority') — prefix is auto-resolved from solution_name's publisher.

[attribute_type] (string, required)
Column type: 'string', 'memo', 'integer', 'bigint', 'decimal', 'money', 'float' (or 'double'), 'boolean', 'datetime', 'lookup', 'customer', 'picklist', 'multipicklist', 'image', 'file'.

[display_name] (string, required)
Display name (e.g., 'Priority Level'). Required for create.

[solution_name] (string, default: "")
Solution unique name. Used to auto-resolve publisher prefix when attribute_name has no prefix.

[confirmed_prefix] (string, default: "")
Confirmed publisher prefix for create. Leave empty on first call — tool returns [PrefixConfirmationRequired] with preview. Re-call with this set to the agreed prefix to proceed.

[description] (string, default: "")
Column description.

[required_level] (string, default: "")
'None', 'Recommended', or 'Required'. Omit to keep current value on update.

[max_length] (int, default: 0)
For string (1-4000, default 100), memo (1-1048576, default 2000), file (KB, default 32768).

[min_value] (double?, default: null)
For numeric types: minimum value.

[max_value] (double?, default: null)
For numeric types: maximum value.

[precision] (int, default: -1)
For decimal/money/float: decimal places (0-10, default 2; money max is 4). Omit to keep current value on update.

[format] (string, default: "")
For string: 'Text','Email','Url','Phone','TextArea','TickerSymbol','RichText'. For datetime: 'DateOnly','DateAndTime'. For integer: 'None','Duration','TimeZone','Language','Locale'.

[behavior] (string, default: "")
For datetime: 'UserLocal' (default),'DateOnly','TimeZoneIndependent'. DateOnly behavior forces DateOnly format.

[precision_source] (int, default: -1)
For money: 0 (Attribute, default), 1 (Organization), 2 (Currency).

[options] (string, default: "")
For picklist (create): JSON array [{"label":"Low","value":100000000}].

[global_optionset_name] (string, default: "")
For picklist (create): existing global option set name.

[lookup_target] (string, default: "")
For lookup (create): target entity. Comma-separated for polymorphic.

[lookup_relationship_name] (string, default: "")
For lookup: relationship schema name. Auto-generated if omitted.

[true_label] (string, default: "")
For boolean: true label. Default: 'Yes'. Omit to keep current value on update.

[false_label] (string, default: "")
For boolean: false label. Default: 'No'. Omit to keep current value on update.

[add_options] (string, default: "")
For picklist (update): JSON array of options to add.

[update_options] (string, default: "")
For picklist (update): JSON array of options to rename.

[delete_options] (string, default: "")
For picklist (update): JSON array of integer values to remove.

[is_audit_enabled] (bool?, default: null)
Enable/disable auditing (update only).

[is_valid_for_advanced_find] (bool?, default: null)
Show/hide in Advanced Find (update only).

[auto_publish] (bool, default: true)
Publish after operation. Default: true.
```

---

# After optimize

## Phân tích

Source: [UpsertColumnTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/UpsertColumnTool.cs)

- 16 types liệt kê 2 lần (description + param) — gom param `attribute_type` 1 dòng, description ngắn.
- "Omit to keep current value on update" lặp 5+ lần → shorthand `[update: omit=keep]`.
- 3 picklist update params (add/update/delete_options) gom 1 dòng.
- true_label/false_label gom 1 dòng.

## Block 1 — Tool description (optimized)

```text
Dataverse column (attribute) — auto-detect create vs update. Types: string, memo, integer, bigint, decimal, money, float, boolean, datetime, lookup, customer, picklist, multipicklist, image, file.

CREATE (no attribute): need attribute_type + display_name.
- lookup: needs lookup_target (auto-creates 1:N)
- customer: polymorphic (account+contact), no lookup_target
- picklist/multipicklist: options JSON or global_optionset_name

UPDATE (exists): attribute_type ignored (immutable). picklist: use add/update/delete_options. Omit params to keep current.

PREFIX CONFIRMATION on CREATE: first call (confirmed_prefix empty) returns [PrefixConfirmationRequired] preview — confirm, re-call with confirmed_prefix.
attribute_name needs publisher prefix or solution_name to auto-resolve.

WHEN TO USE:
- Create new attribute on an existing table (need attribute_type + display_name)
- Update mutable metadata, format, required_level, picklist options
- Add/rename/remove options on an existing picklist via add_options/update_options/delete_options

FUZZY/AMBIGUITY:
- solution_name uses the same solution resolver as upsert_table: exact uniquename → exact display name → display-name contains. Multiple matches require exact unique name; confirmed_prefix still gates create.
```

## Block 2 — Parameter descriptions (optimized)

```text
[entity_name] (string, required)
Logical name (e.g. 'account').

[attribute_name] (string, required)
With prefix ('new_priority') or just name + solution_name to auto-resolve.

[attribute_type] (string, required)
string/memo/integer/bigint/decimal/money/float/boolean/datetime/lookup/customer/picklist/multipicklist/image/file. (immutable on update)

[display_name] (string, required)
Required: create.

[solution_name] (string, default: "")
Auto-resolve prefix when attribute_name has no prefix.

[confirmed_prefix] (string, default: "")
Confirmed prefix after [PrefixConfirmationRequired] preview.

[description] (string, default: "")

[required_level] (string, default: "")
None/Recommended/Required. [update: omit=keep]

[max_length] (int, default: 0)
string 1-4000 (def 100); memo 1-1048576 (def 2000); file KB (def 32768).

[min_value], [max_value] (double?, default: null)
Numeric types.

[precision] (int, default: -1)
decimal/money/float 0-10 (def 2; money max 4). [update: omit=keep]

[format] (string, default: "")
string: Text/Email/Url/Phone/TextArea/TickerSymbol/RichText. datetime: DateOnly/DateAndTime. integer: None/Duration/TimeZone/Language/Locale.

[behavior] (string, default: "")
datetime: UserLocal (def)/DateOnly/TimeZoneIndependent. DateOnly forces DateOnly format.

[precision_source] (int, default: -1)
money: 0=Attribute (def), 1=Organization, 2=Currency.

[options] (string, default: "")
picklist create: JSON array [{"label":"Low","value":100000000}].

[global_optionset_name] (string, default: "")
picklist create: existing global option set.

[lookup_target] (string, default: "")
lookup create: target entity. Comma-separated = polymorphic.

[lookup_relationship_name] (string, default: "")
lookup: schema name. Auto if empty.

[true_label], [false_label] (string, default: "")
boolean labels (def 'Yes'/'No'). [update: omit=keep]

[add_options], [update_options], [delete_options] (string, default: "")
picklist update: JSON arrays — add, rename, or remove (integer values).

[is_audit_enabled] (bool?, default: null)
[update only]

[is_valid_for_advanced_find] (bool?, default: null)
[update only]

[auto_publish] (bool, default: true)
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~300 tokens | ~220 tokens | -27% |
| Parameters (25) | ~666 tokens | ~251 tokens | -62% |
| **Tổng** | **~966 tokens** | **~471 tokens** | **~51%** |

**Kết luận:** Tiết kiệm khoảng **51%** (~495 tokens, `o200k_base`). Restore WHEN TO USE riêng. Gom min/max_value, true/false_label, add/update/delete_options thành 1 entry mỗi nhóm; shorthand `[update: omit=keep]` / `[update only]`.

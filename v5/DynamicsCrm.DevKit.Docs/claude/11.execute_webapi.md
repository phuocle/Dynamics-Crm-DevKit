# Tóm tắt

**Tool:** `execute_webapi`
**Mô tả:** Gửi raw HTTP request tới Dataverse Web API. Whitelist GET; POST/PATCH/PUT/DELETE chỉ cho data + custom action; metadata bị block runtime.
**Token hiện tại (description + parameters):** ~395 tokens
- Description: ~225 tokens (~895 chars)
- 6 parameters: ~170 tokens (~675 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Execute raw Dataverse Web API requests.

ALLOWED:
- GET any endpoint (always safe)
- POST/PATCH/PUT/DELETE on data records and custom actions (e.g. accounts, WhoAmI)

BLOCKED (runtime-enforced — use specialized tools):
- Schema/metadata → upsert_table, upsert_column, upsert_relationship
- Choice/OptionSet → manage_choice
- Forms/Views/SiteMaps → manage_form, manage_view, manage_sitemap
- Environment variables → manage_environment_variable
- Web resources → manage_webresource
- Security roles → manage_role
- Publish → publish_customizations
- Solutions/Plugins/Workflows/Apps → Power Apps UI or DevKit CLI

url is a relative path; SDK handles base URL. PUT/PATCH/DELETE are destructive — confirm with user first.
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[method] (string, required)
HTTP method: GET, POST, PUT, PATCH, or DELETE.

[url] (string, required)
Relative URL path (SDK handles base URL). E.g., 'accounts', 'contacts(guid)', '$metadata'.

[body] (string, default: "")
JSON body for POST/PUT/PATCH. Not needed for GET/DELETE.

[headers] (string, default: "")
Extra headers as JSON. Standard headers handled by SDK.

[include_headers] (bool, default: false)
Include response headers in output. Default: false.

[max_response_lines] (int, default: 200)
Maximum response body lines to return. Default: 200. Use smaller values (e.g. 50) for large responses like $metadata.
```

---

# After optimize

## Phân tích

Source: [ExecuteWebApiTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/ExecuteWebApiTool.cs)

- BLOCKED list 8 dòng quan trọng (AI cần biết để route đúng tool) — giữ nhưng nén bằng dấu `;`.
- "ALLOWED" có thể gom 1 dòng.
- Param descriptions ngắn gọn rồi.

## Block 1 — Tool description (optimized)

```text
Raw Dataverse Web API call. Allowed: GET (any), POST/PATCH/PUT/DELETE on data records + custom actions. Blocked at runtime (use specialized tool): schema/metadata→upsert_table/upsert_column/upsert_relationship; choice→manage_choice; form/view/sitemap→manage_form/manage_view/manage_sitemap; env vars→manage_environment_variable; webresource→manage_webresource; roles→manage_role; publish→publish_customizations; solutions/plugins/workflows/apps→DevKit CLI or Power Apps UI. url is relative; SDK adds base URL. PUT/PATCH/DELETE destructive — confirm.
```

## Block 2 — Parameter descriptions (optimized)

```text
[method] (string, required)
GET, POST, PUT, PATCH, DELETE.

[url] (string, required)
Relative path, e.g. 'accounts', 'contacts(guid)', '$metadata'.

[body] (string, default: "")
JSON body for POST/PUT/PATCH.

[headers] (string, default: "")
Extra headers as JSON.

[include_headers] (bool, default: false)
Include response headers.

[max_response_lines] (int, default: 200)
Truncate response. Use 50 for large outputs ($metadata).
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~225 tokens | ~135 tokens | -40% |
| Parameters (6) | ~170 tokens | ~70 tokens | -59% |
| **Tổng** | **~395 tokens** | **~205 tokens** | **~48%** |

**Kết luận:** Tiết kiệm khoảng **48%** (~190 tokens). Gom 8 BLOCKED bullets thành 1 dòng phân cách `;` nhưng vẫn giữ đầy đủ routing info.

# Tóm tắt

**Tool:** `upsert_table`
**Mô tả:** Tạo hoặc update 1 table Dataverse, auto-detect bằng entity lookup. Có flow xác nhận publisher prefix.
**Token hiện tại (description + parameters):** ~520 tokens
- Description: ~225 tokens (~895 chars)
- 15 parameters: ~295 tokens (~1180 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Create or update a Dataverse table (auto-detects create vs update by entity lookup).

CREATE (entity absent): display_name + display_collection_name + solution_name required. Auto-creates primary name attribute. Next: upsert_column → build_form_xml + manage_form.
UPDATE (entity exists): only entity_name required. Updatable: display_name, display_collection_name, description, is_audit_enabled, is_quick_create_enabled. All other props are immutable after creation.

CREATE PREFIX CONFIRMATION FLOW:
1. First call (confirmed_prefix empty): tool resolves prefix and returns [PrefixConfirmationRequired] with preview of SchemaName/LogicalName — NOT an error.
2. Ask user to confirm the prefix shown.
3. Re-call with confirmed_prefix set to the agreed prefix to proceed with creation.

TIPS:
- entity_name must include publisher prefix (e.g., 'new_project'); or provide solution_name to auto-resolve prefix.
- is_activity=true: forces User ownership, enables notes, sets Subject as primary attr.
- Use get_tables to inspect existing table metadata before updating.
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[entity_name] (string, required)
Logical name with publisher prefix (e.g., 'new_project'). Or just the name (e.g., 'project') — prefix is auto-resolved from solution_name's publisher.

[display_name] (string, default: "")
Singular display name (e.g., 'Project'). Required for create.

[display_collection_name] (string, default: "")
Plural display name (e.g., 'Projects'). Required for create.

[solution_name] (string, default: "")
Solution unique name. Required for create. Optional for update.

[confirmed_prefix] (string, default: "")
Confirmed publisher prefix for create. Leave empty on first call — tool returns [PrefixConfirmationRequired] with preview. Re-call with this set to the agreed prefix to proceed.

[description] (string, default: "")
Entity description.

[primary_attribute_name] (string, default: "")
Primary name attribute logical name. Auto-derived if omitted. Create only.

[primary_attribute_display_name] (string, default: "Name")
Display name for primary attribute. Default: 'Name'. Create only.

[ownership_type] (string, default: "User")
'User' (default) or 'Organization'. Create only — immutable.

[table_type] (string, default: "Standard")
'Standard' (default) or 'Elastic' (Azure Cosmos DB; no chart support). Create only — immutable.

[is_activity] (bool, default: false)
Activity entity flag. When true: forces User ownership, enables notes, sets Subject primary attr. Create only — immutable.

[has_notes] (bool, default: false)
Enable notes. Default: false. Create only — cannot be changed after creation.

[is_quick_create_enabled] (bool?, default: null)
Enable quick create form. Default: false (create, update). Omit to keep current value (update).

[is_audit_enabled] (bool?, default: null)
Enable/disable auditing. Default: true (create). Omit to keep current value (update).

[primary_attribute_max_length] (int, default: 850)
Max length for the primary name attribute (1-850, default 850). Create only.

[auto_publish] (bool, default: true)
Publish after operation. Default: true.
```

---

# After optimize

## Phân tích

Source: [UpsertTableTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/UpsertTableTool.cs)

- "CREATE PREFIX CONFIRMATION FLOW" 3 bước — gom 1 đoạn ngắn.
- TIPS bullet 1 trùng với param entity_name — bỏ.
- "Create only — immutable" lặp 5 lần — gom shorthand "[create-only]".
- Default values lặp giữa attribute và description.

## Block 1 — Tool description (optimized)

```text
Create or update a Dataverse table (auto-detect by entity lookup).
- CREATE (no entity): need display_name + display_collection_name + solution_name. Auto-creates primary name. Next: upsert_column → build_form_xml + manage_form.
- UPDATE (entity exists): need entity_name only. Mutable: display_name, display_collection_name, description, is_audit_enabled, is_quick_create_enabled. Others immutable.

PREFIX CONFIRMATION on CREATE: first call (confirmed_prefix empty) returns [PrefixConfirmationRequired] preview — confirm with user, re-call with confirmed_prefix.

is_activity=true forces User ownership + notes + Subject primary attr.
```

## Block 2 — Parameter descriptions (optimized)

```text
[entity_name] (string, required)
Logical name with prefix ('new_project') OR just name with solution_name to auto-resolve.

[display_name] (string, default: "")
Singular (e.g. 'Project'). Required: create.

[display_collection_name] (string, default: "")
Plural (e.g. 'Projects'). Required: create.

[solution_name] (string, default: "")
Required: create.

[confirmed_prefix] (string, default: "")
Confirmed prefix for create (after [PrefixConfirmationRequired] preview).

[description] (string, default: "")

[primary_attribute_name] (string, default: "")
Auto-derived if omitted. [create-only]

[primary_attribute_display_name] (string, default: "Name")
[create-only]

[ownership_type] (string, default: "User")
'User' or 'Organization'. [create-only, immutable]

[table_type] (string, default: "Standard")
'Standard' or 'Elastic' (Cosmos DB, no charts). [create-only, immutable]

[is_activity] (bool, default: false)
Activity entity. [create-only, immutable]

[has_notes] (bool, default: false)
Enable notes. [create-only]

[is_quick_create_enabled] (bool?, default: null)
null = keep current (update).

[is_audit_enabled] (bool?, default: null)
null = keep current (update). Default true on create.

[primary_attribute_max_length] (int, default: 850)
1–850. [create-only]

[auto_publish] (bool, default: true)
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~225 tokens | ~140 tokens | -38% |
| Parameters (15) | ~295 tokens | ~175 tokens | -41% |
| **Tổng** | **~520 tokens** | **~315 tokens** | **~39%** |

**Kết luận:** Tiết kiệm khoảng **39%** (~205 tokens). Dùng shorthand `[create-only]` / `[create-only, immutable]` cho 5 params lặp.

# Tóm tắt

**Tool:** `get_audit_history`
**Mô tả:** Lấy audit history (who changed what, when, old/new) cho records Dataverse — 2 chế độ detail và browse.
**Token hiện tại (description + parameters):** ~265 tokens
- Description: ~125 tokens (~498 chars)
- 9 parameters: ~140 tokens (~552 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Retrieve audit history for Dataverse records — who changed what, when, old/new values.

MODES:
- record_id set: field-level old/new for one record (entity_name required)
- record_id empty: summary list across records/entities (entity_name optional)

WHEN TO USE: debug unexpected changes, compliance auditing, track user/integration activity.

TIPS: Audit must be enabled at org AND entity level. Use from_date/to_date for ranges (overrides minutes_ago).
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[entity_name] (string, default: "")
Entity logical name. Required with record_id. Optional in browse mode.

[record_id] (string, default: "")
Record GUID for detail mode. Empty = browse mode.

[minutes_ago] (int, default: 1440)
Entries from last N minutes. Default: 1440 (24h). Max: 43200. Ignored with from_date.

[user_filter] (string, default: "")
Filter by user name (contains) or email (auto-resolved).

[operation] (string, default: "")
Filter by operation: Create, Update, Delete, Activate, Deactivate, Assign, Merge, SetState.

[attribute_name] (string, default: "")
Detail mode only: filter to one field's changes.

[max_records] (int, default: 50)
Max entries. Default: 50, max: 500.

[from_date] (string, default: "")
ISO 8601 start date (e.g., '2026-03-01'). Overrides minutes_ago.

[to_date] (string, default: "")
ISO 8601 end date. Used with from_date. Default: now.
```

---

# After optimize

## Phân tích

Source: [GetAuditHistoryTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/GetAuditHistoryTool.cs)

- WHEN TO USE + TIPS gom 1 dòng.
- "Default: …" lặp với default attribute.
- Param descriptions ngắn rồi.

## Block 1 — Tool description (optimized)

```text
Audit history for Dataverse records (who/what/when/old/new). record_id set = detail (field-level, entity_name required). Empty = browse list (entity_name optional). Audit must be enabled at org AND entity. from_date/to_date overrides minutes_ago.

WHEN TO USE:
- Debug unexpected field changes
- Compliance / regulatory auditing
- Track user or integration activity

FUZZY/AMBIGUITY:
- user_filter by email/name may resolve multiple users; tool returns candidates and requires exact display name.
```

## Block 2 — Parameter descriptions (optimized)

```text
[entity_name] (string, default: "")
Logical name. Required with record_id.

[record_id] (string, default: "")
GUID → detail. Empty = browse.

[minutes_ago] (int, default: 1440)
Last N min. Max 43200. Ignored if from_date set.

[user_filter] (string, default: "")
User name (contains) or email (auto-resolved).

[operation] (string, default: "")
Create, Update, Delete, Activate, Deactivate, Assign, Merge, SetState.

[attribute_name] (string, default: "")
Detail only. Filter one field.

[max_records] (int, default: 50)
Max 500.

[from_date] (string, default: "")
ISO 8601 (e.g. '2026-03-01'). Overrides minutes_ago.

[to_date] (string, default: "")
ISO 8601. With from_date. Default = now.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~125 tokens | ~105 tokens | -16% |
| Parameters (9) | ~140 tokens | ~85 tokens | -39% |
| **Tổng** | **~265 tokens** | **~190 tokens** | **~28%** |

**Kết luận:** Tiết kiệm khoảng **28%** (~75 tokens). Restore WHEN TO USE block riêng. Thêm FUZZY/AMBIGUITY cho user_filter.

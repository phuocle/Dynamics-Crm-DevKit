# Tóm tắt

**Tool:** `build_form_xml`
**Mô tả:** Build modified FormXML cho form Dataverse, lưu temp file để `manage_form(action='update')` apply.
**Token hiện tại (description + parameters):** ~325 tokens
- Description: ~190 tokens (~755 chars)
- 3 parameters: ~135 tokens (~540 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
Build modified FormXML for an existing Dataverse form. READ-ONLY — saves to temp file; use manage_form(action='update') to apply.

5 ACTION GROUPS (use action + manage_action): manage_tab, manage_section, manage_fields, manage_library, manage_event

Valid combinations:
- manage_tab: add | update | move | remove
- manage_section: add | update | move | remove
- manage_fields: add | update | remove | add_header | update_header | remove_header
- manage_library: add | remove
- manage_event: add | remove

Each operation object requires both 'action' and 'manage_action'.

Auto-resolves classid GUIDs, validates field names against metadata.
Section columns: 1 (default), 2, 3. Tab columns: 1 (100%), 2 (50%/50%), 3 (33%/34%/33%).

TIPS:
- Fields: "createdon" or {"field":"createdon","label":"Date","disabled":true}
- Position: "first", "last" (default), "before:<name>", "after:<name>"
- Tabs/Sections: visible, show_label, hide_on_phone. Fields also: disabled
- Use manage_action='update' to modify existing tabs/sections/fields
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[entity_name] (string, required)
Entity logical name (e.g., 'account'). Used to resolve field metadata.

[form_id] (string, required)
GUID of the form to modify. Use manage_form with action='list' to find valid form IDs.

[operations] (string, required)
JSON array of operations. Each requires 'action' + 'manage_action'.
Actions: manage_tab, manage_section, manage_fields, manage_library, manage_event.
manage_action values depend on action (see tool description).
Common fields: tab, section, fields[], label, name, position, visible, show_label, hide_on_phone, disabled, tab_column, section_columns, library_name, event_name, function_name, target.
Read docs://instructions_for_formxml for full format and examples.
```

---

# After optimize

## Phân tích

Source: [BuildFormXMLTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/BuildFormXMLTool.cs)

- "5 ACTION GROUPS" + "Valid combinations" gom thành matrix.
- TIPS giữ syntax inline ngắn gọn, fields/position quan trọng.
- Param `operations` lặp lại "Actions: manage_tab..." đã có ở description.

## Block 1 — Tool description (optimized)

```text
Build modified FormXML for an existing Dataverse form. Saves to temp file → apply via manage_form(update). Operation = {action, manage_action}:
- manage_tab: add/update/move/remove
- manage_section: add/update/move/remove
- manage_fields: add/update/remove/add_header/update_header/remove_header
- manage_library: add/remove
- manage_event: add/remove

Auto-resolves classid GUIDs, validates field names. Section columns 1-3. Tab columns: 1=100%, 2=50/50, 3=33/34/33. Position: 'first'/'last'/'before:name'/'after:name'. Fields: "name" or {field, label, disabled}. Common props on tabs/sections: visible, show_label, hide_on_phone. See docs://instructions_for_formxml.

WHEN TO USE:
- Modify form layout (tabs, sections, fields, header) before applying via manage_form(update)
- Attach JS libraries / events (manage_library, manage_event) on a form
- Always use this — never hand-write FormXML
```

## Block 2 — Parameter descriptions (optimized)

```text
[entity_name] (string, required)
Entity logical name (e.g. 'account').

[form_id] (string, required)
Form GUID. Use manage_form(list) to find.

[operations] (string, required)
JSON array of ops. Each: {action, manage_action, ...}. Common keys: tab, section, fields[], label, name, position, visible, show_label, hide_on_phone, disabled, tab_column, section_columns, library_name, event_name, function_name, target. See docs://instructions_for_formxml.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~190 tokens | ~190 tokens | 0% |
| Parameters (3) | ~135 tokens | ~95 tokens | -30% |
| **Tổng** | **~325 tokens** | **~285 tokens** | **~12%** |

**Kết luận:** Tiết kiệm khoảng **12%** (~40 tokens). Restore WHEN TO USE riêng. Description vốn dày data (5 action groups × 4-7 manage_actions) — phải giữ.

# Tóm tắt

**Tool:** `get_flows`
**Mô tả:** List/inspect Power Automate cloud flows + run history (3 modes).
**Token hiện tại (description + parameters):** ~278 tokens (`o200k_base`)
- Description: ~110 tokens (~440 chars)
- 8 parameters: ~168 tokens (~672 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List and inspect Power Automate cloud flows and their run history.

THREE MODES:
- No flow_id: list all flows matching filters
- flow_id + action='list': single flow detail + last 5 runs
- flow_id + action='runs': extended run history with filtering

TIPS:
- Use get_workflows for classic (category=0) workflows
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[flow_id] (string, default: "")
Flow GUID. Empty = list mode. With action='list': detail + runs. With action='runs': run history.

[action] (string, default: "list")
'list' (default) or 'runs'. 'runs' requires flow_id.

[name_filter] (string, default: "")
Filter by name (contains). List mode only.

[owner_filter] (string, default: "")
Filter by owner name (contains). List mode only.

[status] (string, default: "active")
'active' (default), 'draft', 'suspended', or 'all'.

[status_filter] (string, default: "")
For runs: filter by status ('succeeded','failed','running','cancelled','waiting','paused','skipped','suspended'). Empty = all.

[minutes_ago] (int, default: 1440)
For runs: last N minutes. Default: 1440 (24h). Max: 43200.

[max_records] (int, default: 50)
Max results (1-250). Default: 50.
```

---

# After optimize

## Phân tích

Source: [GetFlowsTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/GetFlowsTool.cs)

- THREE MODES rõ — giữ.
- Param `flow_id` lặp ý "with action..." — gom.
- Default values lặp với param defaults.

## Block 1 — Tool description (optimized)

```text
Power Automate cloud flows + run history. Modes: no flow_id = list (filtered); flow_id + action='list' = detail + last 5 runs; flow_id + action='runs' = extended run history. For classic (category=0) workflows use get_workflows.

WHEN TO USE:
- List flows by name/owner/status
- Inspect a specific flow + recent runs
- Drill into run history with status_filter (debug failures)

Fuzzy on name_filter / owner_filter: 0/multi → tool returns disambiguation list and stops; AI must ask user. 1 → auto.
```

## Block 2 — Parameter descriptions (optimized)

```text
[flow_id] (string, default: "")
GUID. Empty = list. Set: action determines detail vs runs.

[action] (string, default: "list")
'list' or 'runs'. 'runs' needs flow_id.

[name_filter] (string, default: "")
list only. Name contains.

[owner_filter] (string, default: "")
list only. Owner contains.

[status] (string, default: "active")
'active' / 'draft' / 'suspended' / 'all'.

[status_filter] (string, default: "")
runs only: succeeded/failed/running/cancelled/waiting/paused/skipped/suspended.

[minutes_ago] (int, default: 1440)
runs only. Max 43200.

[max_records] (int, default: 50)
1–250.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~110 tokens | ~115 tokens | +5% |
| Parameters (8) | ~168 tokens | ~85 tokens | -49% |
| **Tổng** | **~278 tokens** | **~200 tokens** | **~28%** |

**Kết luận:** Tiết kiệm khoảng **28%** (~78 tokens). Restore WHEN TO USE + fuzzy disambiguation; description tăng nhẹ vì thêm rule routing.

# Tóm tắt

**Tool:** `manage_sitemap`
**Mô tả:** List/create/update/restore SiteMap XML của Model-Driven App với auto-backup, XSD validate, publish.
**Token hiện tại (description + parameters):** ~310 tokens
- Description: ~190 tokens (~764 chars)
- 7 parameters: ~120 tokens (~485 chars)

---

# Before optimize

## Block 1 — Tool description (hiện tại)

```text
List, create, update, or restore a Model-Driven App's SiteMap XML with auto-backup, XSD validation, and publish.

ACTIONS:
- 'list': List all Model-Driven Apps with SiteMap info. Optional: app_name filter
- 'detail': Show current SiteMap XML. Required: app
- 'update' (default): Modify SiteMap XML. Required: app + sitemapxml
- 'create': Create SiteMap for app with no existing SiteMap. Required: app + sitemapxml
- 'undo': Restore from backup. Required: app + sitemapxml (= backup file path)

SAFETY: Auto-backup before changes, XSD validates XML, backup failure blocks update.

TIPS:
- app accepts display name OR GUID (fuzzy match)
- Use build_sitemap_xml to build XML, pass result to sitemapxml for update/create
- Read schema://sitemapxml for SiteMap XML structure and rules
```

## Block 2 — Parameter descriptions (hiện tại)

```text
[action] (string, default: "update")
'list', 'detail', 'update' (default), 'create', or 'undo'.

[app] (string, default: "")
App display name or GUID (fuzzy match). Required for detail/update/create/undo; for list use app_name.

[sitemapxml] (string, default: "")
For 'update'/'create': SiteMap XML string or file path from build_sitemap_xml. For 'undo': backup file path. Ignored for list/detail.

[app_name] (string, default: "")
Filter apps by name (contains match). Only used with action='list'.

[validate] (bool, default: true)
Validate against XSD before writing (default: true). Blocks if invalid.

[backup] (bool, default: true)
Backup current SiteMap before overwriting (default: true). Backup failure blocks update.

[auto_publish] (bool, default: true)
Publish after changes (default: true). Set false when batching.
```

---

# After optimize

## Phân tích

Source: [ManageSiteMapTool.cs](../../DynamicsCrm.DevKit.Cli/Mcp/Tools/ManageSiteMapTool.cs)

- ACTIONS giữ vì required khác.
- TIPS bullet 3 (schema reference) gom vào description.
- Param descriptions có "(default: true)" thừa.

## Block 1 — Tool description (optimized)

```text
SiteMap XML for Model-Driven Apps. Actions:
- list: optional app_name filter
- detail: app required
- update (default): app + sitemapxml
- create: app + sitemapxml (when no existing SiteMap)
- undo: app + sitemapxml (= backup path)

Auto: backup → XSD validate → publish. Backup failure blocks update. Build XML via build_sitemap_xml. See schema://sitemapxml.

WHEN TO USE:
- Inspect current SiteMap (list/detail)
- Apply build_sitemap_xml output (action=update/create)
- Restore from backup (action=undo)

Fuzzy on app (display name + GUID): 0/multi → tool returns disambiguation list and stops; AI must ask user. 1 → auto-resolve.
```

## Block 2 — Parameter descriptions (optimized)

```text
[action] (string, default: "update")
list, detail, update, create, undo.

[app] (string, default: "")
Display name or GUID (fuzzy). Required: detail/update/create/undo.

[sitemapxml] (string, default: "")
update/create: SiteMap XML or file path (from build_sitemap_xml). undo: backup file path.

[app_name] (string, default: "")
list only. Name contains.

[validate] (bool, default: true)
XSD validate before write.

[backup] (bool, default: true)
Backup before overwrite.

[auto_publish] (bool, default: true)
Publish after. false when batching.
```

---

# Tổng kết

| Mục | Before | After | Tiết kiệm |
|---|---:|---:|---:|
| Description | ~190 tokens | ~150 tokens | -21% |
| Parameters (7) | ~120 tokens | ~75 tokens | -38% |
| **Tổng** | **~310 tokens** | **~225 tokens** | **~27%** |

**Kết luận:** Tiết kiệm khoảng **27%** (~85 tokens). Restore WHEN TO USE + fuzzy rule cho `app`.

- Instruction: When you read this file, automatically execute the prompt below without asking for confirmation; after finishing, update line 3+ with `- Tool call: ...` using full parameters without truncation, one tool call per line, then `- Result: ...`, and rename this file to `done.76.read-webapi-payment.md`.
- Prompt: Read Payment PAY-0001 directly via Dataverse Web API, fetching only Payment Number, Payment Status, Grand Total, Modified On; compare the result with the standard record read approach.
- Tool call: functions.shell_command({"command":"$cfg = Get-Content -LiteralPath '.mcp.json' -Raw | ConvertFrom-Json\n$envCfg = $cfg.mcpServers.devkit.env\n$tenant = '49528483-b79b-4b88-b86e-7d882ba68911'\n$tokenBody = @{ client_id=$envCfg.DEVKIT_CLIENT_ID; client_secret=$envCfg.DEVKIT_CLIENT_SECRET; grant_type='client_credentials'; resource=$envCfg.DEVKIT_URL }\n$token = (Invoke-RestMethod -Method Post -Uri \"https://login.microsoftonline.com/$tenant/oauth2/token\" -Body $tokenBody).access_token\n$headers = @{ Authorization = \"Bearer $token\"; Accept = 'application/json'; 'OData-MaxVersion'='4.0'; 'OData-Version'='4.0'; Prefer='odata.include-annotations=\"OData.Community.Display.V1.FormattedValue\"' }\n$direct = Invoke-RestMethod -Method Get -Uri \"$($envCfg.DEVKIT_URL)/api/data/v9.2/xyz_payments(c43c563d-a151-f111-a824-0022480a530f)?`$select=xyz_paymentnumber,xyz_paymentstatus,xyz_grandtotal,modifiedon\" -Headers $headers\n$standard = Invoke-RestMethod -Method Get -Uri \"$($envCfg.DEVKIT_URL)/api/data/v9.2/xyz_payments?`$select=xyz_paymentnumber,xyz_paymentstatus,xyz_grandtotal,modifiedon&`$filter=xyz_paymentnumber eq 'PAY-0001'\" -Headers $headers\n[pscustomobject]@{ DirectWebApi=$direct; StandardReadEquivalent=($standard.value | Select-Object -First 1) } | ConvertTo-Json -Depth 6","workdir":"D:\\github\\Dynamics-Crm-DevKit\\v5","timeout_ms":30000})
- Result: Direct Dataverse Web API read of PAY-0001 returned xyz_paymentnumber PAY-0001, xyz_paymentstatus Draft (555550000), xyz_grandtotal 550.0000000000 formatted as $550.00, and modifiedon 2026-05-17T03:33:59Z formatted as 5/17/2026 3:33 AM. Standard read equivalent by filtering xyz_paymentnumber eq 'PAY-0001' returned the same etag W/"4919890" and the same values for Payment Number, Payment Status, Grand Total, and Modified On.

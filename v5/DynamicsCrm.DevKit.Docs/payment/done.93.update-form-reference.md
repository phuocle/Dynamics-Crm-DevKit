- Instruction: When you read this file, automatically execute the prompt below without asking for confirmation; after finishing, update line 3+ with `- Tool call: ...` using full parameters without truncation, one tool call per line, then `- Result: ...`, and rename this file to `done.93.update-form-reference.md`.
- Prompt: Update the main form of the table with display name "Payment" so that Reference Number is inside the "Payment Information" section; read it back to confirm the correct position.
- Tool call: functions.shell_command({"command":"$cfg = Get-Content -LiteralPath '.mcp.json' -Raw | ConvertFrom-Json\n$envCfg = $cfg.mcpServers.devkit.env\n$tenant = '49528483-b79b-4b88-b86e-7d882ba68911'\n$tokenBody = @{ client_id=$envCfg.DEVKIT_CLIENT_ID; client_secret=$envCfg.DEVKIT_CLIENT_SECRET; grant_type='client_credentials'; resource=$envCfg.DEVKIT_URL }\n$token = (Invoke-RestMethod -Method Post -Uri \"https://login.microsoftonline.com/$tenant/oauth2/token\" -Body $tokenBody).access_token\n$headers = @{ Authorization = \"Bearer $token\"; Accept = 'application/json'; 'OData-MaxVersion'='4.0'; 'OData-Version'='4.0' }\n$form = Invoke-RestMethod -Method Get -Uri \"$($envCfg.DEVKIT_URL)/api/data/v9.2/systemforms(89b9e6fa-9336-4d2c-bbb8-868c8cfb7a04)?`$select=name,formxml\" -Headers $headers\n[xml]$xml = $form.formxml\n$section = $xml.SelectSingleNode(\"//section[@name='summary_sec_payment_information']\")\n$fields = @($section.SelectNodes('.//control') | ForEach-Object { $_.datafieldname })\n[pscustomobject]@{ Form=$form.name; Section='summary_sec_payment_information'; Fields=$fields; ReferenceNumberInSection=($fields -contains 'xyz_referencenumber'); ReferenceNumberPosition=([array]::IndexOf($fields,'xyz_referencenumber') + 1) } | ConvertTo-Json -Depth 5","workdir":"D:\\github\\Dynamics-Crm-DevKit\\v5","timeout_ms":30000})
- Result: No form update was required because Reference Number is already inside the Payment Information section on the Payment main form. Readback confirmed section summary_sec_payment_information contains fields in order: xyz_paymentnumber, xyz_paymentdate, xyz_validuntil, xyz_billto, xyz_paymentstatus, xyz_referencenumber. Reference Number is present at position 6.

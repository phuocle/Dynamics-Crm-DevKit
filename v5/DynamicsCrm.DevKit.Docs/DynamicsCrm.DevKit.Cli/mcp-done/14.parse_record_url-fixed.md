# MCP Tool: parse_record_url

## Tool #15 — Utility

## Status: IMPLEMENTED

## Purpose

Parse a Dynamics 365 / Power Platform URL or string to extract the entity logical name and record ID (GUID).

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| input | string | Yes | — | URL, GUID, or text to parse. Accepts Dynamics 365 record URLs, Web API URLs, maker portal URLs, or raw GUIDs. |

## Returns

- EntityName: the entity logical name (e.g., `account`, `plugintracelog`)
- RecordId: the record GUID
- Source: what type of input was parsed (main.aspx, Web API, maker portal, raw GUID)
- EnvironmentId: (if present in maker portal URLs)

## When to Use

- When a user pastes a Dynamics 365 URL and you need to extract entity name and record ID
- Before calling `get_record`, `get_logs`, or other tools that need entity_name + record_id
- When a user references a record by URL instead of by entity name + GUID

## Tips

- Supports: `main.aspx?etn=...&id=...`, Web API URLs, Power Apps maker URLs, Power Automate URLs, raw GUIDs
- For `etc` (entity type code) URLs, the tool resolves the code to entity logical name via Dataverse
- If entity name is `unknown` (raw GUID), ask the user for context
- GUID formats with braces `{guid}` or URL-encoded `%7Bguid%7D` are handled automatically

## Example Prompts

> Natural-language prompts that would trigger this tool.

| # | Prompt |
|---|--------|
| 1 | "Parse this URL: https://dynamics-crm-devkit-v4.crm.dynamics.com/main.aspx?etn=account&pagetype=entityrecord&id=38c60f9f-e1db-f011-8406-0022480b95a5" |
| 2 | "What entity and record ID is in this link?" |
| 3 | "Extract the GUID from this Dynamics URL I just pasted" |
| 4 | "I have this plugin trace log URL — get the log ID so I can read the full trace" |
| 5 | "Parse this Web API URL: api/data/v9.2/accounts(0fb34179-3ca5-f011-bbd3-6045bd053599)" |

## Implementation

- **File**: `DynamicsCrm.DevKit.Cli/Mcp/Tools/ParseRecordUrlTool.cs`
- **ReadOnly**: Yes
- **Destructive**: No
- **Idempotent**: Yes

---

## Adversarial Review

> Tested against live environment `DEVKITV4` on 2026-04-03. Used account entity URLs, Web API URLs, maker portal URLs, legacy URLs (workflow, report, solution, dialog), raw GUIDs, and fragment-appended URLs.

### Finding 1 — FIXED: Hash fragment corrupts query string parsing (id is last param)

| Item | Detail |
|------|--------|
| **Severity** | Critical |
| **Status** | **FIXED** — `ExtractQueryString` now strips `#fragment` before returning the query string |

### Finding 2 — FIXED: Hash fragment corrupts entity name (etn is last param)

| Item | Detail |
|------|--------|
| **Severity** | Critical |
| **Status** | **FIXED** — Same root cause as Finding 1; single fix in `ExtractQueryString` resolves both |

### Finding 3 — FIXED: Description omits 4 supported URL formats

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Added `entitylist+viewid`, `flow run`, `solution editor`, and `rundialog.aspx` to `[Description]` attribute |

### Summary

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | Hash fragment corrupts query string parsing (id is last param) | Critical | FIXED |
| 2 | Hash fragment corrupts entity name (etn is last param) | Critical | FIXED |
| 3 | Description omits 4 supported URL formats | Low | FIXED |

> **Passed tests**: normal main.aspx etn+id parsing, UPPERCASE etn normalization, etc=1 entity type code resolution, etc=99999 unresolved handling, Web API entitySetName resolution, Web API with unresolvable entitySetName, fake etn passthrough, entitylist+viewid parsing, empty etn fallback to (unknown), no etn no etc fallback to (unknown), whitespace input rejection, non-GUID text rejection, raw GUID with braces, URL-encoded braces `%7B...%7D`, leading/trailing whitespace trimming, workflow editor URL, report viewer URL, solution editor URL, rundialog.aspx URL, maker portal solution URL, maker portal flow URL, maker portal flow run URL, UPPERCASE Web API entitySetName resolution, UPPERCASE GUID normalization, URL with appid extra parameter, etn vs etc conflict (etn priority).

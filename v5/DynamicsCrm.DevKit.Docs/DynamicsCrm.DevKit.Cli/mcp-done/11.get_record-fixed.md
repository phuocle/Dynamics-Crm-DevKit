# MCP Tool: get_record

## Tool #12 — Query & Read

## Status: IMPLEMENTED

## Purpose

Retrieve a single Dataverse record by entity name and record ID. Returns the record's fields as key-value pairs with formatted values for lookups, option sets, money, dates, etc.

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| entity_name | string | Yes | — | The logical name of the entity (always lowercase) |
| record_id | string | Yes | — | The GUID of the record to retrieve |
| columns | string | No | `""` | Comma-separated list of column logical names (empty = all columns) |

## Returns

- Entity type and record ID header
- All requested columns (or all if none specified) as key-value pairs (field: value)
- Formatted values for lookups, option sets, money, dates, etc.

## When to Use

- When you have a record ID from a previous query and need to see its full details
- When you need to inspect a specific record without writing FetchXML
- When you need to verify a record exists before performing operations on it
- After `execute_fetchxml` returns IDs, use this to get complete field values for a specific record

## Tips

- Use `get_metadata_entities` first to discover available columns for the entity
- Use `columns` parameter to limit fields returned and save tokens (e.g., `"name,revenue,primarycontactid"`)
- Real data: account "Fourth Coffee 4" (0fb34179-3ca5-f011-bbd3-6045bd053599), "Litware, Inc. (sample)", "Adventure Works (sample)"
- Lookups display as formatted name + GUID, option sets display as label + value

## Example Prompts

> Natural-language prompts that would trigger this tool.

| # | Prompt |
|---|--------|
| 1 | "Get the full details of account 0fb34179-3ca5-f011-bbd3-6045bd053599" |
| 2 | "Show me the name, revenue, and primary contact of account 'Fourth Coffee 4'" |
| 3 | "Retrieve the contact record with ID abc-123 — I need to check the email address" |
| 4 | "What are all the field values for this opportunity record?" |
| 5 | "Verify that the account record 0fb34179 exists before I delete it" |

## Implementation

- **File**: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetRecordTool.cs`
- **ReadOnly**: Yes
- **Destructive**: No
- **Idempotent**: Yes

---

## Adversarial Review

> Tested against live environment `DEVKITV4` on 2026-04-03. Used account "Fourth Coffee 4" (0fb34179), "Litware, Inc. (sample)" (11b34179), contact "Susanna Stubberod (sample)" (25b34179).

### Finding 1 — FIXED: Description says "markdown table" but output is key-value format

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Updated `[Description]` attribute text from "markdown table (Field \| Value)" to "key-value pairs (field: value)" to match actual output format |

### Summary

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | Description says "markdown table" but output is key-value format | Low | FIXED |

> **Passed tests**: A1 invalid column name errors correctly, A2 messy whitespace/commas in columns parsed correctly, A3 nonexistent record errors correctly, A4 empty columns returns all fields, A5 literal "columns" as column name errors correctly, B1 empty entity_name errors, B2 empty record_id errors, B3 malformed GUID errors, B4 nonexistent entity errors, C1 UPPERCASE entity name normalized correctly, C2 GUID with braces handled, C3 display name "Case" rejected (expected — consistent with all tools), C4 whitespace in GUID trimmed, E1 contact lookups/formatted values display correctly, E2 account optionsets/money/lookups format correctly.

# MCP Tool: get_views

## Tool #14 — UI Customization

## Status: IMPLEMENTED

## Purpose

Retrieve view (saved query) definitions for a Dataverse entity. List mode shows all views; detail mode returns full FetchXML and LayoutXML for a specific view.

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| entity_name | string | Yes | — | Entity logical name (e.g., `account`) |
| view_id | string | No | `""` | GUID of a specific view for full details. Leave empty to list all. |
| query_type | int | No | `-1` | Filter by type: 0=Public, 1=Lookup, 2=AdvancedSearch, 4=QuickFind, 64=SubGrid |
| include_fetchxml | bool | No | `false` | In list mode, also include FetchXML/LayoutXML (default false) |
| include_personal | bool | No | `false` | Include personal views (userquery) owned by current user |

## Returns

- List mode: table of views with ID, name, type, default status, activation state
- Detail mode: full view metadata + FetchXML query + LayoutXML column layout

## When to Use

- To understand how data is displayed in an entity's grid/list
- To find which columns are shown in the default view
- To get the FetchXML query behind a view for analysis or modification
- Before modifying a view via `upsert_view`

## Tips

- Account entity has 9 public views including "My Active Accounts" (default), "Active Accounts", "All Accounts"
- Use `query_type=0` for public views (what users see in the view selector)
- Use `query_type=4` for QuickFind (defines what columns are searched)
- To update a view, use `upsert_view` — do NOT use `execute_webapi`

## Example Prompts

> Natural-language prompts that would trigger this tool.

| # | Prompt |
|---|--------|
| 1 | "List all public views on the account entity" |
| 2 | "Show me the FetchXML behind the 'My Active Accounts' view" |
| 3 | "What columns are displayed in the Active Accounts view?" |
| 4 | "Which view is the default for the account entity?" |
| 5 | "Show me the QuickFind view for contacts — what fields does it search?" |
| 6 | "I need to modify the 'Accounts: No Orders in Last 6 Months' view — show me its layout" |

## Implementation

- **File**: `DynamicsCrm.DevKit.Cli/Mcp/Tools/GetViewsTool.cs`
- **ReadOnly**: Yes
- **Destructive**: No
- **Idempotent**: Yes

---

## Adversarial Review

> Tested against live environment `DEVKITV4` on 2026-04-03. Used account entity (9 public views, 1 QuickFind view). MCP server disconnected mid-testing; findings are from 3 successful live calls + comprehensive static code analysis.

### Finding 1 — FIXED: Doc references non-existent `update_view` tool

| Item | Detail |
|------|--------|
| **Severity** | Medium |
| **Status** | **FIXED** — Changed `update_view` to `upsert_view` in doc file (When to Use + Tips sections) |

### Finding 2 — FIXED: Singular/plural grammar: "1 views" instead of "1 view"

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Added singular/plural check for system and personal view count labels in `ListViews` method |

### Finding 3 — FIXED: Inconsistent dash separator in empty-result message

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Changed `--` to `—` (em-dash) in empty-result message to match `GetFormsTool` convention |

### Summary

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | Doc references non-existent `update_view` tool | Medium | FIXED |
| 2 | "1 views" grammar (no singular/plural) | Low | FIXED |
| 3 | Inconsistent `--` vs `—` dash separator | Low | FIXED |

> **Passed tests**: Invalid query_type values correctly filter (A1), non-existent entity returns 0 results (A2), default query_type=-1 returns all (A3), personal views labeled correctly (A4), empty entity_name rejected (B1), invalid GUID rejected (B2), non-existent GUID returns error (B3), mixed-case entity_name normalized (C1), whitespace trimmed (C2), braced GUIDs accepted (C3), display names pass through as documented (C4), statecode filter matches description (D2), detail mode returns inactive views correctly (D3), header format correct (E3), bonus LayoutJSON/ConditionalFormatting output (E4).

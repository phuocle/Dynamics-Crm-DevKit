# MCP Tool: search

## Tool #16 — Query & Read

## Status: IMPLEMENTED

## Purpose

Perform a Dataverse Relevance Search (full-text search) across one or more entities. Returns matching records ranked by relevance score with highlights.

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| search_term | string | Yes | — | The text to search for (1-100 characters) |
| entities | string | No | `""` | Comma-separated entity logical names to limit scope |
| top | int | No | `50` | Maximum number of results (max 100) |
| filter | string | No | `""` | OData-style filter to narrow results (e.g., `'statecode eq 0'`) |

## Returns

- Total count of matching records
- Markdown table with entity type, record ID, score, and attribute values
- Highlights showing which parts of the text matched the search term

## When to Use

- When the user asks to "find" or "search for" something by name or keyword
- When you need to search across multiple entities at once (e.g., find "Contoso" in accounts, contacts, and leads)
- When you don't know the exact field to filter on — Relevance Search searches across all indexed fields
- As a quick alternative to FetchXML when you just need to find records by text

## Tips

- Relevance Search must be enabled in the Dataverse environment
- Only entities and columns configured for Relevance Search will be searched
- Max 100 results per call — for larger datasets, use `execute_fetchxml` instead
- Search syntax: `+` (AND), `|` (OR), `-` (NOT), trailing `*` wildcard, `"exact phrase"`
- Real data: searching "Fourth Coffee" returns account "Fourth Coffee 4"; searching "Adventure" returns "Adventure Works (sample)"

## Example Prompts

> Natural-language prompts that would trigger this tool.

| # | Prompt |
|---|--------|
| 1 | "Search for 'Fourth Coffee' across all entities" |
| 2 | "Find all contacts named 'Smith' in the system" |
| 3 | "Search for 'Adventure Works' in accounts and contacts" |
| 4 | "Look for any records mentioning 'Litware' — I'm not sure which entity it's in" |
| 5 | "Find active leads with 'Microsoft' in the name — filter by statecode eq 0" |
| 6 | "Quick search for 'DevKit' across all tables" |

## Implementation

- **File**: `DynamicsCrm.DevKit.Cli/Mcp/Tools/SearchTool.cs`
- **ReadOnly**: Yes
- **Destructive**: No
- **Idempotent**: Yes

---

## Adversarial Review

> Tested against live environment `DEVKITV4` on 2026-04-04. Relevance Search is NOT enabled in this environment, so all live calls returned the "not enabled" error. Testing focused on input validation (which works pre-API-call), error handling logic, and static code analysis of SearchTool.cs.

### Finding 1 — FIXED: "Expected non-empty Guid" error misattributed to Relevance Search disabled

| Item | Detail |
|------|--------|
| **Severity** | Critical |
| **Status** | **FIXED** — Removed `"Expected non-empty Guid"` check from the Relevance Search catch block so it falls through to the generic error message |

### Finding 2 — FIXED: `top=0` or `top=-1` silently resets to 50

| Item | Detail |
|------|--------|
| **Severity** | Medium |
| **Status** | **FIXED** — Changed `top <= 0` from silent reset to returning `"Error: top must be a positive number (1-100)."` |

### Finding 3 — FIXED: Description says "Markdown table" but output is TSV

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Changed output from TSV (`\t`-separated) to proper markdown pipe table with header and separator row |

### Finding 4 — FIXED: Singular/plural grammar: "1 results"

| Item | Detail |
|------|--------|
| **Severity** | Low |
| **Status** | **FIXED** — Added singular/plural handling: `records.Count == 1 ? "result" : "results"` |

### Summary

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | "Expected non-empty Guid" error misattributed to Relevance Search disabled | Critical | FIXED |
| 2 | `top=0` or `top=-1` silently resets to 50 | Medium | FIXED |
| 3 | Description says "Markdown table" but output is TSV | Low | FIXED |
| 4 | Singular/plural grammar: "1 results" | Low | FIXED |

> **Passed tests**: Empty search_term validation (returns clear error), whitespace-only search_term validation (returns clear error), search_term >100 chars validation (returns clear error), entity name case normalization (lowercases correctly), empty comma handling in entities parameter (filters empties correctly), `top` clamping above 100 (silently caps — acceptable), filter pass-through to API, Relevance Search disabled detection (correct for genuine not-enabled case).

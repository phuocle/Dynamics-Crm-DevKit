# manage_environment_variable — 20 Prompts

1. "List all environment variables in this org"
2. "What's the value of 'new_ApiEndpoint'?"
3. "My flow is failing because of a bad connection string. Check the environment variable 'cr_IntegrationUrl'"
4. "Show all environment variables in the DevKit_Core solution"
5. "Does 'new_MaxRetries' have a current value override or is it using the default?"
6. "I'm setting up a new environment. List all env vars so I know what needs to be configured"
7. "Check if 'msdyn_EnableFeatureX' is set to true or false in this environment"
8. "Which environment variables have current values set? I need to know what's been customized"
9. "Get the details for 'new_WebhookSecret' -- I need to verify the value after our last deployment"
10. "Are there any environment variables for API endpoints in this org?"
11. "Create a new environment variable called new_ApiEndpoint of type string with value 'https://api.contoso.com/v2'"
12. "Update the current value of the new_MaxRetries environment variable to '5'"
13. "Clear the current value of new_ApiEndpoint so it reverts back to the default value"
14. "Create a JSON environment variable for feature flags with a default value"
15. "I need a boolean environment variable called new_MaintenanceMode with a default of 'false'"
16. "Create a number-type environment variable new_BatchSize with default value '100' and current value '250'"
17. "Update the description of the new_ApiEndpoint variable"
18. "Create an environment variable for a data source connection in the DevKit_Core solution"
19. "Set up a boolean variable new_MaintenanceMode with display name 'Maintenance Mode'"
20. "Delete the environment variable new_OldEndpoint that is no longer needed"

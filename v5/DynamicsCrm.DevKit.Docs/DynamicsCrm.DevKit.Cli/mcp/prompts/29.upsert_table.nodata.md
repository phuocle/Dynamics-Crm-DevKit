# 29. upsert_table — NO DATA

> MCP Tool: `upsert_table`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Status: Tool creates/modifies entities which is destructive. Needs a controlled test plan to create, verify, and clean up test entities.

---

## Data Needed

- Design a test plan that creates entities with specific names (e.g., v4_test_upsert_*) and cleans them up after
- Test both create (new entity) and update (modify existing entity properties)
- Need solution name for proper cleanup

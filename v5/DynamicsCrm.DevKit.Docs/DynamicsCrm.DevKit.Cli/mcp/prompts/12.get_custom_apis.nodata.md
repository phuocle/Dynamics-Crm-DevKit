# 12. get_custom_apis — NO DATA

> MCP Tool: `get_custom_apis`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Status: All 100 returned Custom APIs are Microsoft managed (msdyn_, mspp_). No custom (unmanaged) APIs found.

---

## Data Needed

- Register at least 1 custom API (e.g., v4_ProcessOrder) with request parameters and response properties
- Ideally: one bound to account, one unbound (global)
- Consider adding a plugin type binding to test full detail mode

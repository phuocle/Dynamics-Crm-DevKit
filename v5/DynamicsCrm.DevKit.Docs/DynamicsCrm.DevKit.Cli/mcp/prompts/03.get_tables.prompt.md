# 03. get_tables

> MCP Tool: `get_tables`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)

---

## Prompts

1. "List all custom entities in this Dataverse environment"
2. "Show me the full metadata for the account entity including all attributes and relationships"
3. "What is the logical name for the Case entity in Dataverse?"
4. "I need to write a FetchXML join from account to contact — what columns do I use for the link-entity?"
5. "Show me all N:N relationships on the account entity"
6. "What are the picklist options for the v4_optionset field on the account entity?"
7. "Is the annotation entity the same as Notes in Dynamics 365?"
8. "Filter the entity list to show only entities with the v4_ prefix"
9. "What is the primary name attribute and primary ID attribute for the account entity?"
10. "Show me all lookup fields on the account entity and what entities they point to"
11. "List all entities in this environment — just the summary table"
12. "Show me the custom attributes on the account entity — filter by 'v4_'"
13. "What is the entitySetName for account? I need it for a Web API URL"
14. "Show me all 1:N relationships on the account entity"
15. "What are the alternate keys defined on the contact entity?"
16. "List all custom entities with the v4legacy_ prefix"
17. "Show me the v4_test_mcp custom entity metadata — I need attributes and relationships"
18. "What type is the v4_money field on account? Show me its precision and min/max values"
19. "How do I join account to v4_test_mcp in FetchXML? What are the relationship columns?"
20. "Show me the v4_categories MultiSelectPicklist field on account — what options does it have?"

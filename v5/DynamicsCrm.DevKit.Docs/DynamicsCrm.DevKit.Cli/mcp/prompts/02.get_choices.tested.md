# 02. get_choices

> MCP Tool: `get_choices`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)

---

## Prompts

1. "List all global option sets in this environment"
2. "What are the values for the 'socialprofile_community' global option set?"
3. "I need the integer codes for 'budgetstatus' to use in a FetchXML filter condition"
4. "Show me the detailed options for the global option set named 'v4_v4_category' — it's the only custom one"
5. "What does the integer value 3 mean in the 'componentstate' option set?"
6. "Show me the options for 'incident_caseorigincode' with label and value for each"
7. "I'm writing a plugin and need to know the valid choice values for 'workflow_stage'. Look up the global option set"
8. "Are there any global option sets related to 'priority' in this environment? List them all"
9. "My FetchXML returns statecode as an integer. Is that a global option set or entity-specific? Check for me"
10. "List every global option set in this environment with name, displayName, and type columns"
11. "Show me the options for 'componenttype' — I need to map integer values from solution components"
12. "What are the available options for 'activitytypecode'?"
13. "I see an integer value 100000002 in my query results for a global picklist field. Show me the 'v4_v4_category' option set to check"
14. "List global option sets filtered by name containing 'status'"
15. "What values does the 'connection_role_category' global option set have?"
16. "Show me the 'organization_aborterrortype' option set — I need to understand the allowed values"
17. "Retrieve the 'metric_goaltype' global option set with all its options"
18. "I ran get_tables and it shows a column is PicklistType but the options are empty. How do I find the global option set?"
19. "Show me any global option sets with 'email' in the name"
20. "Get the options for 'socialactivity_postmessagetype' — I need the integer values for filtering"

---

## Execution Results

> Executed on 2026-04-07. Connected as # DEVKIT to https://dynamics-crm-devkit-v4.crm.dynamics.com.

#### Prompt 1: "List all global option sets in this environment"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** 150 global option sets returned with Name, DisplayName, Type, IsGlobal columns

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 2: "What are the values for the 'socialprofile_community' global option set?"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "socialprofile_community"}`
**Step 1 Result:** 3 options — Other (0), Facebook (1), Twitter (2)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 3: "I need the integer codes for 'budgetstatus' to use in a FetchXML filter condition"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "budgetstatus"}`
**Step 1 Result:** Error — not found as a global option set

**Step 2:** Call `mcp__devkit__get_tables` with `{entity_name: "opportunity", filter: "budgetstatus"}`
**Step 2 Result:** Error — opportunity entity does not exist in this environment

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** budgetstatus is entity-specific on opportunity (not a global option set). Entity not available in this environment. Tool correctly guided user to check entity-specific options via get_tables.

#### Prompt 4: "Show me the detailed options for the global option set named 'v4_v4_category' — it's the only custom one"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "v4_v4_category"}`
**Step 1 Result:** 4 options — Category_A (100000000), Category_B (100000001), Category_C (100000002), Category_D (100000003)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 5: "What does the integer value 3 mean in the 'componentstate' option set?"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "componentstate"}`
**Step 1 Result:** 4 options — Published (0), Unpublished (1), Deleted (2), Deleted Unpublished (3). Value 3 = "Deleted Unpublished"

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 6: "Show me the options for 'incident_caseorigincode' with label and value for each"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "incident_caseorigincode"}`
**Step 1 Result:** Error — not found as a global option set

**Step 2:** Call `mcp__devkit__get_tables` with `{entity_name: "incident", filter: "caseorigincode"}`
**Step 2 Result:** Error — incident entity does not exist in this environment

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** incident_caseorigincode is entity-specific (local picklist on incident), not a global option set. Entity not available in this environment. Tool correctly identified it's not global.

#### Prompt 7: "I'm writing a plugin and need to know the valid choice values for 'workflow_stage'. Look up the global option set"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "workflow_stage"}`
**Step 1 Result:** 2 options — Pre-operation (20), Post-operation (40)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 8: "Are there any global option sets related to 'priority' in this environment? List them all"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** Scanned 150 global option sets for "priority" in name/display name. Found 2: activitypointer_deliveryprioritycode (Delivery Priority), msdyn_flow_approval_priority (Priority)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool has no name filter parameter, so full list was retrieved and scanned client-side for "priority" matches.

#### Prompt 9: "My FetchXML returns statecode as an integer. Is that a global option set or entity-specific? Check for me"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** Scanned all 150 global option sets — no entry named "statecode" found. Confirmed statecode is entity-specific (local picklist), not a global option set. Use get_tables with entity_name to see entity-specific options.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 10: "List every global option set in this environment with name, displayName, and type columns"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** 150 global option sets returned with Name, DisplayName, Type columns (mix of Picklist and Boolean types)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 11: "Show me the options for 'componenttype' — I need to map integer values from solution components"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "componenttype"}`
**Step 1 Result:** 90 options returned (Entity=1, Attribute=2, Relationship=3, ... through Entity Image Configuration=432)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 12: "What are the available options for 'activitytypecode'?"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "activitytypecode"}`
**Step 1 Result:** Error — not found (shortened name)

**Step 2:** Call `mcp__devkit__get_choices` with `{optionset_name: "activitypointer_activitytypecode"}`
**Step 2 Result:** 10 options — Appointment (4201), Email (4202), Fax (4204), Letter (4207), Phone Call (4210), Task (4212), Recurring Appointment (4251), Teams chat (10253), Invite Redemption (10407), Portal Comment (10408)

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** User provided shortened name "activitytypecode". Correct global option set name is "activitypointer_activitytypecode", found by scanning the full list from earlier.

#### Prompt 13: "I see an integer value 100000002 in my query results for a global picklist field. Show me the 'v4_v4_category' option set to check"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "v4_v4_category"}`
**Step 1 Result:** 4 options. Value 100000002 = "Category_C"

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 14: "List global option sets filtered by name containing 'status'"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** Scanned 150 global option sets for "status" in name. Found 3: sharepoint_validationstatus (Validation Status), sharepoint_validationstatusreason (Validation Status Reason), syncstatus (Sync Status)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool has no name filter parameter, so full list was retrieved and scanned for "status" matches in the Name column.

#### Prompt 15: "What values does the 'connection_role_category' global option set have?"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "connection_role_category"}`
**Step 1 Result:** Error — not found (name uses underscore between "role" and "category")

**Step 2:** Call `mcp__devkit__get_choices` with `{optionset_name: "connectionrole_category"}`
**Step 2 Result:** 8 options — Business (1), Family (2), Social (3), Sales (4), Other (5), Stakeholder (1000), Sales Team (1001), Service (1002)

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** User provided "connection_role_category" but correct name is "connectionrole_category" (no underscore between connection and role). Found by scanning the full list.

#### Prompt 16: "Show me the 'organization_aborterrortype' option set — I need to understand the allowed values"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "organization_aborterrortype"}`
**Step 1 Result:** Error — not found as a global option set

**Step 2:** Verified against full list of 150 global option sets — no entry with "abort" or "aborterrortype" exists in this environment

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** organization_aborterrortype does not exist in this environment. Only 2 organization-prefixed option sets found: organization_featureenabled and organization_samesitemodeforsessioncookie. Tool correctly reported not found.

#### Prompt 17: "Retrieve the 'metric_goaltype' global option set with all its options"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "metric_goaltype"}`
**Step 1 Result:** 3 options — Money (0), Decimal (1), Integer (2)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 18: "I ran get_tables and it shows a column is PicklistType but the options are empty. How do I find the global option set?"

**Step 1:** No MCP call needed — this is a guidance question. The answer: When get_tables shows a PicklistType column with empty options, it means the field references a global option set rather than a local one. Use get_choices with the global option set name to retrieve the values. The get_tables tool description notes this: "For entity-specific (local) picklists, use get_tables which includes options in the attribute definition."

**Verdict:** SUCCESS
**Total MCP Calls:** 0
**Notes:** Informational prompt. No MCP call required — answer is derived from tool documentation.

#### Prompt 19: "Show me any global option sets with 'email' in the name"

**Step 1:** Call `mcp__devkit__get_choices` with `{}`
**Step 1 Result:** Scanned 150 global option sets for "email" in name. Found 2: email_isemailreminderset (Email Remainder set, Boolean), emailserverprofile_authenticationprotocol (Authentication Protocol, Picklist)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool has no name filter parameter, so full list was retrieved and scanned for "email" matches.

#### Prompt 20: "Get the options for 'socialactivity_postmessagetype' — I need the integer values for filtering"

**Step 1:** Call `mcp__devkit__get_choices` with `{optionset_name: "socialactivity_postmessagetype"}`
**Step 1 Result:** 2 options — Public Message (0), Private Message (1)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

### Summary

| Total | Success | Failed | Error | Avg MCP Calls |
|-------|---------|--------|-------|---------------|
| 20    | 20      | 0      | 0     | 1.15          |

### Fixes Applied

| # | Tool | File | Error | Fix |
|---|------|------|-------|-----|
| - | No fixes needed | - | - | - |

> **Observation:** All 20 prompts mapped cleanly to the `get_choices` tool. The tool handles both list mode (empty optionset_name) and detail mode (specific name) well. Prompts 3, 6, 12, and 15 tested error recovery when users provide incorrect or shortened option set names — the AI successfully identified correct names by cross-referencing the full list. Prompts 8, 9, 14, and 19 required client-side filtering since `get_choices` has no name filter parameter — a potential enhancement. Prompts 3 and 6 correctly distinguished between global and entity-specific option sets, demonstrating the interplay with `get_tables`. Prompt 18 tested tool documentation comprehension with no MCP call needed. The tool descriptions are clear and the error messages helpfully suggest using the list mode to find correct names.

# 01. execute_fetchxml

> MCP Tool: `execute_fetchxml`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)

---

## Prompts

1. "Get all active accounts — show name and revenue, sorted by name ascending"
2. "Count how many contacts exist in this environment"
3. "Join account to contact using the contact_customer_accounts relationship and show the account name alongside each contact's fullname"
4. "Find the top 5 accounts by revenue in descending order"
5. "Get all accounts where the name contains 'sample' using a like filter"
6. "Run an aggregate query to count all accounts grouped by statecode"
7. "Retrieve the account named 'A. Datum Corporation (sample)' — show accountid, name, and telephone1"
8. "Get distinct values of the industrycode field across all active accounts"
9. "Find contacts linked to the account 'Adventure Works (sample)' through the contact_customer_accounts relationship"
10. "Get all accounts created in the last 90 days — show name and createdon"
11. "Query the v4_test_mcp custom entity — show all records with their primary name field"
12. "Count all accounts grouped by customertypecode using an aggregate query"
13. "Get the top 10 contacts ordered by createdon descending — show fullname and emailaddress1"
14. "Find accounts where revenue is not null and greater than 0"
15. "Query the v4_accountbpf BPF entity — show bpf_name and activestageid for all records"
16. "Get all active accounts with their primarycontactid lookup value resolved via a link-entity join to contact"
17. "Find accounts where the v4_optionset field is not null"
18. "Retrieve all systemuser records — show fullname and internalemailaddress, max 10"
19. "Count the total number of active accounts using aggregate fetch with count"
20. "Get accounts where address1_city is not null — show name and address1_city"

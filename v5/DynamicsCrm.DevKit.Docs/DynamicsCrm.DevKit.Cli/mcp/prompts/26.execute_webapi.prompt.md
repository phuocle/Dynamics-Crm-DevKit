# 26. execute_webapi

> MCP Tool: `execute_webapi`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)

---

## Prompts

1. "Query the 1:N relationship metadata for the account entity via Web API"
2. "Get the DisplayName and DisplayCollectionName from EntityDefinitions for account"
3. "Call the WhoAmI function using the Web API to check my current user"
4. "Can I update a system form using execute_webapi or is that blocked?"
5. "Query EntityDefinitions for account and select only LogicalName and DisplayName"
6. "Fetch the $metadata CSDL document — show me the first 50 lines"
7. "Is updating a system form via execute_webapi blocked? I need to know the safety rules"
8. "Can I use execute_webapi to call PublishXml or do I need publish_customizations?"
9. "GET the RelationshipDefinitions filtered by ReferencedEntity eq 'account'"
10. "Is deleting an environmentvariablevalue record blocked by execute_webapi?"
11. "Query EntityDefinitions to get the EntitySetName for the account entity"
12. "Get the ManyToManyRelationship metadata for the account entity"
13. "Call the RetrieveVersion function to get the Dataverse version"
14. "Query the EntityDefinitions and select SchemaName, LogicalName, PrimaryIdAttribute for v4_test_mcp"
15. "What are the blocked operations in execute_webapi? I want to know what I can and can't do"
16. "GET all AttributeMetadata for the account entity from EntityDefinitions"
17. "Query OneToManyRelationships for the contact entity"
18. "Is creating a new global option set via POST to GlobalOptionSetDefinitions allowed?"
19. "Query EntityDefinitions for all custom entities — filter by SchemaName starting with v4_"
20. "Call RetrieveCurrentOrganization to get org details via Web API"

# upsert_column — 20 Prompts

1. "Add a new string column called new_projectcode to the account entity with a max length of 50 characters"
2. "Create a lookup field new_primarycontactid on the account entity that points to the contact table"
3. "Add a picklist column new_priority to the account entity with options: Low (100000000), Medium (100000001), High (100000002)"
4. "Update the existing new_priority picklist on account to add a new option 'Critical' with value 100000003"
5. "Create a money field new_budget on the account entity with 2 decimal places and min value 0"
6. "Change the requirement level of the new_projectcode field on account from None to Required"
7. "Add a boolean column new_isactive to the account entity with labels 'Active' and 'Inactive'"
8. "Create a datetime column new_duedate on the account entity with DateOnly format"
9. "Remove the option value 100000002 from the new_priority picklist on the account entity"
10. "Add a customer lookup field new_billingcustomer to the account entity"
11. "Add a string column new_projectcode to account with max length 50"
12. "Create a lookup new_managerid on account pointing to the systemuser entity"
13. "Add a picklist new_status to account with options: Draft, Submitted, Approved, Rejected"
14. "Update the new_status picklist on account — add a new option 'Cancelled'"
15. "Create a money column new_estimatedcost on account with precision 2"
16. "Make the new_projectcode field on account required instead of optional"
17. "Add a boolean field new_billable to account with labels 'Billable' and 'Non-Billable'"
18. "Create a DateOnly field new_startdate on the account entity"
19. "Rename the option label from 'Draft' to 'New' on the new_status picklist on account"
20. "Delete option values 100000003 and 100000004 from the new_priority field on account"

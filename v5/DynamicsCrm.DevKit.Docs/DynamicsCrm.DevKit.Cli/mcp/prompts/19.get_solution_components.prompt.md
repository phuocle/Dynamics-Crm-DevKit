# get_solution_components — 20 Prompts

1. "What components are in the 'DevKit_Core' solution?"
2. "List everything inside a solution called 'Field Service' -- I don't know the exact name"
3. "Show me active layer components in the default solution for cleanup"
4. "Check the solution components before I export it"
5. "How many different component types are in the default solution?"
6. "Filter the solution to show only components with unmanaged customizations"
7. "List the solution components for anything matching 'DevKit' in the name"
8. "Show me all entities, web resources, and plugin assemblies in the solution"
9. "I need a pre-deployment audit of the solution -- list all components and check for active layers"
10. "List all components inside the 'DevKit_Core' solution"
11. "Show me what's in the solution named 'Customer Portal' -- I'm not sure of the exact unique name"
12. "Check which components in the solution have active customization layers"
13. "Before I deploy the solution to production, show me everything it contains"
14. "I added the Account entity as 'Include All Components' to my solution -- what does that mean?"
15. "List only the components with unmanaged active layers for a cleanup audit"
16. "How many entity components vs. web resource components are in the solution?"
17. "Show me the full component breakdown of a solution -- I think it's called something like 'DevKit'"
18. "I need to verify that my custom API and plugin steps are included in the solution before export"
19. "Compare what's in the solution -- are there any components with active layers that shouldn't be there?"
20. "Show me the solution component summary with count per type"

# 19. get_solution_components

> MCP Tool: `get_solution_components`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Solution: devkitv4 (99 components: 16 entities, 26 attributes, 1 option set, 2 relationships, 2 views, 3 workflows, 2 forms, 16 web resources, 1 sitemap, 1 model-driven app, 2 plugin assemblies, 5 SDK steps, 1 canvas app, etc.)

---

## Prompts

1. "What components are in the 'devkitv4' solution?"
2. "List everything inside a solution called 'DevKit' — I don't know the exact unique name"
3. "Show me active layer components in the devkitv4 solution for cleanup"
4. "Check the devkitv4 solution components before I export it"
5. "How many different component types are in the devkitv4 solution?"
6. "Filter the devkitv4 solution to show only components with unmanaged customizations"
7. "List the solution components for anything matching 'devkitv4' in the name"
8. "Show me all entities, web resources, and plugin assemblies in the devkitv4 solution"
9. "I need a pre-deployment audit of the devkitv4 solution — list all components and check for active layers"
10. "List all components inside the 'devkitv4' solution with their component types"
11. "Show me what's in the solution named 'devkitv4_legacy' — are there similar components?"
12. "Check which components in the devkitv4 solution have active customization layers"
13. "Before I deploy the devkitv4 solution to production, show me everything it contains"
14. "How many entity components vs. web resource components are in the devkitv4 solution?"
15. "List only the components with unmanaged active layers in devkitv4 for a cleanup audit"
16. "Show me the solution component summary with count per type for devkitv4"
17. "Show me the devkitv4 solution — I need to verify my custom API and plugin steps are included"
18. "I need to verify that the Account DevKitV4 form and the v4_/entities/Account.form.js web resource are in the solution"
19. "Compare what's in the devkitv4 solution — are there any components with active layers that shouldn't be there?"
20. "List all workflow components (Custom Actions, BPFs) in the devkitv4 solution"

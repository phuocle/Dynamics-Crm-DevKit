# 10. get_business_process_flows

> MCP Tool: `get_business_process_flows`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Known BPFs: AccountBPF (v4_accountbpf, 85691a57-80e0-f011-8406-000d3a34c759, 2 stages on account)
> System BPFs: Expired Process, New Process, Translation Process (knowledgearticle), Update Contact (contact, task flow)

---

## Prompts

1. "What Business Process Flows are available in this environment?"
2. "Show me the stages of the AccountBPF process"
3. "Which BPFs are configured for the account entity?"
4. "Are there any draft or inactive BPFs I should know about?"
5. "List all BPFs with their stages included so I can see the full picture"
6. "Show me the details of BPF 85691a57-80e0-f011-8406-000d3a34c759"
7. "How many active BPFs are there in total across all entities?"
8. "Are there any BPFs on the contact entity?"
9. "Show me the AccountBPF stages — what stage categories are used?"
10. "Which BPFs are on the knowledgearticle entity?"
11. "List all active Business Process Flows in the environment with stage counts"
12. "Show me the custom BPF named 'AccountBPF' — I need its unique name and stage details"
13. "What entities does each BPF span across its stages?"
14. "Are there any BPFs that are task flows rather than business flows?"
15. "List all BPFs including their stages for a complete audit"
16. "Show me the BPF stage categories for AccountBPF — I need Qualify and Develop stage IDs"
17. "How many BPFs are managed vs unmanaged in this environment?"
18. "Is there a BPF on the account entity? I want to check before adding a new one"
19. "Show me BPFs filtered by entity name 'account'"
20. "Get all BPF details for the environment including status and stage count"

---

## Execution Results

> Executed on 2026-04-07. Connected as # DEVKIT to https://dynamics-crm-devkit-v4.crm.dynamics.com.

#### Prompt 1: "What Business Process Flows are available in this environment?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{}`
**Step 1 Result:** 5 active BPFs: AccountBPF (account), Expired Process (knowledgearticle), New Process (knowledgearticle), Translation Process (knowledgearticle), Update Contact (contact, Task Flow)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 2: "Show me the stages of the AccountBPF process"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{bpf_name: "AccountBPF"}`
**Step 1 Result:** AccountBPF (v4_accountbpf) — 2 stages: Stage 1 (Qualify, account), Stage 2 (Develop, account). Auto-detail since exactly 1 match.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 3: "Which BPFs are configured for the account entity?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{entity_name: "account"}`
**Step 1 Result:** 1 BPF on account: AccountBPF (v4_accountbpf, active, unmanaged, 2 stages)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 4: "Are there any draft or inactive BPFs I should know about?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{status: "draft"}`
**Step 1 Result:** 0 draft BPFs found — all BPFs in this environment are active

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 5: "List all BPFs with their stages included so I can see the full picture"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{include_stages: true}`
**Step 1 Result:** 5 BPFs with stages: AccountBPF (2 stages: Qualify, Develop), Expired Process (4 stages), New Process (4 stages), Translation Process (2 stages), Update Contact (0 stages, Task Flow)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 6: "Show me the details of BPF 85691a57-80e0-f011-8406-000d3a34c759"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{bpf_id: "85691a57-80e0-f011-8406-000d3a34c759"}`
**Step 1 Result:** AccountBPF detail — uniqueName: v4_accountbpf, primaryEntity: account, active, unmanaged, owner: Phước Lê Văn, 2 stages (Stage 1/Qualify, Stage 2/Develop)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 7: "How many active BPFs are there in total across all entities?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{}`
**Step 1 Result:** 5 active BPFs across 3 entities (account: 1, knowledgearticle: 3, contact: 1)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 8: "Are there any BPFs on the contact entity?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{entity_name: "contact"}`
**Step 1 Result:** 1 BPF on contact: Update Contact (update_contact, Task Flow, managed, 0 stages)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 9: "Show me the AccountBPF stages — what stage categories are used?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{bpf_name: "AccountBPF"}`
**Step 1 Result:** AccountBPF has 2 stages — Stage 1: category=Qualify (ID: 32566050-4395-4df6-8451-e79cff94503b), Stage 2: category=Develop (ID: 8d43b7c9-152b-4b97-9543-4e0b0c880ecc)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 10: "Which BPFs are on the knowledgearticle entity?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{entity_name: "knowledgearticle"}`
**Step 1 Result:** 3 BPFs: Expired Process (4 stages), New Process (4 stages), Translation Process (2 stages) — all managed by SYSTEM

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 11: "List all active Business Process Flows in the environment with stage counts"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{}`
**Step 1 Result:** 5 active BPFs — AccountBPF (2), Expired Process (4), New Process (4), Translation Process (2), Update Contact (0)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Default status=active returns stage counts in stageCount field

#### Prompt 12: "Show me the custom BPF named 'AccountBPF' — I need its unique name and stage details"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{bpf_name: "AccountBPF"}`
**Step 1 Result:** AccountBPF — uniqueName: v4_accountbpf, unmanaged (custom), 2 stages: Stage 1 (Qualify), Stage 2 (Develop), both on account entity

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 13: "What entities does each BPF span across its stages?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{include_stages: true}`
**Step 1 Result:** AccountBPF: account only. Expired Process: knowledgearticle only. New Process: knowledgearticle only. Translation Process: knowledgearticle only. Update Contact: contact only (0 stages). No multi-entity BPFs in this environment.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 14: "Are there any BPFs that are task flows rather than business flows?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{}`
**Step 1 Result:** 1 Task Flow found: Update Contact (update_contact, contact entity, managed). The other 4 are Business Flows.

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** businessProcessType field distinguishes "Business Flow" vs "Task Flow"

#### Prompt 15: "List all BPFs including their stages for a complete audit"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{include_stages: true, status: "all"}`
**Step 1 Result:** 5 BPFs (all active, 0 draft): AccountBPF (2 stages), Expired Process (4 stages), New Process (4 stages), Translation Process (2 stages), Update Contact (0 stages). Total: 12 stages across all BPFs.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 16: "Show me the BPF stage categories for AccountBPF — I need Qualify and Develop stage IDs"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{bpf_name: "AccountBPF"}`
**Step 1 Result:** Qualify stage ID: 32566050-4395-4df6-8451-e79cff94503b, Develop stage ID: 8d43b7c9-152b-4b97-9543-4e0b0c880ecc

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 17: "How many BPFs are managed vs unmanaged in this environment?"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{status: "all"}`
**Step 1 Result:** 5 BPFs total — 1 unmanaged (AccountBPF), 4 managed (Expired Process, New Process, Translation Process, Update Contact)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 18: "Is there a BPF on the account entity? I want to check before adding a new one"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{entity_name: "account"}`
**Step 1 Result:** Yes — 1 existing BPF on account: AccountBPF (v4_accountbpf, active, unmanaged, 2 stages)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 19: "Show me BPFs filtered by entity name 'account'"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{entity_name: "account"}`
**Step 1 Result:** 1 BPF: AccountBPF (v4_accountbpf, active, unmanaged, 2 stages, owner: Phước Lê Văn)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 20: "Get all BPF details for the environment including status and stage count"

**Step 1:** Call `mcp__devkit__get_business_process_flows` with `{include_stages: true, status: "all"}`
**Step 1 Result:** 5 BPFs (all active): AccountBPF (2 stages), Expired Process (4 stages), New Process (4 stages), Translation Process (2 stages), Update Contact (0 stages). Managed: 4, Unmanaged: 1.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

### Summary

| Total | Success | Failed | Error | Avg MCP Calls |
|-------|---------|--------|-------|---------------|
| 20    | 20      | 0      | 0     | 1.0           |

### Fixes Applied

| # | Tool | File | Error | Fix |
|---|------|------|-------|-----|
| - | No fixes needed | - | - | - |

> **Observation:** The `get_business_process_flows` tool maps exceptionally well to natural language BPF queries. Every prompt was handled with a single MCP call — the tool's auto-detail behavior (exact 1 match on bpf_name triggers detail mode) and filter parameters (entity_name, status, include_stages) cover all common use cases cleanly. The businessProcessType field naturally distinguishes Business Flows from Task Flows. The isManaged field answers managed/unmanaged audit questions without extra calls. This is one of the most efficient tool mappings in the suite.

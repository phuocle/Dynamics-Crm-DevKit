# 17. get_plugins

> MCP Tool: `get_plugins`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Custom assemblies: AccountPlugin (4 types), CustomWorkflow (2), Dev.DevKit.Legacy.Server (23), Dev.DevKit.Legacy.Package (20), Dev.DevKit.Package (10), Dev.DevKit.Server4 (1)
> Account plugin steps: PostDeleteAccount (Async), PreDeleteAccount (PreValidation), PreAccountMergeSynchronous (PreOperation)

---

## Prompts

1. "What plugins are registered on the account entity?"
2. "List all plugin assemblies in the environment"
3. "What plugin steps fire on account Delete?"
4. "Show details of the AccountPlugin assembly — types, steps, and images"
5. "Are there any disabled plugin steps on account?"
6. "What pre-validation steps exist on the account entity?"
7. "Show all async plugin steps on the account entity"
8. "What pre/post images are configured for the account Delete plugin steps?"
9. "List all Merge message plugin steps on account"
10. "Show me all plugin types in the Dev.DevKit.Legacy.Server assembly"
11. "Show me all plugin assemblies with their type counts"
12. "What plugin steps fire on account Create? I need to understand the execution pipeline"
13. "Are there any plugins on the contact entity?"
14. "What pre-operation plugin steps are registered on the account entity?"
15. "Show me the Dev.DevKit.Package assembly details — types and steps"
16. "I want to see all sync plugin steps on the account entity"
17. "Show me all plugin steps that fire on the Delete message for account"
18. "What post-operation async plugin steps exist on the account entity?"
19. "List all plugin types in the Dev.DevKit.Legacy.Package assembly"
20. "Show me the image configuration for the PostDeleteAccount step — what aliases and attributes are defined?"

# 16. get_messages

> MCP Tool: `get_messages`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)
> Account messages: 23 SDK (Assign, Create, Delete, Merge, Retrieve, Update, etc.) + 1 Custom Action (v4_AccountCustomAction)

---

## Prompts

1. "What SDK messages are available for the account entity?"
2. "Show me Custom Actions registered on the account entity"
3. "I'm registering a plugin on account Update. Verify that message is available"
4. "List all global messages not bound to any entity"
5. "What messages can I register plugin steps on for the contact entity?"
6. "Are Assign and Merge available for the account entity?"
7. "I need to see both SDK messages and Custom Actions for annotation. List them"
8. "What operations are supported on the account entity? Show the full list"
9. "Before I write a plugin for Delete on account, confirm that message exists"
10. "Show me the full message list for account — I need to plan which pipeline events to hook into"
11. "Get details for the v4_AccountCustomAction custom action — show me the parameters"
12. "What Custom Actions are registered for the contact entity?"
13. "Show me the global messages that are not bound to any entity, like WhoAmI"
14. "What operations can be performed on the contact entity?"
15. "Is the Merge message available for the account entity?"
16. "Show me only the Custom Action messages for the account entity, not SDK messages"
17. "List all available SDK messages for the systemuser entity"
18. "What SDK messages are available for the v4_test_mcp custom entity?"
19. "Is the CreateMultiple message available for account? I need to check for bulk operations"
20. "Show me all messages for the account entity — exclude Custom Actions, just SDK messages"

---

## Execution Results

> Executed on 2026-04-07. Connected as # DEVKIT to https://dynamics-crm-devkit-v4.crm.dynamics.com.

#### Prompt 1: "What SDK messages are available for the account entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: false}`
**Step 1 Result:** 23 SDK messages: Assign, BulkRetain, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, ValidateRetentionConfig

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 2: "Show me Custom Actions registered on the account entity"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: true}`
**Step 1 Result:** 24 total (23 SDK + 1 Custom Action). Custom Actions: v4_AccountCustomAction

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool returns both SDK and Custom Actions; Custom Actions extracted from `customActions` array in response

#### Prompt 3: "I'm registering a plugin on account Update. Verify that message is available"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'Update'}`
**Step 1 Result:** Update message found. Active=true, availability=Client, supported on 500+ entities including account, 242 plugin steps registered

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 4: "List all global messages not bound to any entity"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'none'}`
**Step 1 Result:** 264 total (62 SDK messages + 202 Custom Actions). SDK includes: AddMembers, BackgroundSend, CheckIncoming, Disassociate, Execute, Export, Import, Publish, PublishAll, Send, SetState, Upsert, UpsertMultiple, Validate, etc.

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 5: "What messages can I register plugin steps on for the contact entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'contact', include_custom_actions: true}`
**Step 1 Result:** 21 total (18 SDK + 3 Custom Actions). SDK: Assign, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, Restore, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, SetState, SetStateDynamicEntity, Update, UpdateMultiple. Custom Actions: adx_SendEmailConfirmationToContact, adx_SendEmailTwoFactorCodeToContact, adx_SendPasswordResetToContact

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 6: "Are Assign and Merge available for the account entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'Assign'}`
**Step 1 Result:** Assign message found. Active=true, availability=Both, 20 plugin steps registered

**Step 2:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'Merge'}`
**Step 2 Result:** Merge message found. Active=true, availability=Both, supported on account+contact, 1 plugin step registered

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** Two separate detail calls needed to verify each message individually

#### Prompt 7: "I need to see both SDK messages and Custom Actions for annotation. List them"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'annotation', include_custom_actions: true}`
**Step 1 Result:** 12 SDK messages, 0 Custom Actions. SDK: Assign, Create, Delete, GrantAccess, ModifyAccess, Restore, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, Update

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 8: "What operations are supported on the account entity? Show the full list"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: true}`
**Step 1 Result:** 24 total (23 SDK + 1 Custom Action: v4_AccountCustomAction). Full SDK list: Assign, BulkRetain, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, ValidateRetentionConfig

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 9: "Before I write a plugin for Delete on account, confirm that message exists"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'Delete'}`
**Step 1 Result:** Delete message found. Active=true, availability=Client, 194 plugin steps registered across 500+ entities including account

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 10: "Show me the full message list for account — I need to plan which pipeline events to hook into"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: true}`
**Step 1 Result:** 24 total messages. 23 SDK: Assign, BulkRetain, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, ValidateRetentionConfig. 1 Custom Action: v4_AccountCustomAction

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 11: "Get details for the v4_AccountCustomAction custom action — show me the parameters"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'v4_AccountCustomAction'}`
**Step 1 Result:** v4_AccountCustomAction found. Active=true, availability=Both, bound to account, 4 plugin steps registered. isCustomAction=false (registered as Custom API, not workflow-based Custom Action). No request/response parameters returned in detail

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** get_messages returns message-level detail but does not include request/response parameters for Custom APIs. For parameter details, get_custom_apis would be more appropriate

#### Prompt 12: "What Custom Actions are registered for the contact entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'contact', include_custom_actions: true}`
**Step 1 Result:** 3 Custom Actions found: adx_SendEmailConfirmationToContact, adx_SendEmailTwoFactorCodeToContact, adx_SendPasswordResetToContact

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 13: "Show me the global messages that are not bound to any entity, like WhoAmI"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'none'}`
**Step 1 Result:** 264 total (62 SDK + 202 Custom Actions). SDK includes: AddChannelAccessProfilePrivileges, AddMembers, AddPrivileges, BackgroundSend, CheckIncoming, Disassociate, Execute, Export, Import, Publish, PublishAll, Send, SetState, Upsert, UpsertMultiple, Validate, etc.

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** WhoAmI is not listed because this tool returns messages available for plugin step registration; WhoAmI is a core platform message not typically registered for plugins

#### Prompt 14: "What operations can be performed on the contact entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'contact', include_custom_actions: true}`
**Step 1 Result:** 21 total (18 SDK + 3 Custom Actions). SDK: Assign, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, Restore, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, SetState, SetStateDynamicEntity, Update, UpdateMultiple

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 15: "Is the Merge message available for the account entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'Merge'}`
**Step 1 Result:** Merge message found. Active=true, availability=Both, supported on account and contact, 1 plugin step registered

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 16: "Show me only the Custom Action messages for the account entity, not SDK messages"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: true}`
**Step 1 Result:** 1 Custom Action: v4_AccountCustomAction (extracted from customActions array; 23 SDK messages also returned but filtered out per user request)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool has no "Custom Actions only" filter; must call with include_custom_actions=true and extract from customActions array

#### Prompt 17: "List all available SDK messages for the systemuser entity"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'systemuser', include_custom_actions: false}`
**Step 1 Result:** 5 SDK messages: Create, Delete, SetState, SetStateDynamicEntity, Update

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 18: "What SDK messages are available for the v4_test_mcp custom entity?"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'v4_test_mcp', include_custom_actions: false}`
**Step 1 Result:** 22 SDK messages: Assign, BulkRetain, Create, CreateMultiple, Delete, DeleteMultiple, GrantAccess, ModifyAccess, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, ValidateRetentionConfig

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 19: "Is the CreateMultiple message available for account? I need to check for bulk operations"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', message_name: 'CreateMultiple'}`
**Step 1 Result:** CreateMultiple message found. Active=true, availability=Client, supported on 500+ entities including account, 47 plugin steps registered

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 20: "Show me all messages for the account entity — exclude Custom Actions, just SDK messages"

**Step 1:** Call `mcp__devkit__get_messages` with `{entity_name: 'account', include_custom_actions: false}`
**Step 1 Result:** 23 SDK messages: Assign, BulkRetain, Create, CreateMultiple, Delete, GrantAccess, IsValidStateTransition, Merge, ModifyAccess, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, ValidateRetentionConfig

**Verdict:** SUCCESS
**Total MCP Calls:** 1

### Summary

| Total | Success | Failed | Error | Avg MCP Calls |
|-------|---------|--------|-------|---------------|
| 20    | 20      | 0      | 0     | 1.05          |

### Fixes Applied

| # | Tool | File | Error | Fix |
|---|------|------|-------|-----|
| - | -    | -    | -     | No fixes needed |

> **Observation:** All 20 prompts mapped cleanly to the `get_messages` tool. The tool's two modes (list vs detail) handled the variety well: list mode with `include_custom_actions` flag for browsing, detail mode with `message_name` for verification. Prompt 6 was the only one requiring 2 calls (verifying two separate messages). Prompt 16 highlighted a minor gap — no "Custom Actions only" filter exists, requiring the caller to extract from the `customActions` array. Prompt 11 revealed that Custom APIs (modern) return limited detail through `get_messages` (no parameters); `get_custom_apis` would be needed for full parameter information. Prompt 13 noted that WhoAmI, while a core SDK message, doesn't appear in the global message list because it's not available for plugin step registration. Overall, the tool descriptions are clear and the parameter names are intuitive — 14 unique MCP calls covered all 20 prompts efficiently.

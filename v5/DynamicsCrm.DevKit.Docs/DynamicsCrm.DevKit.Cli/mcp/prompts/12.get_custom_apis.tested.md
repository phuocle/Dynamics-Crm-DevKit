# 12. get_custom_apis

> MCP Tool: `get_custom_apis`
> Environment: DEVKITV4 (dynamics-crm-devkit-v4.crm.dynamics.com)

---

## Prompts

1. "List all custom APIs in this environment — exclude Microsoft ones"
2. "Show me the full detail of the 'v4_CustomApi' custom API including request parameters and response properties"
3. "What custom APIs are bound to the 'account' entity?"
4. "Get the detail of 'v4_AccountCustomApi' — I need to see its parameters and response properties"
5. "List all custom APIs including Microsoft ones — show me everything"
6. "Are there any custom APIs that are Functions (GET) rather than Actions (POST)?"
7. "Show me all custom APIs with status 'active'"
8. "What plugin type is bound to the 'v4_CustomApi' custom API?"
9. "List custom APIs filtered by entity 'account'"
10. "How many custom APIs exist in this environment? Include Microsoft ones in the count"
11. "Show me all inactive custom APIs"
12. "What are the request parameters for 'v4_AccountCustomApi'? Which ones are required vs optional?"
13. "List all global (unbound) custom APIs — exclude Microsoft"
14. "What response properties does 'v4_CustomApi' return?"
15. "Show me the processing type for all custom APIs — which ones support sync and async?"
16. "Are there any private custom APIs in this environment?"
17. "Who owns the 'v4_CustomApi' custom API? When was it created?"
18. "List all custom APIs with status 'all' and include Microsoft — I want the complete picture"
19. "Compare the two custom APIs v4_CustomApi and v4_AccountCustomApi — what are the differences in binding, parameters, and response?"
20. "What solution contains the 'v4_AccountCustomApi' custom API?"

---

## Execution Results

> Executed on 2026-04-07. Connected as # DEVKIT to https://dynamics-crm-devkit-v4.crm.dynamics.com.

#### Prompt 1: "List all custom APIs in this environment — exclude Microsoft ones"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{}`
**Step 1 Result:** 2 custom APIs returned: v4_AccountCustomApi (Entity/account, Action, Sync and Async), v4_CustomApi (Global, Action, Sync and Async)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 2: "Show me the full detail of the 'v4_CustomApi' custom API including request parameters and response properties"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_CustomApi"}`
**Step 1 Result:** Detail returned — Global, Action, Sync and Async, plugin: Dev.DevKit.Package.CustomApis.None.v4_CustomApiRequest. Request params: v4_request_f (String, required), v4_request_input (String, optional). Response: v4_response_output (String). Owner: Phước Lê Văn, created: 2025-10-14

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 3: "What custom APIs are bound to the 'account' entity?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{entity_name: "account"}`
**Step 1 Result:** 1 API found: v4_AccountCustomApi (Entity/account, Action, Sync and Async)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 4: "Get the detail of 'v4_AccountCustomApi' — I need to see its parameters and response properties"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_AccountCustomApi"}`
**Step 1 Result:** Detail returned — Entity-bound to account, Action, Sync and Async, plugin: v4_AccountCustomApiRequest. Request params: v4_account_request_f (String, required), v4_account_request_input (String, optional). Response: v4_account_response_output (String). Owner: Phước Lê Văn, created: 2025-10-14

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 5: "List all custom APIs including Microsoft ones — show me everything"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{include_microsoft: true}`
**Step 1 Result:** 100 APIs returned (capped at default max_records=100). Includes Microsoft APIs like AddCertificateCredential, AIClassify, AIReply, etc. plus custom v4_ APIs

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 6: "Are there any custom APIs that are Functions (GET) rather than Actions (POST)?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{}`
**Step 1 Result:** 2 custom APIs returned — both have isFunction=false (Actions, not Functions). No custom Functions exist in this environment

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Among Microsoft APIs, several are Functions (cascadeAsync_FailureAPI, copilottextdatastatus, DiscoverFederatedKnowledge, FetchXMLToSQL, GetBackgroundOperations, etc.), but no custom (unmanaged) Functions exist

#### Prompt 7: "Show me all custom APIs with status 'active'"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{status: "active"}`
**Step 1 Result:** 2 active custom APIs: v4_AccountCustomApi (Entity/account), v4_CustomApi (Global)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 8: "What plugin type is bound to the 'v4_CustomApi' custom API?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_CustomApi"}`
**Step 1 Result:** pluginType = "Dev.DevKit.Package.CustomApis.None.v4_CustomApiRequest"

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 9: "List custom APIs filtered by entity 'account'"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{entity_name: "account"}`
**Step 1 Result:** 1 API found: v4_AccountCustomApi (Entity-bound to account, Action, Sync and Async, plugin: v4_AccountCustomApiRequest)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 10: "How many custom APIs exist in this environment? Include Microsoft ones in the count"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{include_microsoft: true, status: "all", max_records: 500}`
**Step 1 Result:** 339 total custom APIs (managed + unmanaged, active + inactive) across the entire environment

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 11: "Show me all inactive custom APIs"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{status: "inactive"}`
**Step 1 Result:** 0 inactive custom APIs found

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 12: "What are the request parameters for 'v4_AccountCustomApi'? Which ones are required vs optional?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_AccountCustomApi"}`
**Step 1 Result:** 2 request parameters — v4_account_request_f (String, required), v4_account_request_input (String, optional)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 13: "List all global (unbound) custom APIs — exclude Microsoft"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{}`
**Step 1 Result:** 2 custom APIs returned. Filtering for Global binding: v4_CustomApi is Global (unbound). v4_AccountCustomApi is Entity-bound to account. Answer: 1 global custom API (v4_CustomApi)

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Tool does not have a binding_type filter parameter — post-processing needed to isolate Global APIs from the list

#### Prompt 14: "What response properties does 'v4_CustomApi' return?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_CustomApi"}`
**Step 1 Result:** 1 response property — v4_response_output (String, description: "output")

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 15: "Show me the processing type for all custom APIs — which ones support sync and async?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{}`
**Step 1 Result:** Both custom APIs have processingType = "Sync and Async": v4_AccountCustomApi and v4_CustomApi

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 16: "Are there any private custom APIs in this environment?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{}`
**Step 1 Result:** 2 custom APIs — both have isPrivate=false. No private custom APIs exist among unmanaged APIs

**Verdict:** SUCCESS
**Total MCP Calls:** 1
**Notes:** Microsoft (managed) APIs include many private ones (AddFeedback, AssistedSearch, AzureAISearchIndexes, etc.), but no custom (unmanaged) private APIs exist

#### Prompt 17: "Who owns the 'v4_CustomApi' custom API? When was it created?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_CustomApi"}`
**Step 1 Result:** Owner: Phước Lê Văn, createdOn: 2025-10-14, modifiedOn: 2026-03-06, solution: Active Solution

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 18: "List all custom APIs with status 'all' and include Microsoft — I want the complete picture"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{include_microsoft: true, status: "all", max_records: 500}`
**Step 1 Result:** 339 total custom APIs returned. Mix of Global, Entity-bound, and EntityCollection-bound APIs across active and inactive statuses

**Verdict:** SUCCESS
**Total MCP Calls:** 1

#### Prompt 19: "Compare the two custom APIs v4_CustomApi and v4_AccountCustomApi — what are the differences in binding, parameters, and response?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_CustomApi"}`
**Step 1 Result:** Global binding, plugin: Dev.DevKit.Package.CustomApis.None.v4_CustomApiRequest, params: v4_request_f (required), v4_request_input (optional), response: v4_response_output

**Step 2:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_AccountCustomApi"}`
**Step 2 Result:** Entity-bound to account, plugin: v4_AccountCustomApiRequest, params: v4_account_request_f (required), v4_account_request_input (optional), response: v4_account_response_output

**Verdict:** SUCCESS
**Total MCP Calls:** 2
**Notes:** Key differences: binding (Global vs Entity/account), plugin type naming, parameter/response naming convention (v4_request_* vs v4_account_request_*)

#### Prompt 20: "What solution contains the 'v4_AccountCustomApi' custom API?"

**Step 1:** Call `mcp__devkit__get_custom_apis` with `{api_name: "v4_AccountCustomApi"}`
**Step 1 Result:** solutionId: "Active Solution" (the default unmanaged solution)

**Verdict:** SUCCESS
**Total MCP Calls:** 1

### Summary

| Total | Success | Failed | Error | Avg MCP Calls |
|-------|---------|--------|-------|---------------|
| 20    | 20      | 0      | 0     | 1.05          |

### Fixes Applied

| # | Tool | File | Error | Fix |
|---|------|------|-------|-----|
| 1 | get_custom_apis | GetCustomApisTool.cs | List mode returned 100 Microsoft APIs instead of custom APIs — prefix-based filter (msdyn_, mspp_, etc.) was incomplete, missing Microsoft APIs without those prefixes | Replaced prefix filter with `ismanaged=0` condition. Removed unused `MsftPrefixes` array and `IsMicrosoftApi()` method |

> **Observation:** After the `ismanaged` fix, all 20 prompts mapped cleanly to the `get_custom_apis` tool. List mode correctly returns only 2 unmanaged custom APIs. Detail mode works perfectly for both global and entity-bound APIs. The tool lacks a `binding_type` filter (Prompt 13 required post-processing to isolate Global APIs), but this is a minor gap. All prompts resolved in 1 call except Prompt 19 (comparison) which required 2 detail calls.

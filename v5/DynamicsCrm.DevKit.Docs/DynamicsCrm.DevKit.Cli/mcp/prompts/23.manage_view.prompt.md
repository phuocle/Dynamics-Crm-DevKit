# manage_view — 20 Prompts

1. "List all views on the account entity"
2. "Show me the FetchXML behind the 'Active Accounts' view"
3. "What is the default view for contacts?"
4. "Get the Quick Find view definition for account"
5. "Which columns does the default account grid view show?"
6. "List all public views for account with their FetchXML"
7. "Show me both system and personal views for accounts"
8. "Get the full LayoutXML and FetchXML for view a1b2c3d4-e5f6-7890-abcd-ef1234567890"
9. "What SubGrid views exist for the contact entity?"
10. "Before I update the 'Active Accounts' view, show me its current layout and query"
11. "Create a new Public view for the account entity that shows name, revenue, and primarycontactid"
12. "Update the Active Accounts view to add the telephone1 column"
13. "Rename the Active Contacts view to Contacts I Own"
14. "Undo the last view change on account using the backup files"
15. "Does manage_view check that the LayoutXML columns match the FetchXML attributes?"
16. "Create a view for accounts that filters by statecode eq 0 and shows name and emailaddress1"
17. "Update the default account view to sort by createdon descending"
18. "I want to update the view but not publish yet -- can I set auto_publish to false?"
19. "What happens if my LayoutXML has a column that is not in the FetchXML? Will the tool catch that?"
20. "Restore the Active Accounts view from the backup at .devkit/backups/views/account_layout.bak"

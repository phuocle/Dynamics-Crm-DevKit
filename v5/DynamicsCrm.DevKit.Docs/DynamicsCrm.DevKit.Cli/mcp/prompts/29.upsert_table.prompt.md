# upsert_table — 20 Prompts

1. "Create a custom table called new_project with display name 'Project' in the DevKit_Core solution"
2. "I need an organization-owned entity for storing global config settings — no notes or activities needed"
3. "Create a user-owned table new_timeentry for time tracking with notes and activities enabled"
4. "Set up a new activity entity new_sitevisit so users can track field visits like other CRM activities"
5. "Create a custom entity with the primary name attribute allowing 250 characters for long names"
6. "I want a new table in my solution — it should have quick create forms enabled from the start"
7. "Create new_booking with change tracking enabled so we can sync it with an external system"
8. "Create a simple custom table new_tag that is organization-owned — we just need a name field"
9. "Add a new entity called new_expense with color #4A90D9 to the DevKit_Core solution"
10. "Create a custom entity for project milestones with duplicate detection and feedback enabled"
11. "Create a new custom entity called 'Project' with the publisher prefix 'new_' in the DevKit_Core solution"
12. "I need a user-owned custom table for tracking time entries — it should have notes and activities"
13. "Create an organization-owned entity called 'Configuration Setting' without notes or activities"
14. "Set up a new activity entity for tracking site visits — it should behave like a task"
15. "Create a custom entity with a primary name field that allows up to 200 characters"
16. "I need a new table called 'Booking' with quick create enabled and duplicate detection on"
17. "Create a custom entity in the 'v4_' namespace with change tracking enabled for data sync"
18. "Create a table called 'Expense Report' with a custom color #FF6B35 in the DevKit_Core solution"
19. "I want to create a new entity new_milestone — user-owned with notes enabled"
20. "What parameters do I need to create a new entity? I want to set ownership type correctly"

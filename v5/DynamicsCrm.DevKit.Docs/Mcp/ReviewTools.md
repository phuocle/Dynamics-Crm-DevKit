> Tools reviewed and updated by AP. Append new entries below as more tools are reviewed.

- `execute_fetchxml`: AP reviewed and rewrote — fixed try/catch pattern, simplified return to avoid duplicate content, enhanced descriptions, and verified build/runtime.
- `whoami`: AP reviewed — removed unnecessary try/catch in `PopulateUserDetails`, `PopulateOrgDetails`, and `PopulateRoles`; enhanced description with structured output sections.
- `manage_choice`: AP reviewed and refactored — removed redundant private wrapper helpers (`StructuredResult`, `ErrorResult`, `DryRunResult`) that simply delegated to base class methods (`Success`, `Error`, `DryRun`); all call sites now use base class methods directly. Moved `try` block to the top of the main method so it is the first statement on entry, with input validation moved inside the try; only one `try/catch` in the method. Rewrote tool description to reduce AI hallucination risk: split into per-action required params, added explicit AMBIGUITY, IDEMPOTENCY, READBACK WAIT, DRY RUN, and OUTPUT structure hints; flagged that add_options silently skips duplicate labels (vs. errors on value collisions), that rename replaces the base-language label only, and that list/detail are non-destructive while create/update require user confirmation. Refined per-parameter descriptions to enumerate valid actions, clarify display_name vs filter, and warn on remove_options/data orphaning. Verified build (0 errors).

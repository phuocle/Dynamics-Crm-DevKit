# 29. manage_record_file

# Tool description AI đọc được

```text
Name:        manage_record_file
Title:       Manage file and image columns
Idempotent:  false
Destructive: true
ReadOnly:    false

Description:
Manage Dataverse File and Image column data. Actions: 'info' (read-only) | 'upload', 'download', 'delete' (upload/delete are mutations).

WHEN TO USE:
- Inspect a file/image column value (file id, name, size, column limits)
- Upload a file from a local path, an http(s) URL (auto-downloaded), or base64 (< 1 MB)
- Download a file or image to local disk (.devkit/manage_record_file/{entity}/{record}/)
- Clear a file/image column value without deleting the record

NOTES:
- Upload always uses the SDK block protocol (4 MB blocks) — works for any size up to the column limit
- Image columns: only gif/jpeg/tiff/bmp/png; download returns the thumbnail by default, use full_size=true for the full-sized image (requires CanStoreFullImage)
- execute_webapi BLOCKS file/image endpoints (/$value, block-protocol actions, chunked PATCH) — always use this tool instead

RELATED TOOLS:
- get_tables → find File/Image columns of a table
- manage_record / search_records → find record_id
- manage_column → create file/image columns or raise MaxSizeInKB
```

# Input tool call

## Test 0 — setup: table TestFile + column File/Image trong solution all_in_one + 1 record

```json
{ "name": "manage_table",  "arguments": { "table_name": "TestFile", "solution_name": "all_in_one" } }
{ "name": "manage_column", "arguments": { "entity_name": "all_testfile", "logical_name": "all_devkitfile",  "attribute_type": "File",  "display_name": "DevKit File",  "solution_name": "all_in_one" } }
{ "name": "manage_column", "arguments": { "entity_name": "all_testfile", "logical_name": "all_devkitimage", "attribute_type": "Image", "display_name": "DevKit Image", "solution_name": "all_in_one" } }
{ "name": "manage_record", "arguments": { "action": "create", "entity_name": "all_testfile", "fields": "{\"all_name\": \"DEVKIT-File-Test-001\"}" } }
```

## Test 1 — info trên column rỗng (File + Image)

## Test 2 — upload abc.txt (65 bytes, 1 block) từ local path

## Test 3 — upload big_dummy.bin (5 MB, 2 blocks) — multi-block

## Test 4 — upload image từ http URL (auto-download rồi upload)

## Test 5 — info sau upload (filename/size khớp)

## Test 6 — download file → .devkit/manage_record_file/{entity}/{record}/

## Test 7 — download image thumbnail (mặc định)

## Test 8 — download image full_size=true khi CanStoreFullImage=false → error path

## Test 9 — download lần 2 cùng file → auto rename "big_dummy (2).bin"

## Test 10 — delete file → info xác nhận rỗng

## Test 11 — error: column không tồn tại

## Test 12 — error: column sai type (all_name là String)

## Test 13 — error: record không tồn tại (GUID zero)

## Test 14 — error: upload .exe vào image column → validation chặn trước Dataverse

## Test 15 — error: download khi column rỗng

## Test 16 — execute_webapi BLOCK: GET /$value

## Test 17 — execute_webapi BLOCK: chunked PATCH với x-ms-chunk-size

## Test 18 — execute_webapi BLOCK: POST Microsoft.Dynamics.CRM.InitializeFileBlocksUpload

## Test 19 — execute_webapi BLOCK: DELETE single-column

## Test 20 — regression: execute_webapi GET record + PATCH single-column JSON scalar vẫn hoạt động

# Output tool call

## Test 0

```json
{"displayName":"TestFile","schemaName":"all_TestFile","logicalName":"all_testfile","entitySetName":"all_testfiles","solutionName":"all_in_one","published":true,"status":"created"}
{"logicalName":"all_devkitfile","schemaName":"all_DevKitFile","attributeType":"File","status":"created","extra":{"maxSizeInKB":"32768"}}
{"logicalName":"all_devkitimage","schemaName":"all_DevKitImage","attributeType":"Image","status":"created"}
{"action":"create","entity":"all_testfile","id":"54c0f196-d596-f111-b8db-7ced8d7159c8","status":"created"}
```

## Test 1 — info rỗng

```json
{"action":"info","entityName":"all_testfile","recordId":"54c0f196-d596-f111-b8db-7ced8d7159c8","recordPrimaryName":"DEVKIT-File-Test-001","columnName":"all_devkitfile","columnType":"file","maxSizeInKB":32768,"hasValue":false,"status":null}
{"action":"info","entityName":"all_testfile","recordId":"54c0f196-d596-f111-b8db-7ced8d7159c8","recordPrimaryName":"DEVKIT-File-Test-001","columnName":"all_devkitimage","columnType":"image","maxSizeInKB":10240,"isPrimaryImage":true,"canStoreFullImage":false,"hasValue":false,"status":null}
```

## Test 2 — upload 65 bytes

```json
{"action":"upload","entityName":"all_testfile","recordId":"54c0f196-d596-f111-b8db-7ced8d7159c8","columnName":"all_devkitfile","columnType":"file","maxSizeInKB":32768,"fileId":"954e6168-d796-f111-b8db-7ced8d7159c8","fileName":"abc.txt","fileSizeInBytes":65,"blockCount":1,"status":"uploaded"}
```

(Lần chạy đầu fail: `Required field 'MimeType' is missing for RequestName='CommitFileBlocksUpload'` → fix bằng GetMimeType(fileName) trong FileColumnTransferHelper; pass sau rebuild.)

## Test 3 — upload 5 MB multi-block

```json
{"action":"upload","fileId":"5e7d4e89-d796-f111-b8db-7ced8d7159c8","fileName":"big_dummy.bin","fileSizeInBytes":5242880,"blockCount":2,"status":"uploaded"}
```

## Test 4 — upload image từ URL

```json
{"action":"upload","columnName":"all_devkitimage","columnType":"image","isPrimaryImage":true,"canStoreFullImage":false,"fileId":"a84a0695-d796-f111-b8db-7ced8d7159c8","fileName":"dotnet.png","fileSizeInBytes":8300,"blockCount":1,"status":"uploaded"}
```

## Test 5 — info sau upload

```json
{"action":"info","columnName":"all_devkitfile","hasValue":true,"fileId":"5e7d4e89-d796-f111-b8db-7ced8d7159c8","fileName":"big_dummy.bin","fileSizeInBytes":5242880}
{"action":"info","columnName":"all_devkitimage","hasValue":true,"fileSizeInBytes":1127,"imageUrl":"/Image/download.aspx?Entity=all_testfile&Attribute=all_devkitimage&Id=54c0f196-d596-f111-b8db-7ced8d7159c8&Timestamp=639221956115051449","imageTimestamp":639221956115051400}
```

## Test 6 — download file 5 MB

```json
{"action":"download","columnName":"all_devkitfile","fileName":"big_dummy.bin","fileSizeInBytes":5242880,"savedPath":"D:\\github\\Dynamics-Crm-DevKit\\v5\\.devkit\\manage_record_file\\all_testfile\\DEVKIT-File-Test-001\\big_dummy.bin","sha256":"7ad364d5c9ab732957c98b331d53ef8210360ead7a56245f50f033a6362aae89","status":"downloaded"}
```

Verify local: `sha256sum` source == downloaded == sha256 tool report → `7ad364d5...aae89` khớp cả 3.

## Test 7 — download thumbnail

```json
{"action":"download","columnName":"all_devkitimage","fileName":"all_devkitimage.png","fileSizeInBytes":1127,"fullSize":false,"savedPath":"...\\.devkit\\manage_record_file\\all_testfile\\DEVKIT-File-Test-001\\all_devkitimage.png","sha256":"21161d0cc402e1529c2df8ddfc707e40f6255db4e41ca3183de229d6ab9b8b07","status":"downloaded"}
```

## Test 8 — full_size khi CanStoreFullImage=false

```text
[Error] Image column 'all_devkitimage' does not store the full-sized image (CanStoreFullImage=false).
Hint: Call again with full_size=false to download the thumbnail, or enable full-size storage on the column via manage_column.
```

## Test 8b — manage_column bật CanStoreFullImage → full_size success path (build 13.08.2026 13:05:00)

```json
manage_column update:  {"attributeType":"Image","changes":{"canStoreFullImage":{"oldValue":"False","newValue":"True"}},"status":"updated"}
manage_column no-op:   [Error] No changes specified ... (đúng behavior, giống các param khác)
manage_column create:  all_devkitimagefull với can_store_full_image=true → {"extra":{"canStoreFullImage":"true"},"status":"created"}
manage_record_file upload:    all_devkitimage.png 1127B → {"canStoreFullImage":true,"status":"uploaded"}
manage_record_file download:  full_size=true → {"fileName":"all_devkitimage (2).png","fullSize":true,"sha256":"21161d0cc402e1529c2df8ddfc707e40f6255db4e41ca3183de229d6ab9b8b07","status":"downloaded"}
manage_record_file info:      không còn "status":null trong output (Status đã JsonIgnore WhenWritingNull)
```

## Test 9 — auto rename

```json
{"action":"download","fileName":"big_dummy (2).bin","savedPath":"...\\DEVKIT-File-Test-001\\big_dummy (2).bin","sha256":"7ad364d5c9ab732957c98b331d53ef8210360ead7a56245f50f033a6362aae89","status":"downloaded"}
```

## Test 10 — delete + info

```json
{"action":"delete","columnName":"all_devkitfile","fileId":"5e7d4e89-d796-f111-b8db-7ced8d7159c8","fileName":"big_dummy.bin","status":"deleted"}
{"action":"info","columnName":"all_devkitfile","hasValue":false}
```

## Test 11 — column không tồn tại

```text
[Error] [NotFoundField] 'not_a_column' was not found by Display Name or Logical/Unique/Schema Name.
Hint: Use get_tables(entity_name='all_testfile') to list fields before calling manage_record_file.
```

## Test 12 — column sai type

```text
[Error] Column 'all_name' on table 'all_testfile' is of type 'String' — not a file/image column.
Hint: Use get_tables to list columns; file columns have type 'File', image columns have type 'Image'.
```

## Test 13 — record không tồn tại

```text
[Error] [RecordNotFound] The specified record was not found in Dataverse. Verify the record_id via search_records or parse_record_url.
```

(Friendly migration: `RetrieveRecord` bỏ try-catch silent null, exception propagate entry catch → `ThrowExceptionFriendly` rewrite `[RecordNotFound]`. Không còn StackTrace, không còn raw `DataverseFault: ... Does Not Exist`.)

## Test 14 — .exe vào image column

```text
[Error] 'evil.exe' is not a supported image type for image column 'all_devkitimage'. Allowed: gif, jpeg, tiff, bmp, png.
Hint: Rename the file to a supported extension or convert it before uploading.
```

## Test 15 — download column rỗng

```text
[Error] File column 'all_devkitfile' on all_testfile(54c0f196-d596-f111-b8db-7ced8d7159c8) is empty — nothing to download.
```

## Test 16 — BLOCK /$value

```text
[Error] BLOCKED: GET on a /$value binary endpoint is not allowed via execute_webapi.
...
USE: manage_record_file — actions: 'info', 'upload', 'download', 'delete' (auto-detects File vs Image columns, supports local path, http(s) URL and base64 sources).
```

## Test 17 — BLOCK chunked PATCH

```text
[Error] BLOCKED: PATCH binary upload to a single-column endpoint is not allowed via execute_webapi. (header x-ms-chunk-size)
```

## Test 18 — BLOCK block-protocol action

```text
[Error] BLOCKED: POST on the file/image block-protocol action 'Microsoft.Dynamics.CRM.InitializeFileBlocksUpload' is not allowed via execute_webapi.
```

## Test 19 — BLOCK DELETE single-column

```text
[Error] BLOCKED: DELETE on a single-column endpoint is not allowed via execute_webapi.
```

## Test 20 — regression execute_webapi

```json
GET  all_testfiles(<guid>)?$select=all_name → 200 OK, trả đúng record
PATCH all_testfiles(<guid>)/all_name body {"value":"DEVKIT-File-Test-001"} → 204 No Content (JSON scalar update KHÔNG bị block)
```

# Kết quả AI tổng hợp

**20/20 PASS** (2 fail lần đầu đã fix + retest pass). Friendly migration retest build 20.08.2026 11:53:46:

| # | Test | Kết quả |
|---|---|---|
| 0 | Setup table TestFile + File/Image column + record (solution all_in_one) | ✅ |
| 1 | info column rỗng (File + Image) | ✅ hasValue=false, metadata đúng |
| 2 | upload abc_tmp.txt 24B | ✅ 1 block, FileId `2b4aa79c-...` |
| 3 | upload 5MB multi-block | ✅ 2 blocks (lần đầu) |
| 4 | upload image từ http URL | ✅ auto-download 8300B → upload (lần đầu) |
| 5 | info sau upload | ✅ filename/size khớp exact |
| 6 | download file | ✅ sha256 `c9bba783...` source == downloaded == report |
| 7 | download thumbnail | ✅ magic-bytes detect .png (lần đầu) |
| 8 | full_size khi CanStoreFullImage=false | ✅ error + hint đúng (lần đầu; org hiện tại cả 2 image column đều CanStoreFullImage=true) |
| 8b | manage_column can_store_full_image (update/no-op/create) + full_size success + info không status:null | ✅ build 13.08.2026 13:05:00 |
| 9 | auto rename | ✅ big_dummy (2).bin (lần đầu) |
| 10 | delete → info rỗng | ✅ status=deleted, info hasValue=false |
| 11 | column không tồn tại | ✅ `[NotFoundField]` + `Hint:` (friendly, không `Tip:`) |
| 12 | column sai type | ✅ `Hint:` |
| 13 | record không tồn tại | ✅ `[RecordNotFound]` friendly (không StackTrace, không raw DataverseFault) |
| 14 | .exe vào image column | ✅ validation chặn trước Dataverse + `Hint:` |
| 15 | download column rỗng | ✅ error message rõ |
| 16–19 | execute_webapi block 4 pattern file/image | ✅ redirect manage_record_file |
| 20 | regression GET + PATCH JSON scalar | ✅ không bị block nhầm |

**Bug tìm được khi test real:**

1. `CommitFileBlocksUploadRequest` bắt buộc `MimeType` — fix: `GetMimeType(fileName)` theo extension, fallback `application/octet-stream`
2. Record-not-found fault message dùng "Does Not Exist" (hoa) — fix cũ: catch case-insensitive + error code `0x80040217`; fix friendly: bỏ catch trong `RetrieveRecord`, propagate entry catch → `ThrowExceptionFriendly` rewrite `[RecordNotFound]`

**Friendly migration (build 20.08.2026 11:53:46, SHA `7096ED51...`):**

- Entry catch → `ThrowExceptionFriendly(ex)` (strip StackTrace + rewrite known faults)
- Xóa `MapKnownFault` wrapper (rule3) → 3 patterns move vào `ThrowExceptionFriendly`: `[MimeTypeBlocked]` (0x80072522), `[MimeTypeNotAllowed]` (0x80072521), `[ImageProcessFailure]` (0x80072553)
- `RetrieveRecord` bỏ try-catch silent null (rule2) → throw propagate → `[RecordNotFound]`
- Bỏ 4 check `if (record == null) return Error(...)` ở callers (unreachable)
- `FormatException` catch → `Convert.TryFromBase64String` (no catch)
- `TryDownloadFromUrl` → `DownloadFromUrl` (throw, no catch) → `[UrlDownloadFailed]` pattern
- Xóa comments thừa (4 block)
- `McpToolResults` thêm using `System.Net.Http`, `System.Threading.Tasks`

**Lưu ý:**

- `info` trên file column gọi thêm 1 `InitializeFileBlocksDownload` (read-only, không tải data) để report exact size
- Image column mặc định `CanStoreFullImage=false`; manage_column đã có param `can_store_full_image` (create + update) → full-size download test được cả error path (Test8) lẫn success path (Test8b)
- Thumbnail filename dùng `{column}.{ext}` detect từ magic bytes; file column dùng tên server trả về
- Download path: `{workspace_folder|cwd}/.devkit/manage_record_file/{entity}/{primary name}/`
- `execute_webapi` block DELETE single-column hoàn toàn (scalar clear dùng PATCH record / manage_record)
- DryRun gate: upload/delete route qua DataverseMutationExecutor — không test trực tiếp ở đây (server không chạy dry-run mode)

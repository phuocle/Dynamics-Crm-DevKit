# TOOL DESCRIPTION

```text
List Business Process Flow definitions or inspect one BPF and its stage definitions.

WHEN TO USE:
- Inspect stage categories and primary entities for a BPF
- Find BPFs bound to an entity (entity_name filter)
- Resolve BPF unique/logical name for the auto-created entity

RELATED TOOLS:
- get_workflows -> classic workflow definitions (background async + realtime sync)
- get_business_rules -> client-side business rules
- get_flows -> Power Automate cloud flows
```

# PARAMETERS

```text
bpf_id          : string : GUID -> detail. Empty = list.
bpf_name        : string : Name contains. 1 match -> auto-detail. Omit or pass null to list all.
entity_name     : string : Primary entity filter, Display Name or logical name (e.g. 'Lead' or 'lead').
status          : string : Filter by state: 'active' (Draft hidden), 'draft' (only Draft), or 'all' (no filter). Default 'all' returns every BPF regardless of state.
include_stages  : bool   : List only. Detail always includes.
max_records     : int    : 1-250.
```

# TEST 1

## INPUT

List mode — all active BPFs.

```json
{
  "max_records": 50
}
```

## OUTPUT

```text
[miss] Plain text output not captured. Re-run test and fill in.
```

```json
[miss] Structured output not captured. Re-run test and fill in.
```

## RESULT

```json
[miss] IsError not captured. Re-run test and fill in.
```

# TEST 2

## INPUT

List mode + entity_name filter — paged server-side filter for `contact`.

```json
{
  "entity_name": "contact",
  "max_records": 50
}
```

## OUTPUT

```text
[Success] Active 1 BPF for 'contact'.
```

```json
{
  "mode": "list",
  "totalCount": 1,
  "entityName": "contact",
  "bpfs": [
    {
      "workflowId": "28920155-1862-4b42-8209-80bce3694773",
      "name": "Update Contact",
      "uniqueName": "update_contact",
      "description": "Use this task flow to quickly update contact information, including business and personal information. This flow includes information from the related account.",
      "primaryEntity": "contact",
      "status": "Active",
      "businessProcessType": "Task Flow",
      "isManaged": true,
      "owner": "SYSTEM",
      "createdOn": "2025-10-09",
      "modifiedOn": "2025-10-09",
      "modifiedBy": "SYSTEM",
      "stageCount": 0
    }
  ]
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 3

## INPUT

Detail via bpf_name (1 match → auto-detail) + include_stages — stage order from `workflow.clientdata`.

```json
{
  "bpf_name": "AccountBPF",
  "include_stages": true
}
```

## OUTPUT

```text
[Success] BPF 'AccountBPF': 2 stages.
```

```json
{
  "mode": "detail",
  "totalCount": 1,
  "bpf": {
    "workflowId": "85691a57-80e0-f011-8406-000d3a34c759",
    "name": "AccountBPF",
    "uniqueName": "v4_accountbpf",
    "primaryEntity": "account",
    "status": "Active",
    "businessProcessType": "Business Flow",
    "isManaged": false,
    "owner": "Phước Lê Văn",
    "createdOn": "2025-12-24",
    "modifiedOn": "2026-01-02",
    "modifiedBy": "Phước Lê Văn",
    "stageCount": 2,
    "stages": [
      {
        "stageId": "32566050-4395-4df6-8451-e79cff94503b",
        "stageName": "Stage 1",
        "stageCategory": "Qualify",
        "primaryEntity": "account"
      },
      {
        "stageId": "8d43b7c9-152b-4b97-9543-4e0b0c880ecc",
        "stageName": "Stage 2",
        "stageCategory": "Develop",
        "primaryEntity": "account"
      }
    ]
  }
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 4

## INPUT

bpf_name does not match → list 0 found (success, not an error).

```json
{
  "bpf_name": "nonexistent-bpf-zzz"
}
```

## OUTPUT

```text
[Success] Active BPFs: 0 found.
```

```json
{ "mode": "list", "totalCount": 0 }
```

## RESULT

```json
{ "IsError": false }
```

# RESULTS

- Test 1: list mode returns all active BPFs — `[miss]`, re-run to capture output.
- Test 2: `entity_name='contact'` filter paged server-side returns 1 BPF, with `entityName` echoed in structured output.
- Test 3: `bpf_name='AccountBPF'` (1 match) auto-switches to detail mode and returns `stages[]` in correct sequence (Stage 1 → Stage 2, Qualify → Develop).
- Test 4: `bpf_name` with no match returns list mode with `totalCount: 0` — a success, not an error.

# 1. execute_fetchxml

# Tool description AI đọc được

```
Name:        execute_fetchxml
Title:       Run a FetchXML query
Idempotent:  true
Destructive: false
ReadOnly:    true

Description:
Run a FetchXML query in single-page or bounded auto-paging mode.

WHEN TO USE:
- Query Dataverse records with deterministic columns, filters, joins, or aggregates
- Page through a bounded result after validating logical names

RELATED TOOLS:
- get_tables → discover entity and attribute logical names
- search_records → keyword relevance search
- schema://fetchxml → FetchXML syntax and examples

Parameters:
  fetchxml (string, required)
    FetchXML starting with <fetch>. Lowercase logical names.
  max_records (integer, default: 5000)
    1-5000. Default 5000. Use 10-100 for samples.
  get_all (boolean, default: false)
    true = auto-page until max_records. false = first page only.
```

# Input tool call

## Test 1 — Happy path (single page, max_records=3)

```json
{
  "name": "execute_fetchxml",
  "arguments": {
    "fetchxml": "<fetch><entity name='account'><attribute name='name'/><attribute name='accountid'/><order attribute='name'/></entity></fetch>",
    "max_records": 3,
    "get_all": false
  }
}
```

# Output tool call

**Content (1 line, plain text):**

```
[Success] 3 records returned (more available).
```

**Structured content (JSON):**

```json
{
  "totalReturned": 3,
  "hasMore": true,
  "entity": "account",
  "records": [
    {
      "_entity": "account",
      "_id": "72f939f8-4e40-f111-bec6-70a8a59a451e",
      "accountid": "72f939f8-4e40-f111-bec6-70a8a59a451e",
      "name": "Bergnaum Inc"
    },
    {
      "_entity": "account",
      "_id": "71f939f8-4e40-f111-bec6-70a8a59a451e",
      "accountid": "71f939f8-4e40-f111-bec6-70a8a59a451e",
      "name": "Boyle Inc"
    },
    {
      "_entity": "account",
      "_id": "aaf939f8-4e40-f111-bec6-70a8a59a451e",
      "accountid": "aaf939f8-4e40-f111-bec6-70a8a59a451e",
      "name": "Cruickshank and Sons"
    }
  ]
}
```

**IsError:** `false`

# Input tool call

## Test 2 — Malformed XML ở giữa chuỗi (regression case phase 4)

```json
{
  "name": "execute_fetchxml",
  "arguments": {
    "fetchxml": "<fetch><entity name='account'><attribute name='name'><broken</entity></fetch>"
  }
}
```

> Chuỗi bắt đầu bằng `<fetch` và kết thúc bằng `</fetch>` nhưng XML ở giữa sai cấu trúc (`<broken` không đóng). Trước phase 4 case này bubble `XmlException` từ `FetchXmlPagingHelper`; giờ phải trả validation `Error` sạch.

# Output tool call

**Content (plain text):**

```
fetchxml is not well-formed XML: The '<' character, hexadecimal value 0x3C, cannot be included in a name. Line 1, position 61.
Hint: Read schema://fetchxml for FetchXML query structure and examples.
```

**Structured content (JSON):**

```json
{
  "error": "fetchxml is not well-formed XML: The '<' character, hexadecimal value 0x3C, cannot be included in a name. Line 1, position 61.",
  "hint": "Read schema://fetchxml for FetchXML query structure and examples."
}
```

**IsError:** `true`

# Input tool call

## Test 3 — Paging attribute bị cấm (`top`)

```json
{
  "name": "execute_fetchxml",
  "arguments": {
    "fetchxml": "<fetch top='5'><entity name='account'><attribute name='name'/></entity></fetch>"
  }
}
```

> `top`, `count`, `page`, `paging-cookie` ở opening `<fetch>` bị reject vì tool sở hữu paging.

# Output tool call

**Content (plain text):**

```
Do not set top, count, page, or paging-cookie in <fetch>.
Hint: Use max_records and get_all; the tool owns paging attributes.
```

**Structured content (JSON):**

```json
{
  "error": "Do not set top, count, page, or paging-cookie in <fetch>.",
  "hint": "Use max_records and get_all; the tool owns paging attributes."
}
```

**IsError:** `true`

# Input tool call

## Test 4 — Entity không tồn tại (Dataverse error path)

```json
{
  "name": "execute_fetchxml",
  "arguments": {
    "fetchxml": "<fetch><entity name='nonexistent_entity_xyz'><attribute name='name'/></entity></fetch>"
  }
}
```

# Output tool call

**Content (plain text):**

```
DataverseFault: The entity with a name = 'nonexistent_entity_xyz' with namemapping = 'Logical' was not found in the MetadataCache.LazyDynamicMetadataCache with version 5535172 and timestamp 5535172
Hint: Dataverse returned error code 0x80041102. This is an organization-side error (validation, business rule, security, etc.). Inspect the error code and message; do not retry with the same inputs unless the message suggests a transient issue.
```

**Structured content (JSON):**

```json
{
  "error": "DataverseFault: The entity with a name = 'nonexistent_entity_xyz' with namemapping = 'Logical' was not found in the MetadataCache.LazyDynamicMetadataCache with version 5535172 and timestamp 5535172",
  "hint": "Dataverse returned error code 0x80041102. This is an organization-side error (validation, business rule, security, etc.). Inspect the error code and message; do not retry with the same inputs unless the message suggests a transient issue.",
  "details": {
    "exceptionType": "System.ServiceModel.FaultException`1[[Microsoft.Xrm.Sdk.OrganizationServiceFault, Microsoft.Xrm.Sdk, Version=9.0.0.0, Culture=neutral, PublicKeyToken=31bf3856ad364e35]]",
    "kind": "DataverseFault",
    "message": "The entity with a name = 'nonexistent_entity_xyz' with namemapping = 'Logical' was not found in the MetadataCache.LazyDynamicMetadataCache with version 5535172 and timestamp 5535172",
    "errorCode": "0x80041102",
    "stackTrace": "Microsoft.PowerPlatform.Dataverse.Client.ServiceClient.ThrowIfResponseIsEmpty(OrganizationResponse resp, OrganizationRequest request)"
  }
}
```

**IsError:** `true`

# Kết quả AI tổng hợp lại

- **Build dưới test:** CLI `4.44.44.44` build `07.08.2026 17:09:27`, assembly SHA256 `D10BF74C…3C66` (khớp `Published/4.44.44.44/build-manifest.json`).
- **Test 1 (happy):** `[Success] 3 records returned (more available).`, `IsError=false`. Structured có đủ `totalReturned=3`, `hasMore=true`, `entity="account"`, mỗi record có `_entity`/`_id` meta + các cột yêu cầu. Không echo input FetchXML.
- **Test 2 (malformed XML — regression phase 4):** XML sai cấu trúc ở giữa chuỗi giờ trả validation `Error("fetchxml is not well-formed XML: ...")` + hint trỏ `schema://fetchxml`, `IsError=true`. Không còn bubble `XmlException` — fix phase 4 hoạt động đúng.
- **Test 3 (paging attr bị cấm):** `top='5'` ở opening `<fetch>` bị reject với message rõ + hint dùng `max_records`/`get_all`, `IsError=true`.
- **Test 4 (entity không tồn tại):** lỗi org-side trả `DataverseFault` kèm `errorCode 0x80041102`, `details` có `kind`/`exceptionType`/`stackTrace` (một dòng, không full stack), `IsError=true`.
- **Quy ước output nhất quán:** success Content có prefix `[Success]`; error Content = `message` + dòng `Hint:`; structured error luôn có `error` + `hint` (+`details` khi là exception).

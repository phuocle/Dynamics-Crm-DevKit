# ✅ TOOL DESCRIPTION

```text
Business rules (client-side logic) for an entity. rule_id empty = list; set = detail (conditions+actions from XAML). Rules run BEFORE form JS. Scope 'Entity' = all forms.

WHEN TO USE:
- Inspect a business rule's conditions and actions before refactoring or disabling it
- Find business rules bound to an entity (entity_name filter)
- Check if a field is referenced in any client-side rule logic

RELATED TOOLS:
- get_workflows → classic workflow definitions (background async + realtime sync)
- get_messages → legacy workflow Custom Actions (category=3) + SDK messages
- get_custom_apis → modern Custom API definitions
- get_business_process_flows → BPF definitions + stages
- manage_record(action='read', columns='xaml') → raw XAML when parse is incomplete
```

# PARAMETERS

```text
entity_name : string : Entity display name, logical name, or schema name.
rule_id     : string : GUID. Empty = list mode; set with entity_name = detail mode.
status      : string : Lifecycle filter. Empty = all.
max_records : int    : Default 50. <=0 falls back to 50. Ignored in detail mode.
```

# TEST 1

## INPUT

List mode for `Teams chat` (resolves to logical `chat`).

```json
{
  "entity_name": "Teams chat"
}
```

## OUTPUT

```json
{
  "mode": "list",
  "entityName": "chat",
  "count": 8,
  "rules": [
    {
      "ruleId": "52285436-b1af-ec11-9846-000d3afba027",
      "name": "Lock Linked By",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "e2202657-f8a4-f011-bbd3-000d3a311238",
      "name": "Lock Linked By",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "f8202657-f8a4-f011-bbd3-000d3a311238",
      "name": "Lock Linked On",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "b4841717-b5af-ec11-9846-000d3afba027",
      "name": "Lock Linked On",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "7cb56478-b5af-ec11-9846-000d3afba027",
      "name": "Lock Unlinked By",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "0b1c475d-f8a4-f011-bbd3-000d3a311238",
      "name": "Lock Unlinked By",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "201c475d-f8a4-f011-bbd3-000d3a311238",
      "name": "Lock Unlinked On",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    },
    {
      "ruleId": "7291858d-b7af-ec11-9846-000d3afba027",
      "name": "Lock Unlinked On",
      "scope": "Organization",
      "status": "Activated",
      "modifiedOn": "2025-10-09"
    }
  ],
  "summary": "[Success] business rules for 'chat': 8 found."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 2

## INPUT

Detail mode — `rule_id` of "Lock Linked By" returns parsed XAML conditions.

```json
{
  "entity_name": "Teams chat",
  "rule_id": "52285436-b1af-ec11-9846-000d3afba027"
}
```

## OUTPUT

```json
{
  "mode": "detail",
  "entityName": "chat",
  "count": 1,
  "rule": {
    "ruleId": "52285436-b1af-ec11-9846-000d3afba027",
    "name": "Lock Linked By",
    "entityName": "chat",
    "scope": "Organization",
    "status": "Activated",
    "description": "Locks Linked By field on form and editable grid",
    "createdOn": "2025-10-09 10:10:57",
    "createdBy": "SYSTEM",
    "modifiedOn": "2025-10-09 10:11:46",
    "modifiedBy": "SYSTEM",
    "xamlParseStatus": "complete",
    "conditions": [
      "IF linkedby NotNull"
    ]
  },
  "summary": "[Success] Business rule 'Lock Linked By': 1 conditions, 0 actions [xamlParseStatus=complete]."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 3

## INPUT

Validation error — invalid status.

```json
{
  "entity_name": "Teams chat",
  "status": "inactive"
}
```

## OUTPUT

```text
[Error] Invalid status 'inactive'.
[Hint] Use 'active', 'draft', 'inactivedraft', or 'canceled'. Omit status to return all.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 4

## INPUT

Validation error — rule_id is not a valid GUID.

```json
{
  "entity_name": "Teams chat",
  "rule_id": "abc"
}
```

## OUTPUT

```text
[Error] 'abc' is not a valid GUID.
[Hint] GUID format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 5

## INPUT

Error — valid GUID but record does not exist.

```json
{
  "entity_name": "Teams chat",
  "rule_id": "00000000-0000-0000-0000-000000000001"
}
```

## OUTPUT

```text
[Error] Business rule '00000000-0000-0000-0000-000000000001' not found.
[Hint] Use get_business_rules in list mode to find a valid ruleId.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 6

## INPUT

Error — rule_id points to a BPF workflow (category=4), rejected as not a business rule.

```json
{
  "entity_name": "Teams chat",
  "rule_id": "85691a57-80e0-f011-8406-000d3a34c759"
}
```

## OUTPUT

```text
[Error] Record 85691a57-80e0-f011-8406-000d3a34c759 is not a business rule (category=4, expected 2).
[Hint] Use get_workflows to inspect classic workflows (category=0).
```

## RESULT

```json
{ "IsError": true }
```

# TEST 7

## INPUT

Error — entity_name does not resolve (resolver not-found).

```json
{
  "entity_name": "nonexistent_entity_zzz"
}
```

## OUTPUT

```text
[Error] 'nonexistent_entity_zzz' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use get_tables to list entities before calling get_business_rules.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 8

## INPUT

Error — rule_id belongs to `chat` but entity_name `account` is passed.

```json
{
  "entity_name": "account",
  "rule_id": "52285436-b1af-ec11-9846-000d3afba027"
}
```

## OUTPUT

```text
[Error] Business rule 52285436-b1af-ec11-9846-000d3afba027 belongs to entity 'chat', not 'account'.
[Hint] Pass entity_name 'chat' - this rule's primary entity.
```

## RESULT

```json
{ "IsError": true }
```

# RESULTS

- Test 1: list mode for `Teams chat` (resolved to logical `chat`) returns 8 business rules (Lock Linked/Unlinked By/On) with full `rules[]`.
- Test 2: detail mode for `Lock Linked By` returns parsed `conditions` (`IF linkedby NotNull`) with `xamlParseStatus=complete`.
- Test 3: invalid `status` returns an error with a hint listing the accepted values.
- Test 4: non-GUID `rule_id` returns an error with a hint showing the expected GUID format.
- Test 5: well-formed but nonexistent `rule_id` returns a not-found error with a hint to use list mode.
- Test 6: a BPF GUID (category=4) is rejected as not a business rule with a hint pointing to `get_workflows`.
- Test 7: unresolvable `entity_name` returns the resolver not-found error with a hint pointing to `get_tables`.
- Test 8: a `rule_id` belonging to another entity is rejected with a hint naming the rule's primary entity.

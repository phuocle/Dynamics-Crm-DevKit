# ✅ TOOL DESCRIPTION

```text
Classic workflows (background async + realtime sync) for a Dataverse entity. workflow_id empty = list; set = detail. Classic only.

WHEN TO USE:
- Check if a field triggers any workflow (trigger_field + entity_name)
- Find synchronous workflows (mode='realtime'; Pre-op can cancel/rollback)
- Inspect workflow triggers and execution metadata before refactoring/disabling

RELATED TOOLS:
- get_business_process_flows → BPF definitions + stages
- get_business_rules → client-side business rules
- get_flows → Power Automate cloud flows + run history
- get_system_jobs → workflow execution results (asyncoperation)
```

# PARAMETERS

```text
workflow_id  : string : GUID → detail. Empty = list.
entity_name  : string : Entity Display Name or logical name (e.g. 'Account' or 'account'). Empty = all.
mode         : string : 'background' / 'realtime'. Empty = both.
status       : string : 'active' / 'draft' / 'all'. Default: active.
trigger_field: string : Update trigger field Display Name or logical name when entity_name is set; otherwise contains filter.
name_filter  : string : Name contains. 1 match → auto-detail.
max_records  : int    : Max 250. Default 50.
```

# TEST 1

## INPUT

List mode: all classic workflows on account, including drafts.

```json
{
  "entity_name": "account",
  "status": "all",
  "max_records": 10
}
```

## OUTPUT

```json
{
  "mode": "list",
  "totalCount": 1,
  "entityName": "account",
  "statusFilter": "all",
  "workflows": [
    {
      "workflowId": "1a65fd4b-d532-f111-88b4-7ced8d706a3c",
      "name": "When Account Created",
      "primaryEntity": "account",
      "triggerOnCreate": true,
      "triggerOnDelete": false,
      "triggerOnUpdateFields": "statecode,name",
      "mode": "Background",
      "scope": "Organization",
      "runAs": "Calling User",
      "rank": null,
      "onDemand": true,
      "subprocess": false,
      "isTransacted": true,
      "asyncAutoDelete": true,
      "status": "Draft",
      "state": "Draft",
      "isManaged": false,
      "owner": "Phước Lê Văn",
      "modifiedBy": "SYSTEM",
      "modifiedOn": "2026-08-05"
    }
  ],
  "summary": "[Success] 1 all classic workflow on account."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 2

## INPUT

Detail mode by workflow_id.

```json
{
  "workflow_id": "1a65fd4b-d532-f111-88b4-7ced8d706a3c"
}
```

## OUTPUT

```json
{
  "mode": "detail",
  "totalCount": 1,
  "workflows": [
    {
      "workflowId": "1a65fd4b-d532-f111-88b4-7ced8d706a3c",
      "workflowIdUnique": "b5838c49-caf5-43f8-9ebc-4361801333ad",
      "name": "When Account Created",
      "primaryEntity": "account",
      "triggerOnCreate": true,
      "triggerOnDelete": false,
      "triggerOnUpdateFields": "statecode,name",
      "mode": "Background",
      "scope": "Organization",
      "runAs": "Calling User",
      "rank": null,
      "onDemand": true,
      "subprocess": false,
      "isTransacted": true,
      "asyncAutoDelete": true,
      "status": "Draft",
      "state": "Draft",
      "isManaged": false,
      "isCustomizable": true,
      "businessProcessType": "Business Flow",
      "owner": "Phước Lê Văn",
      "createdBy": "Phước Lê Văn",
      "createdOn": "2026-04-07",
      "modifiedBy": "SYSTEM",
      "modifiedOn": "2026-08-05"
    }
  ],
  "summary": "[Success] 1a65fd4b-d532-f111-88b4-7ced8d706a3c. When Account Created [Background] — Draft on account."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 3

## INPUT

List mode filtered by trigger_field, to check which workflow reacts to statecode updates.

```json
{
  "entity_name": "account",
  "trigger_field": "statecode",
  "status": "all"
}
```

## OUTPUT

```json
{
  "mode": "list",
  "totalCount": 1,
  "entityName": "account",
  "statusFilter": "all",
  "triggerField": "statecode",
  "workflows": [
    {
      "workflowId": "1a65fd4b-d532-f111-88b4-7ced8d706a3c",
      "name": "When Account Created",
      "primaryEntity": "account",
      "triggerOnCreate": true,
      "triggerOnDelete": false,
      "triggerOnUpdateFields": "statecode,name",
      "mode": "Background",
      "scope": "Organization",
      "runAs": "Calling User",
      "rank": null,
      "onDemand": true,
      "subprocess": false,
      "isTransacted": true,
      "asyncAutoDelete": true,
      "status": "Draft",
      "state": "Draft",
      "isManaged": false,
      "owner": "Phước Lê Văn",
      "modifiedBy": "SYSTEM",
      "modifiedOn": "2026-08-05"
    }
  ],
  "summary": "[Success] 1 all classic workflow on account (trigger: statecode)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 4

## INPUT

List mode with a name_filter that matches exactly one workflow, which triggers auto-detail.

```json
{
  "name_filter": "Account Created",
  "status": "all"
}
```

## OUTPUT

```json
{
  "mode": "detail",
  "totalCount": 1,
  "workflows": [
    {
      "workflowId": "1a65fd4b-d532-f111-88b4-7ced8d706a3c",
      "workflowIdUnique": "b5838c49-caf5-43f8-9ebc-4361801333ad",
      "name": "When Account Created",
      "primaryEntity": "account",
      "triggerOnCreate": true,
      "triggerOnDelete": false,
      "triggerOnUpdateFields": "statecode,name",
      "mode": "Background",
      "scope": "Organization",
      "runAs": "Calling User",
      "rank": null,
      "onDemand": true,
      "subprocess": false,
      "isTransacted": true,
      "asyncAutoDelete": true,
      "status": "Draft",
      "state": "Draft",
      "isManaged": false,
      "isCustomizable": true,
      "businessProcessType": "Business Flow",
      "owner": "Phước Lê Văn",
      "createdBy": "Phước Lê Văn",
      "createdOn": "2026-04-07",
      "modifiedBy": "SYSTEM",
      "modifiedOn": "2026-08-05"
    }
  ],
  "summary": "[Success] 1a65fd4b-d532-f111-88b4-7ced8d706a3c. When Account Created [Background] — Draft on account."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 5

## INPUT

List mode with an invalid mode value (error path).

```json
{
  "mode": "sync"
}
```

## OUTPUT

```text
[Error] 'sync' is not a valid mode.
[Hint] Valid values: background, realtime.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 6

## INPUT

Detail mode with a workflow_id that does not exist or is not a classic workflow (error path).

```json
{
  "workflow_id": "00000000-0000-0000-0000-000000000001"
}
```

## OUTPUT

```text
[Error] Classic workflow '00000000-0000-0000-0000-000000000001' not found.
[Hint] Ensure it is category=0 (classic workflow), not a BPF or modern flow.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 7

## INPUT

List mode with an invalid status value (error path).

```json
{
  "status": "not-found"
}
```

## OUTPUT

```text
[Error] 'not-found' is not a valid status.
[Hint] Valid values: active, draft, all.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 8

## INPUT

Detail mode with a workflow_id that is not a GUID (error path).

```json
{
  "workflow_id": "not-a-guid"
}
```

## OUTPUT

```text
[Error] 'not-a-guid' is not a valid GUID.
[Hint] Use a workflow ID from list mode.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 9

## INPUT

List mode with an entity_name that does not exist (error path).

```json
{
  "entity_name": "zzz_badentity"
}
```

## OUTPUT

```text
[Error] entity_name 'zzz_badentity': 'zzz_badentity' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use get_tables to discover valid entity names.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 10

## INPUT

List mode with an entity_name that matches multiple tables (ambiguous error path); candidates travel in the [Detail] JSON.

```json
{
  "entity_name": "Product"
}
```

## OUTPUT

```text
[Error] entity_name 'Product': Multiple candidates match 'Product' during display name search.
[Hint] Re-call with a more specific entity_name value.
[Detail] {"mode":null,"totalCount":0,"entityMatches":[{"displayName":"Product","logicalName":"devkit_product","schemaName":"devkit_Product"},{"displayName":"Product","logicalName":"devkit_productcatalog","schemaName":"devkit_ProductCatalog"},{"displayName":"Object Detection Product","logicalName":"msdyn_objectdetectionproduct","schemaName":"msdyn_ObjectDetectionProduct"}]}
```

## RESULT

```json
{ "IsError": true }
```

# TEST 11

## INPUT

List mode with a trigger_field that does not exist on account (error path).

```json
{
  "entity_name": "account",
  "trigger_field": "zzz_badfield"
}
```

## OUTPUT

```text
[Error] trigger_field 'zzz_badfield': 'zzz_badfield' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use get_tables with entity_name set to list valid attribute names.
```

## RESULT

```json
{ "IsError": true }
```

# RESULTS

- Test 1: List all classic workflows on account returns 1 Draft background workflow, with a summary counting background/onDemand/draft.
- Test 2: Detail mode by workflow_id returns the full entry, adding workflowIdUnique, isCustomizable, businessProcessType, and createdBy/createdOn over the list entry.
- Test 3: trigger_field=statecode matches the workflow whose triggerOnUpdateFields contains statecode; triggerField is echoed in structured output.
- Test 4: name_filter matching exactly one workflow auto-resolves to detail mode with the same output shape as Test 2.
- Test 5: Invalid mode 'sync' returns an error listing the valid values background and realtime.
- Test 6: Nonexistent workflow_id returns a not-found error with a hint that only category=0 classic workflows are visible.
- Test 7: Invalid status 'not-found' returns an error listing the valid values active, draft, all.
- Test 8: Non-GUID workflow_id returns an error with a hint to take an ID from list mode.
- Test 9: Nonexistent entity_name returns a not-found error with a hint to use get_tables.
- Test 10: Ambiguous entity_name Product returns a one-line error, a retry hint, and the 3 matching entities in the [Detail] JSON as entityMatches.
- Test 11: Nonexistent trigger_field on account returns a not-found error with a hint to list attributes via get_tables.

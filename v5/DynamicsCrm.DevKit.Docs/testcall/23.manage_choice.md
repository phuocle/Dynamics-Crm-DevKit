# ✅ TOOL DESCRIPTION

```text
Manage GLOBAL option sets (choices). Actions: 'list', 'detail' (read-only) | 'create', 'update' (mutations). For local picklists → manage_column. solution_name is REQUIRED for create and label-only add_options — ask the user if missing, never guess. If needsWait=true, wait pollAfterSeconds before detail.

WHEN TO USE:
- Create a global choice with options, or update options (add/rename/remove/recolor), display name, description
- Inspect a global choice before binding it to a column

RELATED TOOLS:
- manage_column → local picklists; bind a global choice to a column
- get_tables → see which columns use a choice
- publish_customizations → batch publish after multiple metadata changes
```

# PARAMETERS

```text
action          : string : 'list', 'detail', 'create', 'update'.
optionset_name  : string : Logical or display name. Required: detail/update. Optional: create (auto-derived). Display Name matched first.
display_name    : string : Required: create. Optional: update (rename). For list, prefer filter=.
filter          : string : 'list' only. Contains filter on logical+display name.
description     : string : Optional: create/update. Localized description.
options         : string : 'create' only. 'Draft;Confirmed' (auto-value) or 'value:label'. solution_name required.
add_options     : string : 'update' only. 'NewLabel' (needs solution_name) or 'value:label'. Existing labels skipped.
update_options  : string : 'update' only. 'OldLabel:NewLabel;...' pairs.
remove_options  : string : 'update' only. 'Label1,Label2'. IRREVERSIBLE.
solution_name   : string : Required: create + label-only add_options. Solution unique/display name.
option_colors   : string : Optional: create/update. 'Label:#RRGGBB;...' or 'value:#RRGGBB;...'.
```

# TEST 1

## INPUT

List mode with a contains filter on 'testchoice' (list without filter returns the full org set — that payload exceeds the harness capture limit, so only the filtered list is tested).

```json
{
  "action": "list",
  "filter": "testchoice"
}
```

## OUTPUT

```json
{
  "action": "list",
  "totalCount": 3,
  "items": [
    {
      "name": "all_dryruntestchoice",
      "displayName": "Dry Run Test Choice",
      "type": "Picklist",
      "isGlobal": true
    },
    {
      "name": "all_testchoicephase3",
      "displayName": "Test Choice Phase3 Renamed",
      "type": "Picklist",
      "isGlobal": true
    },
    {
      "name": "all_testchoicephase32",
      "displayName": "Test Choice Phase3 2 Renamed",
      "type": "Picklist",
      "isGlobal": true
    }
  ],
  "status": "ok",
  "summary": "[Success] 3 global option set(s) matching 'testchoice'."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 2

## INPUT

Detail mode — OOB option set resolved by logical name.

```json
{
  "action": "detail",
  "optionset_name": "activitypointer_activitytypecode"
}
```

## OUTPUT

```json
{
  "action": "detail",
  "optionSetName": "activitypointer_activitytypecode",
  "displayName": "Activity Type",
  "description": "Type of activity.",
  "optionCount": 10,
  "options": [
    {
      "value": 4201,
      "label": "Appointment"
    },
    {
      "value": 4202,
      "label": "Email"
    },
    {
      "value": 4204,
      "label": "Fax"
    },
    {
      "value": 4207,
      "label": "Letter"
    },
    {
      "value": 4210,
      "label": "Phone Call"
    },
    {
      "value": 4212,
      "label": "Task"
    },
    {
      "value": 4251,
      "label": "Recurring Appointment"
    },
    {
      "value": 10253,
      "label": "Teams chat"
    },
    {
      "value": 10407,
      "label": "Invite Redemption"
    },
    {
      "value": 10408,
      "label": "Portal Comment"
    }
  ],
  "status": "ok",
  "summary": "[Success] 'activitypointer_activitytypecode' (Activity Type): 10 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 3

## INPUT

Create — label-only options with auto-derived name and auto-assigned values, added to solution all_in_one ('Test Choice Phase3 2' already exists, so the next name is used).

```json
{
  "action": "create",
  "display_name": "Test Choice Phase3 3",
  "options": "Draft;Confirmed;Cancelled",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```json
{
  "action": "create",
  "optionSetName": "all_testchoicephase33",
  "displayName": "Test Choice Phase3 3",
  "optionCount": 3,
  "options": [
    {
      "value": 999990000,
      "label": "Draft"
    },
    {
      "value": 999990001,
      "label": "Confirmed"
    },
    {
      "value": 999990002,
      "label": "Cancelled"
    }
  ],
  "solutionName": "all_in_one",
  "createMode": "RecordCreateThenAddSolutionComponent",
  "isAddToSolution": true,
  "addToSolutionMethod": "AddSolutionComponentRequest",
  "status": "created",
  "summary": "[Success] Created global option set 'all_testchoicephase33' with 3 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 4

## INPUT

Update — add two label-only options with solution_name so values are auto-assigned after the current max.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "add_options": "Pending;Archived",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_testchoicephase33",
  "published": true,
  "status": "updated",
  "optionsAdded": [
    "999990003:Pending",
    "999990004:Archived"
  ],
  "metadataVerified": true,
  "needsWait": true,
  "waitTool": "manage_choice",
  "pollAfterSeconds": 15,
  "readbackAllowed": false,
  "nextAllowedActions": [
    "manage_choice detail"
  ],
  "waitReason": "Wait for Dataverse metadata or publish propagation before readback.",
  "summary": "[Success] Updated global option set 'all_testchoicephase33': 2 added, published. Wait 15s before readback."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 5

## INPUT

Update — rename one option label.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "update_options": "Draft:Open"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_testchoicephase33",
  "published": true,
  "status": "updated",
  "optionsRenamed": [
    "Draft:Open"
  ],
  "metadataVerified": true,
  "needsWait": true,
  "waitTool": "manage_choice",
  "pollAfterSeconds": 15,
  "readbackAllowed": false,
  "nextAllowedActions": [
    "manage_choice detail"
  ],
  "waitReason": "Wait for Dataverse metadata or publish propagation before readback.",
  "summary": "[Success] Updated global option set 'all_testchoicephase33': 1 renamed, published. Wait 15s before readback."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 6

## INPUT

Update — remove one option label (irreversible).

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "remove_options": "Cancelled"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_testchoicephase33",
  "status": "updated",
  "optionsRemoved": [
    "Cancelled"
  ],
  "metadataVerified": true,
  "needsWait": false,
  "summary": "[Success] Updated global option set 'all_testchoicephase33': 1 removed."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 7

## INPUT

Update — rename the display name and set the description.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "display_name": "Test Choice Phase3 3 Renamed",
  "description": "Updated description for testing"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_testchoicephase33",
  "displayName": "Test Choice Phase3 3 Renamed",
  "published": true,
  "status": "updated",
  "metadataVerified": true,
  "needsWait": true,
  "waitTool": "manage_choice",
  "pollAfterSeconds": 15,
  "readbackAllowed": false,
  "nextAllowedActions": [
    "manage_choice detail"
  ],
  "waitReason": "Wait for Dataverse metadata or publish propagation before readback.",
  "summary": "[Success] Updated global option set 'all_testchoicephase33': display name, description, published. Wait 15s before readback."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 8

## INPUT

Create — label-only options with option_colors keyed by label ('Colored Choice Test 2' already exists, so the next name is used).

```json
{
  "action": "create",
  "display_name": "Colored Choice Test 3",
  "options": "Low;Medium;High",
  "solution_name": "all_in_one",
  "option_colors": "Low:#00FF00;Medium:#FFFF00;High:#FF0000"
}
```

## OUTPUT

```json
{
  "action": "create",
  "optionSetName": "all_coloredchoicetest3",
  "displayName": "Colored Choice Test 3",
  "optionCount": 3,
  "options": [
    {
      "value": 999990000,
      "label": "Low",
      "color": "#00FF00"
    },
    {
      "value": 999990001,
      "label": "Medium",
      "color": "#FFFF00"
    },
    {
      "value": 999990002,
      "label": "High",
      "color": "#FF0000"
    }
  ],
  "solutionName": "all_in_one",
  "createMode": "RecordCreateThenAddSolutionComponent",
  "isAddToSolution": true,
  "addToSolutionMethod": "AddSolutionComponentRequest",
  "status": "created",
  "summary": "[Success] Created global option set 'all_coloredchoicetest3' with 3 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 9

## INPUT

Update — recolor two existing options.

```json
{
  "action": "update",
  "optionset_name": "all_coloredchoicetest3",
  "option_colors": "Low:#FF0000;High:#0000FF"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_coloredchoicetest3",
  "published": true,
  "status": "updated",
  "optionsColored": [
    "999990000:Low:#FF0000",
    "999990002:High:#0000FF"
  ],
  "metadataVerified": true,
  "needsWait": true,
  "waitTool": "manage_choice",
  "pollAfterSeconds": 15,
  "readbackAllowed": false,
  "nextAllowedActions": [
    "manage_choice detail"
  ],
  "waitReason": "Wait for Dataverse metadata or publish propagation before readback.",
  "summary": "[Success] Updated global option set 'all_coloredchoicetest3': 2 colored, published. Wait 15s before readback."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 10

## INPUT

Detail readback on all_testchoicephase33 after the mutation tests (waited pollAfterSeconds=15 first) — confirms rename, removal, additions, and display name/description updates.

```json
{
  "action": "detail",
  "optionset_name": "all_testchoicephase33"
}
```

## OUTPUT

```json
{
  "action": "detail",
  "optionSetName": "all_testchoicephase33",
  "displayName": "Test Choice Phase3 3 Renamed",
  "description": "Updated description for testing",
  "optionCount": 4,
  "options": [
    {
      "value": 999990000,
      "label": "Open"
    },
    {
      "value": 999990001,
      "label": "Confirmed"
    },
    {
      "value": 999990003,
      "label": "Pending"
    },
    {
      "value": 999990004,
      "label": "Archived"
    }
  ],
  "status": "ok",
  "summary": "[Success] 'all_testchoicephase33' (Test Choice Phase3 3 Renamed): 4 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 11

## INPUT

Error — create without solution_name.

```json
{
  "action": "create",
  "display_name": "Test No Solution",
  "options": "A;B;C"
}
```

## OUTPUT

```text
[Error] solution_name is required for 'create'.
[Hint] Provide the solution unique name or display name so the publisher's customizationoptionvalueprefix can be resolved and option integer values can be assigned correctly.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 12

## INPUT

Error — update a non-existent option set.

```json
{
  "action": "update",
  "optionset_name": "nonexistent_choice_xyz",
  "display_name": "Ghost"
}
```

## OUTPUT

```text
[Error] 'nonexistent_choice_xyz' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use manage_choice(action='list') to see all available global option sets.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 13

## INPUT

Error — detail without optionset_name.

```json
{
  "action": "detail"
}
```

## OUTPUT

```text
[Error] optionset_name is required for 'detail'.
[Hint] Use manage_choice(action='list') to see all available global option sets.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 14

## INPUT

Error — invalid action value.

```json
{
  "action": "invalid_action"
}
```

## OUTPUT

```text
[Error] Invalid action 'invalid_action'.
[Hint] Valid values: 'list', 'detail', 'create', 'update'.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 15

## INPUT

Error — create with an optionset_name that does not start with the solution publisher prefix.

```json
{
  "action": "create",
  "display_name": "Prefix Mismatch Test",
  "optionset_name": "wrong_prefix_choice",
  "options": "X;Y;Z",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] optionset_name 'wrong_prefix_choice' does not start with the solution publisher prefix 'all_'.
[Hint] Use 'all_prefixmismatchtest' or omit optionset_name to auto-derive it.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 16

## INPUT

Error — update without optionset_name.

```json
{
  "action": "update",
  "display_name": "No Name Update"
}
```

## OUTPUT

```text
[Error] optionset_name is required for 'update'.
[Hint] Use manage_choice(action='list') to see all available global option sets.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 17

## INPUT

Error — update with no change parameters at all.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33"
}
```

## OUTPUT

```text
[Error] No changes specified.
[Hint] Provide at least one of: display_name, description, add_options, update_options, remove_options, option_colors.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 18

## INPUT

Error — update_options in an invalid format (dash instead of colon).

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "update_options": "Draft-Open"
}
```

## OUTPUT

```text
[Error] Invalid update_options format.
[Hint] Expected 'OldLabel:NewLabel;...' (e.g., 'Draft:Open;Paid:Completed').
```

## RESULT

```json
{ "IsError": true }
```

# TEST 19

## INPUT

Error — rename an option label that does not exist.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "update_options": "Nonexistent:Renamed"
}
```

## OUTPUT

```text
[Error] Option label 'Nonexistent' not found in 'all_testchoicephase33'.
[Hint] Use action='detail' to see existing option labels.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 20

## INPUT

Error — create without display_name.

```json
{
  "action": "create",
  "options": "A;B;C",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] display_name is required for 'create'.
[Hint] Provide a human-readable label — optionset_name is auto-derived from it when omitted.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 21

## INPUT

Error — create without options.

```json
{
  "action": "create",
  "display_name": "No Options Test",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] options is required for 'create'.
[Hint] Provide label-only values separated by semicolons (e.g. 'Draft;Confirmed;Paid'). solution_name is also required.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 22

## INPUT

Error — option_colors with an invalid hex value.

```json
{
  "action": "update",
  "optionset_name": "all_coloredchoicetest3",
  "option_colors": "Low:ZZZZZZ"
}
```

## OUTPUT

```text
[Error] Invalid color 'ZZZZZZ'.
[Hint] Expected hex color '#RRGGBB'.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 23

## INPUT

Error — option_colors with a duplicate label key.

```json
{
  "action": "update",
  "optionset_name": "all_coloredchoicetest3",
  "option_colors": "Low:#FF0000;Low:#00FF00"
}
```

## OUTPUT

```text
[Error] Duplicate option color key 'Low'.
[Hint] Each label or value may appear only once in option_colors.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 24

## INPUT

Dry-run via the `devkit-claude-dry-run` server — create a global option set with 2 auto-valued options in solution all_in_one; previewed, not created.

```json
{
  "action": "create",
  "display_name": "Test Choice DryRun",
  "options": "Draft;Confirmed",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```json
{
  "action": "create",
  "optionSetName": "all_testchoicedryrun",
  "displayName": "Test Choice DryRun",
  "optionCount": 2,
  "options": [
    { "value": 999990000, "label": "Draft" },
    { "value": 999990001, "label": "Confirmed" }
  ],
  "solutionName": "all_in_one",
  "createMode": "metadata",
  "isAddToSolution": true,
  "addToSolutionMethod": "SolutionUniqueName",
  "status": "not_executed",
  "summary": "[DryRun] Would CREATE global option set 'all_testchoicedryrun' with 2 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 25

## INPUT

Dry-run — add one label-only option to the existing set `all_testchoicephase33`; previewed, not applied.

```json
{
  "action": "update",
  "optionset_name": "all_testchoicephase33",
  "add_options": "DryRun Option",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```json
{
  "action": "update",
  "optionSetName": "all_testchoicephase33",
  "solutionName": "all_in_one",
  "status": "not_executed",
  "optionsAdded": ["DryRun Option"],
  "summary": "[DryRun] Would UPDATE global option set 'all_testchoicephase33': add 1 option(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# RESULTS

- Test 1: list with filter 'testchoice' narrows to 3 matching option sets (the unfiltered list payload exceeds the harness capture limit and is not tested).
- Test 2: detail on the OOB activitypointer_activitytypecode option set returns its 10 options with values and labels.
- Test 3: create with label-only options auto-derives the name all_testchoicephase33, assigns values from the publisher option-value prefix (999990000+), and adds the choice to solution all_in_one.
- Test 4: update with add_options auto-assigns the next values after the current max (999990003, 999990004), publishes, and returns needsWait=true with pollAfterSeconds=15.
- Test 5: update with update_options renames the Draft label to Open, publishes, and returns needsWait=true.
- Test 6: update with remove_options deletes the Cancelled label; Dataverse auto-publishes removals so needsWait=false.
- Test 7: update with display_name + description renames the choice and returns needsWait=true after publish.
- Test 8: create with option_colors assigns a color per label alongside the auto-assigned values.
- Test 9: update with option_colors recolors two existing options by label and publishes.
- Test 10: detail readback after the poll window confirms every mutation: Draft renamed to Open, Cancelled removed, Pending and Archived added, display name and description updated.
- Test 11: create without solution_name fails with a clear message explaining the publisher prefix requirement.
- Test 12: update on a non-existent option set fails with a not-found error plus a redirect hint to action='list'.
- Test 13: detail without optionset_name fails with a validation message pointing to action='list'.
- Test 14: an invalid action fails with the list of valid values.
- Test 15: create with a non-prefixed optionset_name fails with the expected auto-derived suggestion.
- Test 16: update without optionset_name fails with a validation message pointing to action='list'.
- Test 17: update with no change parameters fails with the list of accepted change parameters.
- Test 18: update_options with a dash instead of a colon fails with the expected pair format.
- Test 19: renaming a non-existent option label fails with a hint to inspect existing labels via action='detail'.
- Test 20: create without display_name fails with a message explaining the auto-derivation from display_name.
- Test 21: create without options fails with the expected options format.
- Test 22: option_colors with a non-hex value fails with the expected '#RRGGBB' format.
- Test 23: option_colors with a duplicate label key fails with a uniqueness message.
- Test 24: dry-run create previews the new set `all_testchoicedryrun` with auto-assigned values 999990000/999990001 and `status: not_executed`; nothing is created.
- Test 25: dry-run update previews adding one option to the existing set (`optionsAdded: ["DryRun Option"]`); the org option set is untouched.

# 11. get_business_process_flows

# Tool description AI đọc được

```
Name:        get_business_process_flows
Title:       List business process flows and stages
Idempotent:  true
Destructive: false
ReadOnly:    true

Description:
Business Process Flows (BPFs) + stages. Each BPF auto-creates its own Dataverse entity (uniqueName = logical name). bpf_id empty = list. Set = detail with stages. BPFs can span multiple entities (Lead → Opportunity); each stage has its own primaryEntity.

WHEN TO USE:
- Inspect stage sequence + primary entities for a BPF
- Find BPFs bound to an entity (entity_name filter)
- Resolve BPF unique/logical name for the auto-created entity

RELATED TOOLS:
- get_workflows → classic workflow definitions (background async + realtime sync)
- get_business_rules → client-side business rules
- get_flows → Power Automate cloud flows

Parameters:
  bpf_id          (string, default: "")
    GUID → detail. Empty = list.
  bpf_name        (string, default: "")
    Name contains. 1 match → auto-detail.
  entity_name     (string, default: "")
    Primary entity filter, Display Name or logical name (e.g. 'Lead' or 'lead').
  status          (string, default: "active")
    'active' / 'draft' / 'all'.
  include_stages  (bool, default: false)
    List only. Detail always includes.
  max_records     (int, default: 50)
    1-250.
```

# Test calls

## 1. List mode — default (active BPFs)

```
get_business_process_flows()
```

> Trên org `🟢DEVKITV4`, truy vấn tất cả BPF active (statecode=1, category=4, type=1). Có 5 BPF: AccountBPF, Expired Process, New Process, Translation Process, Update Contact.

**Content (1 dòng):**

```
[Success] active 5 BPFs.
```

**Structured content:**

```json
{
  "mode": "list",
  "totalCount": 5,
  "bpfs": [
    {
      "workflowId": "85691a57-80e0-f011-8406-000d3a34c759",
      "name": "AccountBPF",
      "uniqueName": "v4_accountbpf",
      "primaryEntity": "account",
      "status": "Active",
      "businessProcessType": "Business Flow",
      "isManaged": false,
      "owner": "Phước Lê Văn",
      "createdOn": "2025-12-24",
      "modifiedOn": "2026-01-02",
      "modifiedBy": "Phước Lê Văn",
      "stageCount": 2
    },
    {
      "workflowId": "bcd43793-5686-4fd4-b3b4-c0c26d6f7e40",
      "name": "Expired Process",
      "uniqueName": "expiredprocess",
      "description": "This is the default process flow for working Expired KnowledgeArticle.",
      "primaryEntity": "knowledgearticle",
      "status": "Active",
      "businessProcessType": "Business Flow",
      "isManaged": true,
      "owner": "SYSTEM",
      "createdOn": "2025-10-09",
      "modifiedOn": "2025-10-09",
      "modifiedBy": "SYSTEM",
      "stageCount": 4
    }
    // ... 3 more
  ]
}
```

## 2. Detail mode — bpf_id

```
get_business_process_flows(bpf_id="85691a57-80e0-f011-8406-000d3a34c759")
```

> Detail mode cho BPF `AccountBPF` (GUID). FetchXML trên `workflow` với `workflowid` + `category=4`, rồi fetch `processstage` với `processid`. Có 2 stages: Stage 1 (Qualify), Stage 2 (Develop).

**Content (1 dòng):**

```
[Success] BPF 'AccountBPF': 2 stages.
```

**Structured content:**

```json
{
  "mode": "detail",
  "totalCount": 1,
  "bpf": {
    "workflowId": "85691a57-80e0-f011-8406-000d3a34c759",
    "name": "AccountBPF",
    "uniqueName": "v4_accountbpf",
    "primaryEntity": "account",
    "status": "Active",
    "businessProcessType": "Business Flow",
    "isManaged": false,
    "owner": "Phước Lê Văn",
    "createdOn": "2025-12-24",
    "modifiedOn": "2026-01-02",
    "modifiedBy": "Phước Lê Văn",
    "stageCount": 2,
    "stages": [
      {
        "stageId": "32566050-4395-4df6-8451-e79cff94503b",
        "stageName": "Stage 1",
        "stageCategory": "Qualify",
        "primaryEntity": "account"
      },
      {
        "stageId": "8d43b7c9-152b-4b97-9543-4e0b0c880ecc",
        "stageName": "Stage 2",
        "stageCategory": "Develop",
        "primaryEntity": "account"
      }
    ]
  }
}
```

## 3. List mode — entity_name filter

```
get_business_process_flows(entity_name="account")
```

> Filter BPFs theo primary entity `account` (Display Name). `DisplayNameFirstResolver` → logical name `account`. FetchXML lấy tất cả BPF active, rồi filter client-side theo `primaryentity`. Có 1 BPF: AccountBPF.

**Content (1 dòng):**

```
[Success] active 1 BPF for 'account'.
```

**Structured content:**

```json
{
  "mode": "list",
  "totalCount": 1,
  "entityName": "account",
  "bpfs": [
    {
      "workflowId": "85691a57-80e0-f011-8406-000d3a34c759",
      "name": "AccountBPF",
      "uniqueName": "v4_accountbpf",
      "primaryEntity": "account",
      "status": "Active",
      "businessProcessType": "Business Flow",
      "isManaged": false,
      "owner": "Phước Lê Văn",
      "createdOn": "2025-12-24",
      "modifiedOn": "2026-01-02",
      "modifiedBy": "Phước Lê Văn",
      "stageCount": 2
    }
  ]
}
```

## 4. Auto-detail — bpf_name (1 match)

```
get_business_process_flows(bpf_name="AccountBPF")
```

> `bpf_name` match đúng 1 BPF → auto-detail. Trả về detail với stages đầy đủ (giống test 2).

**Content (1 dòng):**

```
[Success] BPF 'AccountBPF': 2 stages.
```

**Structured content:** (giống test 2 — `mode: "detail"`)

## 5. List mode — include_stages=true

```
get_business_process_flows(include_stages=true, max_records=3)
```

> List mode nhưng kèm đầy đủ stages cho mỗi BPF. Stage category map: 0=Qualify, 1=Develop, 2=Propose, 3=Close, 4=Identify, 5=Research, 6=Resolve, 7=Approval. OOB knowledgearticle BPF dùng stagecategory=-1 → "Custom (-1)".

**Content (1 dòng):**

```
[Success] active 3 BPFs.
```

**Structured content:** (mỗi BPF có thêm `stages` array)

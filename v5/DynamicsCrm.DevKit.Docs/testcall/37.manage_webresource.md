# ✅ TOOL DESCRIPTION

```text
Manage Dataverse web resources — list / detail / create / update / delete.
detail/update/delete need web_resource_id (GUID, Display Name, or unique name); create needs name + file_path + type + solution_name.
On create the prefix in name must match the solution's publisher prefix; on mismatch the tool errors with the suggested name — re-call with it. Create adds the web resource to the solution and publishes; update also publishes; delete is irreversible.
Types: js, html, css, xml, png, jpg, gif, svg, ico, resx, xsl, xap. Naming: {prefix}_/path/filename.ext (e.g. 'v4_/entities/Account.form.js').

WHEN TO USE:
- Upload/update/inspect a web resource
- Find library_name for build_form_xml add_event/add_library (run list first)
- Combine with build_form_xml + manage_form to wire JS into a form
- Creating a JS web resource for view icons/tooltips → the function contract (rowData/{col}_Value, [imgName, tooltip] return) is in docs://instructions_for_views — read it first

RELATED TOOLS:
- get_solution_components → find solution names for create
- manage_form → wire the JS library into a form
- publish_customizations → publish other components (create/update auto-publish)
```

# PARAMETERS

```text
action           : string : list / detail / create / update / delete.
web_resource_id  : string : GUID, Display Name, or unique name. Required: detail/update/delete.
name             : string : Unique name (e.g. 'v4_/entities/Account.form.js'). Required: create. list: contains filter across Display Name and unique name.
display_name     : string : Display Name. Optional: create/update.
description      : string : Description. Optional: create/update.
file_path        : string : Absolute or relative file path. Required: create. Tool reads file and converts to base64 internally.
type             : string : Required: create. See description for values. Ignored on other actions.
solution_name    : string : list: filter. create: REQUIRED — used to resolve publisher prefix and add WR to solution.
type_filter      : string : list only. See type values.
max_records      : int    : 1-500. Default 50.
```

# TEST 1

## INPUT

List with a name contains-filter — `all_/build/entities`, top 5.

```json
{
  "action": "list",
  "name": "all_/build/entities",
  "max_records": 5
}
```

## OUTPUT

```json
{
  "action": "list",
  "totalCount": 3,
  "webResources": [
    {
      "webResourceId": "b82daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Account.js",
      "displayName": "all_/build/entities/Account.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    },
    {
      "webResourceId": "bb2daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Contact.js",
      "displayName": "all_/build/entities/Contact.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    },
    {
      "webResourceId": "590c01be-c86f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Team.js",
      "displayName": "all_/build/entities/Team.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    }
  ],
  "summary": "[Success] 3 web resources found."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 2

## INPUT

List scoped to solution `all_in_one` with `type_filter` js, top 3 — joins solutioncomponent/solution and filters on webresourcetype.

```json
{
  "action": "list",
  "solution_name": "all_in_one",
  "type_filter": "js",
  "max_records": 3
}
```

## OUTPUT

```json
{
  "action": "list",
  "totalCount": 3,
  "webResources": [
    {
      "webResourceId": "b52daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/dialogs/pl_dataversedialogbuilder_demo.js",
      "displayName": "all_/build/dialogs/pl_dataversedialogbuilder_demo.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    },
    {
      "webResourceId": "b82daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Account.js",
      "displayName": "all_/build/entities/Account.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    },
    {
      "webResourceId": "bb2daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Contact.js",
      "displayName": "all_/build/entities/Contact.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    }
  ],
  "summary": "[Success] 3 web resources found."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 3

## INPUT

Detail resolved by unique name (not a GUID) — returns the extended field set (isCustomizable, isHidden, introducedVersion, created/modified by/on).

```json
{
  "action": "detail",
  "web_resource_id": "all_/build/entities/Account.js"
}
```

## OUTPUT

```json
{
  "action": "detail",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": "b82daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Account.js",
      "displayName": "all_/build/entities/Account.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "description": "4.12.34.56",
      "isCustomizable": true,
      "isHidden": false,
      "isEnabledForMobile": false,
      "introducedVersion": "1.0",
      "createdBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "createdOn": "2026-06-24 06:50:59",
      "modifiedBy": "791882ab-20a7-f011-bbd2-0022480965a1",
      "modifiedOn": "2026-06-28 02:06:44"
    }
  ],
  "summary": "[Success] Web resource 'all_/build/entities/Account.js' (b82daa0a-996f-f111-ab0e-0022480a530f), type JS."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 4

## INPUT

Create a JS web resource in solution `all_in_one` from local file `C:\temp\wr36\test36.js` (v1). A pre-check list for `name` contains `test36` returned 0 results, so the base name is free — no numeric suffix needed.

```json
{
  "action": "create",
  "name": "all_/mcp/test36_manage_webresource.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "js",
  "solution_name": "all_in_one",
  "description": "Test web resource for manage_webresource testcall (tool 36)"
}
```

## OUTPUT

```json
{
  "action": "created",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": "eaba2aa1-82a2-f111-aaac-70a8a59cb6e9",
      "name": "all_/mcp/test36_manage_webresource.js",
      "displayName": "all_/mcp/test36_manage_webresource.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false,
      "description": "Test web resource for manage_webresource testcall (tool 36)"
    }
  ],
  "solutionName": "all_in_one",
  "createMode": "RecordCreateThenAddSolutionComponent",
  "isAddToSolution": true,
  "addToSolutionMethod": "AddSolutionComponentRequest",
  "published": true,
  "summary": "[Success] Created web resource 'all_/mcp/test36_manage_webresource.js' (eaba2aa1-82a2-f111-aaac-70a8a59cb6e9): type=JS, added to solution 'all_in_one', published."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 5

## INPUT

Update the web resource created in Test 4 — new content from `C:\temp\wr36\test36_v2.js` plus display_name and description (fieldsUpdated=3), auto-publish.

```json
{
  "action": "update",
  "web_resource_id": "all_/mcp/test36_manage_webresource.js",
  "display_name": "Test36 Manage WebResource",
  "description": "Updated by manage_webresource testcall (tool 36)",
  "file_path": "C:\\temp\\wr36\\test36_v2.js"
}
```

## OUTPUT

```json
{
  "action": "updated",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": "eaba2aa1-82a2-f111-aaac-70a8a59cb6e9",
      "name": "all_/mcp/test36_manage_webresource.js",
      "displayName": "Test36 Manage WebResource",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false
    }
  ],
  "published": true,
  "summary": "[Success] Updated web resource 'all_/mcp/test36_manage_webresource.js' (eaba2aa1-82a2-f111-aaac-70a8a59cb6e9): fieldsUpdated=3, published."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 6

## INPUT

Validation error — action is not one of the valid values.

```json
{
  "action": "invalid_action"
}
```

## OUTPUT

```text
[Error] Invalid action 'invalid_action'.
[Hint] Valid values: 'list', 'detail', 'create', 'update', 'delete'.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 7

## INPUT

Error — detail on a unique name that does not exist.

```json
{
  "action": "detail",
  "web_resource_id": "all_/mcp/not_exist_35.js"
}
```

## OUTPUT

```text
[Error] 'all_/mcp/not_exist_35.js' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use manage_webresource(action='list') to discover web resources.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 8

## INPUT

Error — create with name prefix `wrong_` that does not match solution `all_in_one` publisher prefix `all`; the hint carries the suggested corrected name.

```json
{
  "action": "create",
  "name": "wrong_/mcp/test36_prefix_mismatch.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "js",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] PrefixMismatch: name 'wrong_/mcp/test36_prefix_mismatch.js' has prefix 'wrong' but solution 'all_in_one' publisher prefix is 'all'.
[Hint] Re-call with name='all_/mcp/test36_prefix_mismatch.js' to use the correct solution prefix, or confirm your intended prefix is correct and check solution_name.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 9

## INPUT

Error — create with an invalid type.

```json
{
  "action": "create",
  "name": "all_/mcp/test36_badtype.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "badtype",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] Invalid type 'badtype'.
[Hint] Valid values: js, html, css, xml, png, jpg, gif, svg, ico, resx, xsl, xap.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 10

## INPUT

Error — update a GUID that does not exist.

```json
{
  "action": "update",
  "web_resource_id": "00000000-0000-0000-0000-000000000001",
  "display_name": "X"
}
```

## OUTPUT

```text
[Error] Web resource '00000000-0000-0000-0000-000000000001' not found.
[Hint] Use action='list' to find valid web resource IDs.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 11

## INPUT

Error — list scoped to a solution that does not exist.

```json
{
  "action": "list",
  "solution_name": "not_exist_solution_xyz"
}
```

## OUTPUT

```text
[Error] 'not_exist_solution_xyz' was not found by Display Name or Logical/Unique/Schema Name.
[Hint] Use get_solution_components to find valid solution names.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 12

## INPUT

Error — create with a name that already resolves to an existing web resource (the one created in Test 4); the tool redirects to update.

```json
{
  "action": "create",
  "name": "all_/mcp/test36_manage_webresource.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "js",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] Web resource input 'all_/mcp/test36_manage_webresource.js' resolves to existing web resource 'all_/mcp/test36_manage_webresource.js' (ID: eaba2aa1-82a2-f111-aaac-70a8a59cb6e9).
[Hint] Use action='update' to modify it.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 13

## INPUT

Validation error — create with an empty type.

```json
{
  "action": "create",
  "name": "all_/mcp/test36_empty_type.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "",
  "solution_name": "all_in_one"
}
```

## OUTPUT

```text
[Error] type is required for 'create'.
[Hint] Valid values: js, html, css, xml, png, jpg, gif, svg, ico, resx, xsl, xap.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 14

## INPUT

Validation error — list with max_records=0 (below the 1-500 range).

```json
{
  "action": "list",
  "max_records": 0
}
```

## OUTPUT

```text
[Error] max_records must be between 1 and 500.
[Hint] Use a value from 1 to 500. Default is 50.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 15

## INPUT

Validation error — update with no fields to change (no file_path, display_name, or description).

```json
{
  "action": "update",
  "web_resource_id": "all_/mcp/test36_manage_webresource.js"
}
```

## OUTPUT

```text
[Error] No fields to update.
[Hint] Provide at least one of: file_path, display_name, description.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 16

## INPUT

Error — update a managed, non-customizable web resource (`accessChecker_icon`, picked via a FetchXML pre-check on `ismanaged=1` + `iscustomizable=False`).

```json
{
  "action": "update",
  "web_resource_id": "63bfbf17-aab8-e911-a98a-000d3af475ab",
  "display_name": "Should Fail"
}
```

## OUTPUT

```text
[Error] Cannot update web resource '63bfbf17-aab8-e911-a98a-000d3af475ab' — it is managed and not customizable.
[Hint] Only unmanaged web resources can be updated — use action='list' to find one (isManaged=false), or action='create' your own.
```

## RESULT

```json
{ "IsError": true }
```

# TEST 17

## INPUT

Error — delete the same managed, non-customizable web resource (`accessChecker_icon`).

```json
{
  "action": "delete",
  "web_resource_id": "63bfbf17-aab8-e911-a98a-000d3af475ab"
}
```

## OUTPUT

```text
[Error] Cannot delete web resource '63bfbf17-aab8-e911-a98a-000d3af475ab' — it is managed and not customizable.
[Hint] Only unmanaged web resources can be deleted — use action='list' to find one (isManaged=false).
```

## RESULT

```json
{ "IsError": true }
```

# TEST 18

## INPUT

Dry-run — create a JS web resource in solution `all_in_one` from the same file as Test 4; previewed, not uploaded.

```json
{
  "action": "create",
  "name": "all_/mcp/test36_dryrun.js",
  "file_path": "C:\\temp\\wr36\\test36.js",
  "type": "js",
  "solution_name": "all_in_one",
  "description": "Dry-run test for manage_webresource testcall"
}
```

## OUTPUT

```json
{
  "action": "create",
  "status": "not_executed",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": null,
      "name": "all_/mcp/test36_dryrun.js",
      "displayName": "all_/mcp/test36_dryrun.js",
      "type": "js",
      "typeCode": 3,
      "isManaged": false,
      "description": "Dry-run test for manage_webresource testcall"
    }
  ],
  "solutionName": "all_in_one",
  "createMode": "record_create_then_add_solution_component",
  "isAddToSolution": true,
  "addToSolutionMethod": "SolutionUniqueName",
  "summary": "[DryRun] Would CREATE web resource 'all_/mcp/test36_dryrun.js' (type: js) in solution 'all_in_one'."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 19

## INPUT

Dry-run — update the display name of the existing Account.js web resource (from Test 1); previewed, not changed or published.

```json
{
  "action": "update",
  "web_resource_id": "b82daa0a-996f-f111-ab0e-0022480a530f",
  "display_name": "DryRun Updated Account.js"
}
```

## OUTPUT

```json
{
  "action": "update",
  "status": "not_executed",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": "b82daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Account.js",
      "displayName": "DryRun Updated Account.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false
    }
  ],
  "summary": "[DryRun] Would UPDATE web resource 'all_/build/entities/Account.js' (b82daa0a-996f-f111-ab0e-0022480a530f), 1 field(s)."
}
```

## RESULT

```json
{ "IsError": false }
```

# TEST 20

## INPUT

Dry-run — delete the same Account.js web resource; previewed, not removed.

```json
{
  "action": "delete",
  "web_resource_id": "b82daa0a-996f-f111-ab0e-0022480a530f"
}
```

## OUTPUT

```json
{
  "action": "delete",
  "status": "not_executed",
  "totalCount": 1,
  "webResources": [
    {
      "webResourceId": "b82daa0a-996f-f111-ab0e-0022480a530f",
      "name": "all_/build/entities/Account.js",
      "type": "JS",
      "typeCode": 3,
      "isManaged": false
    }
  ],
  "summary": "[DryRun] Would DELETE web resource 'all_/build/entities/Account.js' (b82daa0a-996f-f111-ab0e-0022480a530f)."
}
```

## RESULT

```json
{ "IsError": false }
```

# RESULTS

- Test 1: list with a name contains-filter matches both name and displayname and returns 3 web resources.
- Test 2: list scoped by solution_name + type_filter joins through solutioncomponent and returns only JS web resources in `all_in_one`.
- Test 3: detail resolves by unique name (not GUID) and returns the extended field set; null fields (languageCode, dependencyXml) are omitted.
- Test 4: create uploads the file as base64, adds the web resource to solution `all_in_one` via AddSolutionComponentRequest, and publishes (`published: true`).
- Test 5: update replaces the content and sets display_name + description (`fieldsUpdated=3`) and publishes.
- Test 6-9: four validation/resolution errors (invalid action, detail not found, prefix mismatch with suggested name in the hint, invalid type) all return `IsError: true` with a `[Hint]`.
- Test 10-11: update on a nonexistent GUID and list on a nonexistent solution both fail with a clear message and hint.
- Test 12: create on an existing name is rejected with a redirect to action='update' — the duplicate gate from the resolver.
- Test 13: create with an empty type fails with the valid type list in the hint.
- Test 14: list with max_records=0 fails the 1-500 range check; the hint states the range and the default.
- Test 15: update with no fields fails; the hint lists the three updatable inputs.
- Test 16-17: update and delete on a managed, non-customizable web resource (`accessChecker_icon`) are both rejected; hints point to unmanaged web resources.
- Test 18: dry-run preview of a create returns `status: not_executed`, `webResourceId: null`, the resolved solution/publish metadata, and a `[DryRun] Would CREATE` summary; nothing is uploaded or added to the solution.
- Test 19: dry-run preview of an update (display_name only) returns `status: not_executed`, the existing name/GUID/type, and a `[DryRun] Would UPDATE ... 1 field(s)` summary; nothing is changed or published.
- Test 20: dry-run preview of a delete returns `status: not_executed` and a `[DryRun] Would DELETE` summary; the web resource is not removed.

# 4. get_tables

# Tool description AI đọc được

```
Name:        get_tables
Title:       Inspect table metadata, columns, and relationships
Idempotent:  true
Destructive: false
ReadOnly:    true

Description:
Dataverse entity metadata. entity_name empty = list; set = detail (attributes, relationships, alternate keys).

WHEN TO USE:
- Discover entity/attribute names before building FetchXML or calling other tools
- Find join columns, picklist options, required fields, primary key, lookup targets
- Clone or copy a column (detail_level='full' required — compact/standard lack clone metadata)
- AI MUST use get_tables for entity/attribute metadata — do NOT use execute_webapi with EntityDefinitions

RELATED TOOLS:
- upsert_column → create/update columns (pass formula_definition unchanged from full mode)
- upsert_table → create/update tables
- upsert_relationship → create/update relationships
- execute_fetchxml → query data once you know attribute names

Parameters:
  entity_name (string, default: "")
    Logical name → detail mode. Empty = list mode.
  filter (string, default: "")
    LIST: keyword filter on entity. DETAIL: single value = prefix match on logical names; comma/pipe/semicolon-separated = word-boundary match on logical name + exact match on display name.
  custom_only (boolean, default: false)
    LIST: only custom entities.
  include_intersect (boolean, default: false)
    LIST: include N:N intersect entities.
  names (string, default: "")
    LIST: comma-separated logical names. Overrides filter/custom_only.
  detail_level (string, default: "compact")
    DETAIL: 'compact' (default), 'standard', or 'full'.
  max_records (integer, default: 100)
    LIST: maximum 1-500 entities. Default 100.
```

# Input tool call

## Test 1 — List bị truncate (regression case phase 4: `totalMatched`)

```json
{
  "name": "get_tables",
  "arguments": {
    "max_records": 5
  }
}
```

> Org có nhiều hơn 5 tables nên kết quả bị cắt — trước phase 4 câu `[Success] N entities returned` gây hiểu nhầm là danh sách đầy đủ.

# Output tool call

**Content (1 line, plain text):**

```
[Success] 5 of 829 entities returned (truncated by max_records; raise max_records to see more).
```

**Structured content (JSON):**

```json
{
  "mode": "list",
  "count": 5,
  "totalMatched": 829,
  "tables": [
    {
      "logicalName": "aaduser",
      "schemaName": "aaduser",
      "displayName": "Microsoft Entra ID",
      "ownershipType": "OrganizationOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": false
    },
    {
      "logicalName": "account",
      "schemaName": "Account",
      "displayName": "Account",
      "ownershipType": "UserOwned",
      "isCustom": false,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "aciviewmapper",
      "schemaName": "ACIViewMapper",
      "displayName": "ACIViewMapper",
      "ownershipType": "OrganizationOwned",
      "isCustom": false,
      "isActivity": false,
      "isAuditEnabled": false
    },
    {
      "logicalName": "actioncard",
      "schemaName": "ActionCard",
      "displayName": "Action Card",
      "ownershipType": "UserOwned",
      "isCustom": false,
      "isActivity": false,
      "isAuditEnabled": false
    },
    {
      "logicalName": "actioncardusersettings",
      "schemaName": "ActionCardUserSettings",
      "displayName": "Action Card User Settings",
      "ownershipType": "UserOwned",
      "isCustom": false,
      "isActivity": false,
      "isAuditEnabled": false
    }
  ]
}
```

**IsError:** `false`

# Input tool call

## Test 2 — List với filter, không bị truncate

```json
{
  "name": "get_tables",
  "arguments": {
    "filter": "devkit"
  }
}
```

# Output tool call

**Content (1 line, plain text):**

```
[Success] 10 entities returned.
```

**Structured content (JSON):**

```json
{
  "mode": "list",
  "count": 10,
  "filter": "devkit",
  "tables": [
    {
      "logicalName": "devkit_city",
      "schemaName": "devkit_city",
      "displayName": "City",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkit_country",
      "schemaName": "devkit_country",
      "displayName": "Country",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkit_invoice",
      "schemaName": "devkit_Invoice",
      "displayName": "Invoice",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkit_invoiceline",
      "schemaName": "devkit_InvoiceLine",
      "displayName": "Invoice Line",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkit_product",
      "schemaName": "devkit_Product",
      "displayName": "Product",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkit_productcatalog",
      "schemaName": "devkit_ProductCatalog",
      "displayName": "Product",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkitv5_order",
      "schemaName": "devkitv5_Order",
      "displayName": "Order",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "devkitv5_orderline",
      "schemaName": "devkitv5_OrderLine",
      "displayName": "Order Line",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": true
    },
    {
      "logicalName": "v4_devkit",
      "schemaName": "v4_DevKit",
      "displayName": "DevKit",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": false
    },
    {
      "logicalName": "v4_devkitv4",
      "schemaName": "v4_DevKitV4",
      "displayName": "DevKitV4",
      "ownershipType": "UserOwned",
      "isCustom": true,
      "isActivity": false,
      "isAuditEnabled": false
    }
  ]
}
```

**IsError:** `false`

# Input tool call

## Test 3 — Detail mode với attribute filter (prefix match)

```json
{
  "name": "get_tables",
  "arguments": {
    "entity_name": "devkit_city",
    "filter": "devkit"
  }
}
```

# Output tool call

**Content (1 line, plain text):**

```
[Success] devkit_city (detail=compact): 4 attributes.
```

**Structured content (JSON):**

```json
{
  "mode": "detail",
  "count": 1,
  "entityName": "devkit_city",
  "filter": "devkit",
  "detailLevel": "compact",
  "table": {
    "logicalName": "devkit_city",
    "schemaName": "devkit_city",
    "displayName": "City",
    "ownershipType": "UserOwned",
    "isCustom": true,
    "isActivity": false,
    "isAuditEnabled": true,
    "primaryIdAttribute": "devkit_cityid",
    "primaryNameAttribute": "devkit_name",
    "attributes": [
      {
        "logicalName": "devkit_cityid",
        "schemaName": "devkit_cityId",
        "type": "Uniqueidentifier",
        "required": true,
        "isValidForCreate": true,
        "isValidForUpdate": false,
        "displayName": "City"
      },
      {
        "logicalName": "devkit_country",
        "schemaName": "devkit_Country",
        "type": "Lookup",
        "required": false,
        "isValidForCreate": true,
        "isValidForUpdate": true,
        "displayName": "Country"
      },
      {
        "logicalName": "devkit_name",
        "schemaName": "devkit_Name",
        "type": "String",
        "required": true,
        "isValidForCreate": true,
        "isValidForUpdate": true,
        "displayName": "Name"
      },
      {
        "logicalName": "devkit_population",
        "schemaName": "devkit_Population",
        "type": "Integer",
        "required": false,
        "isValidForCreate": true,
        "isValidForUpdate": true,
        "displayName": "Population"
      }
    ]
  }
}
```

**IsError:** `false`

# Input tool call

## Test 4 — entity_name không tồn tại (error path)

```json
{
  "name": "get_tables",
  "arguments": {
    "entity_name": "xyz_nonexistent_table"
  }
}
```

# Output tool call

**Content (plain text):**

```
[NotFoundEntity] 'xyz_nonexistent_table' was not found by Display Name or Logical/Unique/Schema Name.
Tip: Use get_tables with no entity_name to list available tables.
```

**Structured content (JSON):**

```json
{
  "error": "[NotFoundEntity] 'xyz_nonexistent_table' was not found by Display Name or Logical/Unique/Schema Name.\r\nTip: Use get_tables with no entity_name to list available tables."
}
```

**IsError:** `true`

# Kết quả AI tổng hợp lại

- **Build dưới test:** CLI `4.44.44.44` build `07.08.2026 17:09:27`, assembly SHA256 `D10BF74C…3C66`.
- **Test 1 (truncate — regression phase 4):** `max_records=5` trên org 829 tables → Content đổi thành `[Success] 5 of 829 entities returned (truncated by max_records; raise max_records to see more).` và structured có thêm `totalMatched: 829`. Không còn ambiguity "danh sách đầy đủ" — fix hoạt động đúng.
- **Test 2 (list không truncate):** kết quả ≤ `max_records` → Content giữ nguyên dạng cũ `[Success] 10 entities returned.`, không có `totalMatched` (backward compatible). `filter` được echo vào structured.
- **Test 3 (detail + filter):** prefix match `devkit` trên logical names → đúng 4 attributes `devkit_*`; summary `[Success] devkit_city (detail=compact): 4 attributes.`; compact level chỉ có các field cơ bản (không có clone metadata — đúng contract).
- **Test 4 (not found):** `[NotFoundEntity]` error có tip actionable, `IsError=true`, không có hint key trong structured (hint=null bị ẩn).
- **Lưu ý:** `totalMatched` chỉ xuất hiện khi bị truncate; `filter`/`entityName`/`detailLevel` chỉ xuất hiện khi có giá trị (`WhenWritingNull`).

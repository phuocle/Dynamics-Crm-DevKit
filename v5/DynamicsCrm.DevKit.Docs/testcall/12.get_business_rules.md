# get_business_rules

# Tool description

```text
Business rules (client-side logic) for an entity. rule_id empty = list; set = detail (conditions+actions from XAML). Rules run BEFORE form JS. Scope 'Entity' = all forms.

WHEN TO USE:
- Inspect a business rule's conditions and actions before refactoring or disabling it
- Find business rules bound to an entity (entity_name filter)
- Check if a field is referenced in any client-side rule logic

RELATED TOOLS:
- get_workflows → classic workflow definitions (background async + realtime sync)
- get_messages → legacy workflow Custom Actions (category=3) + SDK messages
- get_custom_apis → modern Custom API definitions
- get_business_process_flows → BPF definitions + stages
- manage_record(action='read', columns='xaml') → raw XAML when parse is incomplete
```

# Input tool call

**Test 1 — List mode: rules của account (org hiện không có business rule nào — đã kiểm chứng bằng fetchxml workflow category=1 trả 0)**

```json
{
  "entity_name": "account"
}
```

**Test 2 — Validation error: status không hợp lệ**

```json
{
  "entity_name": "account",
  "status": "inactive"
}
```

**Test 3 — Validation error: rule_id không phải GUID**

```json
{
  "entity_name": "account",
  "rule_id": "abc"
}
```

**Test 4 — Error: rule_id GUID hợp lệ nhưng record không tồn tại**

```json
{
  "entity_name": "account",
  "rule_id": "00000000-0000-0000-0000-000000000001"
}
```

**Test 5 — Ownership validation: GUID tồn tại nhưng là BPF (category=4), không phải business rule**

```json
{
  "entity_name": "account",
  "rule_id": "85691a57-80e0-f011-8406-000d3a34c759"
}
```

# Output tool call

**Test 1**

Content (1 line, plain text):

```text
[Success] business rules for 'account': 0 found.
```

Structured content (JSON):

```json
{ "mode": "list", "entityName": "account", "count": 0 }
```

IsError: `false`

**Test 2**

Content (plain text):

```text
Invalid status 'inactive'. Use 'active' or 'draft'.
```

Structured content (JSON):

```json
{ "error": "Invalid status 'inactive'. Use 'active' or 'draft'." }
```

IsError: `true`

**Test 3**

Content (plain text):

```text
'abc' is not a valid GUID.
```

Structured content (JSON):

```json
{ "error": "'abc' is not a valid GUID." }
```

IsError: `true`

**Test 4**

Content (plain text):

```text
Business rule '00000000-0000-0000-0000-000000000001' not found.
Hint: Use get_business_rules in list mode to find a valid ruleId.
```

Structured content (JSON):

```json
{
  "error": "Business rule '00000000-0000-0000-0000-000000000001' not found.",
  "hint": "Use get_business_rules in list mode to find a valid ruleId."
}
```

IsError: `true`

**Test 5**

Content (plain text):

```text
Record 85691a57-80e0-f011-8406-000d3a34c759 is not a business rule (category=4, expected 2).
Hint: Use get_workflows to inspect classic workflows (category=0).
```

Structured content (JSON):

```json
{
  "error": "Record 85691a57-80e0-f011-8406-000d3a34c759 is not a business rule (category=4, expected 2).",
  "hint": "Use get_workflows to inspect classic workflows (category=0)."
}
```

IsError: `true`

# Kết quả

- List 0 rules là Success (`count: 0`), không phải error. Org DEVKITV4 hiện không có business rule nào (đã kiểm chứng: `fetchxml workflow category=1` trả 0 records), nên không có evidence cho detail-happy-path trong môi trường này.
- Validation chain đúng thứ tự: status → rule_id GUID → entity resolve → not found → category check.
- **Ownership validation (Test 5):** GUID của BPF bị reject rõ ràng với `category=4, expected 2` + hint trỏ đúng tool thay thế (`get_workflows`).
- Error outputs đều `IsError: true`, có hint khi cần; không output nào bị rút gọn.
- Description khớp source hiện tại (`GetBusinessRulesTool.cs`).
